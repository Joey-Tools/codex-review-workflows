interface:
  display_name: "Synthetic Token Fixtures"
  short_description: "Choose exact catalog-approved credential fixtures"
  default_prompt: "Use $synthetic-token-fixtures to select exact values through the authoritative synthetic-token catalog CLI."
