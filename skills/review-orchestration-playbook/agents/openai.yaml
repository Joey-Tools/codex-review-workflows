interface:
  display_name: "Review Orchestration Playbook"
  short_description: "Run pinned single, double, triple, or PR-readiness review."
  default_prompt: "Use $review-orchestration-playbook as the single review entrypoint. Preserve the pinned Codex and Claude-family model policy, freeze the review range, record explicit Claude-family egress consent, and report terminal evidence for every requested logical lane."
