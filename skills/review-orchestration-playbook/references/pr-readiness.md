# PR Readiness

Use this reference after the local delivery gate has produced a reviewable commit and the parent request owns PR creation/update, review/CI follow-up, or merge-readiness reporting.

## Authorization

- Confirm repository owner/name, base branch, head repository/branch, draft/ready state, current head commit, dirty state, and merge model.
- Joey-owned/default-authorized repositories may be pushed and opened/updated when the parent request asks for a PR, full workflow, merge-ready, stop-before-merge, or triple review.
- For any other target, stop and request explicit confirmation listing the exact repository, base, head, and draft/ready state.
- These phrases authorize PR creation/update when the target check passes; they never authorize merge.

## Gate Sequence

- Measure supervisor ownership-handoff, checkout, reviewer-launch, and reviewer duration limits with monotonic clocks; wall-clock changes never extend them.
1. Establish or reuse the PR and read metadata, review threads, required checks, rulesets, and branch/base state with the bounded probes in [github-pr-probes.md](github-pr-probes.md).
2. Record the current PR head and freeze the local scope as `<merge_base>..<head_sha>`.
3. Run `offline-frozen-diff-review` first through the stateful pinned Codex helper on the exact frozen range with `--keep-workspace`. Require its retained `preflight.json`, companion `control-artifact-state.json`, and `review.diff` before launching any separate Codex session. The preflight must contain a scanned `primary_diff` attestation whose relative path, byte length, and SHA-256 exactly match that retained `review.diff`; bind those fields, the retained source attempt/path, and the exact `<merge_base>..<head_sha>` range, and reject a diff larger than 128 MiB. Bind the companion control-state SHA-256 for source custody, but do not substitute it for the `primary_diff` attestation. This proves the frozen workspace, diff, and prompt passed the sensitive-content and escaping-symlink checks and supplies the primary evidence that the independent lane will seal into its own checkout. `--keep-workspace` provides initial availability, while an actual shared BSD `flock` lease on that state directory's `cleanup.lock` plus an already-open no-follow source descriptor provides race-safe custody: explicit `stateful cleanup` uses the exclusive side of the same lock and cannot unlink the workspace until destination sealing releases the lease. Do not intentionally request cleanup earlier. If the reviewer runtime becomes unavailable only after that preflight, use the helper-retained frozen workspace for the configured clean-context `reviewer` fallback with the same evidence contract, collect the artifact, and retain the same source custody until the independent copy no longer needs it. If the helper cannot complete the preflight itself, stop instead of bypassing it.
4. After the helper preflight passes, render the independent prompt exactly once into bounded owner-private memory, validate those exact UTF-8 bytes, cap them at 64 KiB, prove the argv budget, and compute their length and SHA-256 without creating a task directory or prompt path. Preserve the complete **Scope And Evidence Budget** and **Parent-Process Output Budget** contracts from [review-lane-contracts.md](review-lane-contracts.md), identify the sealed `.codex-review/review.diff` range/length/SHA-256 as the primary diff evidence, forbid PR orchestration, fixes, other reviewers, CI waiting, tools, web search, MCP/Plugin/App actions, and every external mutation. Run only the self-contained Python 3.13 supervisor at `scripts/independent_codex_pr_review/independent-codex-pr-review`; GitHub Codex and the helper-backed frozen-diff lane cannot replace this gate. The supervisor preflight validates the exact helper state, repo/range/PR, custody evidence, bounded prompt/evidence, Codex executable, authentication generation, and retention/checkout admission without starting Codex. Its `run` uses a verified executable snapshot, owner-only isolated `CODEX_HOME`, app-server stdio, strict no-execution config and session flags, and explicit `gpt-5.6-sol` / `xhigh` requested-observed-effective checks. It loads no normal user configuration, Rules, MCP servers, Apps, Plugins, Skills, Hooks, project instructions, or workspace environment. Follow the [supervisor README](../scripts/independent_codex_pr_review/README.md) for exact `preflight`, `run`, `status`, `final`, `recover`, `release`, and `cleanup` commands.
   Before any worktree registration, attempt directory, or prompt path, acquire the interprocess-exclusive retention-root BSD `flock`, perform the minimum bounded reconciliation needed to authenticate durable retained-worktree state, and first reject any durable `retained-worktree` record as `blocked-worktree-capacity`; uncertainty at that gate is `inconclusive`. Only when that gate passes, authenticate the helper-retained source attempt and its no-follow `preflight.json`, companion `control-artifact-state.json`, and `review.diff`, bind the exact range and matching `primary_diff` relative-path/length/SHA-256 fields plus the companion control-state digest, and use the sanitized bare Git view to enumerate the frozen head and calculate separate conservative checkout-root and common-Git-directory charge components. Keep the helper limits of 64 MiB per blob, 512 MiB aggregate raw blobs, 100,000 entries, 128 MiB tree metadata, and 16 KiB per symlink, plus the independent diff's 128 MiB hard maximum, a separate 1 GiB aggregate checkout-accounting admission cap (the 1 GiB checkout-accounting cap), and a 1 GiB per-filesystem host free-space floor. While retaining the lock, measure and record stable filesystem identity, allocation unit, no-follow allocated blocks, and free space for the retention root, checkout root, and common Git directory. Let `A_checkout = checkout_allocation_unit`. For the mandatory external targeted manifest's fixed-format canonical binary encoding, set `targeted_manifest_format_header_bound = 4096`, `checkout_synthetic_path_bytes_bound = 4096`, `registration_descendant_count_cap = 16`, `registration_path_bytes_cap = 4096`, and use a 192-byte fixed identity/digest record per entry. Independently aggregate the actual frozen raw paths to compute overflow-checked `unique_parent_directory_count` and the sum of those unique parents' full component-wise raw relative-path byte lengths as `unique_parent_path_bytes`; do not infer either from `entry_count`. Consume the bounded NUL-delimited frozen-path stream in authenticated Git tree depth-first order and require strict full raw-path byte ordering, where directory descent compares at the literal `/` byte. Retain only the previous full raw path; the current scan keeps scalar depth/accounting state and no boundary stack. Scan the current raw path exactly once, byte by byte, without a current-boundary collection: simultaneously validate leading/double/trailing slash, depth, strict full-raw-path order with raw bytes—never a component-tuple comparator—and the previous/current common raw-byte prefix. A trailing slash is malformed and fails before parent accounting. At each non-shared parent slash, use that slash index directly as the full relative-path byte length, checked-add the parent, and run the complete projector before reading the next byte; on failure do not read any suffix byte from that same raw record. Only after the entire record succeeds may its raw bytes replace the retained previous path; never split or copy component bytes. Never join or retain all parent prefixes and never build a parent set/trie; peak aggregator memory is linear only in the already-capped longest single raw path/depth, not in the sum of prefix lengths. Before requesting the first frozen-path record or calling the iterable's `__next__`, invoke the same complete projector with `unique_parent_directory_count=0`, `unique_parent_path_bytes=0`, and all known fixed inputs. Baseline overflow, checkout-cap excess, or per-filesystem headroom excess returns the same contract status with `consumed_paths=0` and never touches the path stream. After every newly counted parent, invoke that same overflow-checked monotone partial projector with the already-known full `entry_count`, `tree_metadata_bytes`, `checkout_base_bound_without_parents` (the checkout base bound excluding unique-parent directory allocation), Git-admin bound, `A_checkout`, filesystem identities/headroom, and 1 GiB checkout cap. Using the current partial unique-parent count/path bytes, it recomputes the complete manifest payload, alignment, simultaneous temporary-plus-published factor, `2 * A_checkout` control allocation, the checked `A_checkout * current_unique_parent_directory_count` directory allocation, checkout-root/accounting bounds, and per-filesystem grouped physical lower bound. Any arithmetic overflow or checkout-cap excess fails closed immediately, and any per-filesystem headroom excess returns the contract status `blocked-worktree-capacity` immediately, without consuming the rest of the stream. A simpler parent-only count/byte/accounting cap may be an additional earlier gate but never substitutes for this projector. A malformed/out-of-order path or path/stack bound exhaustion likewise fails closed immediately. Immediately after retrieving and counting a record whose ordinal exceeds the authenticated fixed `entry_count`, but before calling `len`, indexing, or otherwise touching that extra record's contents, return `fail-closed-entry-count-mismatch`; requesting that one next record is solely how excess is detected. Immediately after retrieving and counting a record whose ordinal exceeds the authenticated fixed `entry_count`, but before calling `len`, indexing, or otherwise touching the contents of that extra record, return `fail-closed-entry-count-mismatch`. Requesting that one next record is solely how excess is detected. On otherwise successful EOF, require `consumed_paths` to equal the authenticated fixed `entry_count`; a truncated or extra path stream fails closed instead of returning success. Compute `checkout_manifest_entry_bound = 1 + entry_count + unique_parent_directory_count + 3`, `targeted_manifest_entry_bound = checkout_manifest_entry_bound + 1 + registration_descendant_count_cap`, `targeted_manifest_payload_bound = targeted_manifest_format_header_bound + tree_metadata_bytes + unique_parent_path_bytes + checkout_synthetic_path_bytes_bound + registration_path_bytes_cap + 192 * targeted_manifest_entry_bound`, `targeted_manifest_file_bound = align_up(targeted_manifest_payload_bound, A_checkout) + A_checkout`, and `targeted_manifest_bound = 2 * targeted_manifest_file_bound + 2 * A_checkout`. The checkout leading `1` is the root, Gitlinks are already included in `entry_count`, and `3` covers `.git`, `.codex-review`, and `.codex-review/review.diff`; the next `1` is the registration root and its descendants use the independent count/path caps. The external sibling control namespace is not a manifest descendant record and is charged only by the final allocation term. The format header, checkout synthetic raw-path allowance, and registration raw-path allowance are distinct charges. `tree_metadata_bytes` supplies frozen entry raw path/name bytes while `unique_parent_path_bytes` separately supplies synthesized parent paths; symlinks contribute only exact target byte length and SHA-256, not target bytes. The two file terms charge simultaneous temporary and published allocation and the final two allocation units charge the exact sibling control directory and namespace/inode metadata. Define the checkout-root base before unknown parents as `checkout_base_bound_without_parents`; its final and every partial checkout-root bound adds checked `A_checkout * unique_parent_directory_count`. Add both `review_diff_bound = align_up(review_diff_length, checkout_allocation_unit) + 2 * checkout_allocation_unit` and `targeted_manifest_bound` to the checkout-root bound, `checkout_accounting_bound`, checkout-root physical projection, 1 GiB logical cap, reservation, settlement, and retained-worktree charge. `review_diff_bound` belongs to the checkout ledger and is never the 257 MiB process envelope; `targeted_manifest_bound` also belongs only to that checkout ledger on the checkout-root filesystem, and neither charge belongs to the retention ledger. `review_diff_bound` covers the synthetic namespace directory and sealed diff. Keep the process envelope and checkout charges in separate logical ledgers, but group their unallocated physical headroom by filesystem identity: map the process envelope to the retention-root identity, map the checkout-root and Git-admin bounds to their identities, subtract verified already-allocated blocks once, and conservatively charge a full remainder when attribution is uncertain. After the retained-worktree gate, first apply the 512 MiB process cap and process-only floor (`blocked-retention` on proved shortfall); only if it passes, add checkout headroom and apply the 1 GiB checkout cap and combined per-filesystem floors (`blocked-worktree-capacity` for a newly introduced shortfall). Measurement uncertainty at the applicable step is `inconclusive`. The same identity receives one floor check after combining process, checkout-root, and Git-admin remainders; a different retention-root filesystem receives its own floor check. This is conservative accounting, not an OS quota; repositories needing zero physical overshoot require a quota-backed strict profile. After all ordered checks pass, commit one authoritative `phase=reserved` record containing the 257 MiB process charge, grouped checkout charges including `review_diff_bound` and `targeted_manifest_bound`, all three filesystem identities and projections, the exact intended prompt/worktree paths, one exact owner-only/no-follow sibling control namespace and exact temporary/published manifest paths beneath the authenticated checkout parent, that parent identity, checkout-root filesystem identity, `A_checkout`, every manifest formula input and result including independently derived `unique_parent_directory_count` and `unique_parent_path_bytes` and both registration caps, the prompt length/digest, the helper source bindings, and the exact diff range/length/digest. Immediately after worktree creation, exact no-follow registration-root enumeration updates that reserved lineage with actual descendant count and raw relative-path bytes before materialization; the same enumeration repeats before cleanup, and either observation above `registration_descendant_count_cap` or `registration_path_bytes_cap` fails closed. Only after that record is durable may its lock-custody helper create the owner-only prompt with exclusive no-follow open, exact write, file/directory `fsync`, and no-follow identity/digest readback inside the reserved envelope; prompt verification must finish and all prompt-publication helpers must be reaped before ownership handoff or `worktree-adding`. Any unique-parent aggregation/path-byte or registration enumeration uncertainty, formula overflow, or control-path cross-filesystem mismatch fails closed before construction, and any actual payload/allocation above a bound fails closed before construction. If admission fails or reservation durability is uncertain, report the ordered `blocked-worktree-capacity`, `blocked-retention`, or `inconclusive` result with review status `not-run` and create no prompt artifact, checkout, or reviewer. A crash in `reserved` may leave its recorded prompt path absent, partial, or exact, but all forms retain the full process charge and cannot advance without exact cleanup or verification.
   While the attempt is still `reserved` and before any checkout worker or phase helper exists, complete a separate 30-second supervisor ownership-handoff. Start one small native attempt supervisor through the PID-first trampoline with an outer-liveness/control channel and the same-open-file-description publication lease. Require exec acknowledgement and authenticated ready with no side effects; the outer process durably binds its PID/start identity, handoff token, `handoff=pending`, and the exact prompt length/digest, then reaps that state helper before sending an authenticated offer containing the original prompt bytes from owner-private memory. Offer receipt is the supervisor's first side effect: it verifies those bytes against both the offer and the durably published prompt, keeps the original bytes in private memory through Codex spawn, durably publishes `handoff=accepted`, exactly reads back that generation, reaps the helper, and returns an authenticated acceptance record. The outer process verifies acceptance, irreversibly stops publishing attempt state, and only then sends an authenticated start ACK. That ACK authorizes a bounded complete-state attempt but is not an ownership linearization point and does not stop the handoff deadline. Before ACK, EOF, timeout, malformed control, or peer loss cancels the incomplete handoff immediately. After ACK but before durable completion, outer EOF is latched while the supervisor attempts to publish/read back `handoff=complete` plus `process_owner=attempt-supervisor` and reap the helper. Only successful publication, exact readback, and helper reap linearize ownership; any latched EOF then immediately follows the complete-handoff abandonment path. If that complete-state attempt blocks, fails, times out, or remains unverifiable, the supervisor starts no later child, terminates and reaps its live trampoline/handoff/state helpers, closes every control/custody/writer endpoint and publication lease, and exits so recovery can acquire the lock; after outer quiescence it publishes no exact settlement and leaves the last incomplete generation plus full envelope charged. On verified completion, the outer process closes only its lock reference and retains the sole liveness/control peer plus supervisor handle through terminal authorization or explicit abandonment. The complete-handoff supervisor is thereafter the sole state writer and the common direct parent of the checkout worker, every phase helper, and the app-server leader; no checkout operation, protocol endpoint, or Codex process may start sooner. Failure or uncertainty before complete retains the full envelope, no worker/helper may exist to escape that settlement rule, and an incomplete supervisor never holds its lease indefinitely.
   After durable handoff complete, the attempt supervisor creates the clean detached worktree while owning one 10-minute checkout deadline, a checkout-worker PGID, and short-lived clean-exec phase-commit helpers. It starts each direct child through the fixed PID-first trampoline and requires its authenticated post-exec ready record inside that deadline; no parent-side exec wait may escape supervision. All potentially blocking Git, materialization, state write/rename/`fsync`, readback, traversal, and accounting work stays in those killable children so the supervisor can terminate and reap the active helper and anchored checkout PGID on deadline or outer-liveness EOF. The checkout worker pauses at supervisor-controlled phase gates and never inherits the retention lock. Each serialized phase helper additionally holds a publication lease that is a duplicate of the supervisor's same-open-file-description BSD `flock`, verifies the expected predecessor generation/SHA-256, and keeps that lease through rename, file/directory `fsync`, exact readback, success, and exit. The supervisor reaps it before releasing the worker, starting another writer, or unlocking; after supervisor loss, a successor cannot acquire the lock until the old helper can no longer publish. Use only `git worktree add --detach --no-checkout --no-guess-remote --lock --reason independent-codex-pr-review <exact-path> <head_sha>` with hooks and fsmonitor disabled, populate the index with `read-tree --reset <head_sha>` without `-u`, and materialize the exact raw blobs through bounded `cat-file --batch`; never execute `post-checkout`, filters, LFS, or normal checkout commands. Before consuming any ordinary-file blob or retained-diff payload, establish the target filesystem's name-equivalence semantics and reject every base/head manifest or staging alias of `.git` or the top-level synthetic `.codex-review` namespace. Then run a bounded symlink-target-only pre-materialization pass: through raw `cat-file --batch`, read and cache every symlink target referenced by the frozen base and head manifests and no regular-file or retained-diff payload. Verify each requested object ID, returned `blob` type, manifest-declared length, exact payload/delimiter, recomputed repository-object-format digest, producer success, and final stdout EOF. Each target is nonempty, NUL-free, and at most 16 KiB; overflow-checked target reads plus every later frozen-head regular-file blob read must remain within the existing 512 MiB aggregate raw-blob limit, and a cached head target is not reread or charged twice. This pass executes no hook or filter, invokes no LFS process, performs no lazy fetch, and accesses no network. Using only the authenticated cache, manifests, and proven name semantics, validate the complete base/head symlink graph and reject any absolute, transiently escaping, ultimately escaping, looping, unstable, `.git`-resolving, or `.codex-review`-resolving graph. Only after that graph/control-namespace validation succeeds may the worker build the exclusive no-follow skeleton with ordinary placeholders for both regular and symlink leaves and create its synthetic control manifest: `.git`, the exact head manifest, one real owner-only `.codex-review` directory with mode `0700`, and one empty ordinary `.codex-review/review.diff` placeholder with mode `0600`. No other synthetic path is allowed. During subsequent regular-file materialization, perform the content-only Git LFS v1 pointer check before copying bytes through the identity-verified FD; for Git modes `100644` and `100755`, apply exact `0644` or `0755` with `fchmod` and `fstat`-verify the executable-bit mapping and inode before launch. For symlinks, use the authenticated cached target without another blob read, create and verify one alias-free staged symlink, and atomically exchange it with its identity-verified placeholder using `RENAME_EXCHANGE` or `RENAME_SWAP`; verify both post-exchange identities and exact `readlinkat` bytes. Never use unlink-then-symlink or an overwriting rename. Unsupported atomic exchange, `EEXIST`, identity/name drift, followed materialization traversal, or an absolute, transient, or ultimate symlink escape aborts prelaunch as `blocked-checkout-atomic-symlink` or the applicable validation failure. If sound name semantics cannot be established, report `blocked-checkout-name-semantics`.
   During the 10-minute checkout deadline, use the already-open retained `review.diff` descriptor received through the source-custody handoff; never reopen its pathname. Reverify its bound identity and metadata with `fstat`, rewind it, and stream exactly the attested length into the identity-matched empty placeholder while computing SHA-256; require source EOF and the preflight-attested digest. Do not use `cp`, hard links, reflinks, symlinks, or path replacement. Then `fsync`, `fstat`, close, reopen no-follow, and exactly read back the destination identity, mode, length, SHA-256, and bytes. Feed every materialized tracked path and `.codex-review/review.diff`, including unchanged nearby evidence, through the bounded frozen-index `filter` / `working-tree-encoding` query; any active conversion is `blocked-checkout-attributes`. Verify the raw mode/object/path manifest and filesystem executable bits rather than running `git status` or `diff-files`; require `git ls-files` to equal the exact head manifest and the actual filesystem names to equal exactly `.git` plus the head manifest plus the two-entry synthetic control manifest, with no staging or other extra path. Close all materializer handles, verify allocated bytes stay within the reserved checkout charge, canonical containment, detached registration, exact `HEAD`, and exact object and sealed-diff readback before reviewer launch. Only after the destination seal and directory durability are recorded may the supervisor close the source descriptor and shared helper-cleanup lease; `stateful cleanup` may then take its exclusive lock. An attempted concurrent cleanup instead waits or times out without deleting the source. A later cleanup failure retains that workspace and records a separate cleanup warning; it does not invalidate an independently sealed and revalidated diff.
   After checkout, sealed-diff, bounded-evidence, executable-snapshot, auth-generation, and strict app-server configuration validation succeed, the attempt supervisor persists `spawn-intent`. The distinct reviewer-launch deadline covers app-server child startup, runtime/config inventory verification, thread creation, and durable `launched` persistence; the earlier ownership handoff and 10-minute checkout are not charged to this window. Outer-liveness loss before final authorization rejects the result and drives bounded closure rather than authorizing a late launch.
   The supervisor launches only its verified Codex snapshot as `codex app-server --session-source exec --strict-config ... --stdio`, without a shell. The child receives an owner-only isolated `CODEX_HOME` and scrubbed environment. The `config/read` session flags, user instructions, and system instructions must match the pinned no-execution inventory before the model request; any missing or extra tool, web, MCP, App, Plugin, Skill, Hook, subagent, shell, remote-control, memory, history, environment, or project-instruction capability fails closed.
   Keep app-server protocol traffic and bounded diagnostics outside the parent transcript. Preassemble the complete diff and permitted nearby context into the bounded evidence bundle before launch; never give the reviewer a checkout path or filesystem capability. Only the supervisor-authenticated final response may be copied into the fresh owner-only final artifact, which is `fsync`ed, sealed by device/inode/length/SHA-256, independently read back, and bound to the direct predecessor authorization before a clean/findings terminal tuple may be committed.
   Launch each app-server child with `pid == pgid == session` under the authenticated no-child profile and durable process ledger. The owner retains every handle through bounded termination, reap, stdio closure, and settlement. A deadline, cap, unexpected child, process drift, or closure uncertainty makes the review `inconclusive`; exact same-boot settlement remains owner-only, while a verified boot change uses the documented conservative recovery matrix.
   Record and verify the source and snapshot executable identities, authenticated auth generation, requested model/effort, and app-server observed/effective model and effort. Accept only exact `gpt-5.6-sol` and `xhigh`; missing, mismatched, fallback, or ambiguous runtime/config evidence rejects the final artifact rather than being reported as a nonblocking observational difference.
   Keep active and retained process artifacts in one private retention root capped at 512 MiB. Serialize accounting, reclaim, reservation, prompt publication, early ownership handoff, checkout, spawn, and settlement with the retention-root lock and one generation/digest-chained authoritative attempt-state record whose durable phases remain `reserved` -> `worktree-adding` -> `validating` -> `spawn-intent` -> `launched`. `handoff=pending`, `accepted`, and `complete` are substates of `reserved`; durable complete plus the exact supervisor identity/prompt binding is an invariant of every later phase. Persist each phase before its external operation. A no-Codex-child prompt/checkout/validation/spawn failure becomes `prelaunch-aborted` with review status `not-run`; once a Codex direct child exists, failed exec acknowledgement or failed `launched` persistence remains `spawn-intent` plus launch-uncertain, never `prelaunch-aborted`.
   Recovery follows the exhaustive matrix in [review-lane-contracts.md](review-lane-contracts.md). In `reserved` before complete, the outer owner may settle only under the existing reaping/quiescence proof; after complete, a live same-boot supervisor with the original identity is the sole owner and may terminate/reap its children and exact-settle a no-child attempt as `prelaunch-aborted`. In `worktree-adding` or `validating`, that same complete-handoff supervisor must latch outer EOF, terminate/reap the active helper and anchored checkout PGID, reread the exact generation, and settle process status as `not-run`; checkout settlement stays separate. `spawn-intent` without a valid complete-handoff binding is corrupt and retains the full envelope; with that binding, the direct-child rule determines `not-run` versus launch-uncertain. `launched` retains the existing direct-child, terminal-authorization, and artifact rules. Incomplete handoff, a missing or mismatched supervisor tuple, uncertain closure, or a different same-boot owner retains the full 257 MiB envelope. A verified recorded boot-ID change permits a complete exact-settlement tuple for every phase. Prelaunch phases remain `not-run`, uncertain/abandoned spawn and boot-change-recovered launched phases are `inconclusive`, and process settlement remains independent from checkout and cleanup/logging settlement. The current owner releases its lock reference in `finally`, but a live supervisor or phase helper's same-open-file-description publication lease continues excluding successor owners until it exits. Before destructive checkout cleanup when checkout side effects may exist, atomically publish, file/directory `fsync`, and exactly read back a non-TTL `retained-worktree` record with `checkout_settlement=outstanding`, the full grouped checkout charges, both exact entry identities/types for the checkout and registration, both parent-directory device/inode identities and containment bindings, `cleanup-warning`, and `blocked-worktree-capacity`; that durable fallback blocks later worktree admission until exact checkout settlement succeeds.
   After process-group and reader cleanup is safely complete, re-prove exact ownership and canonical containment. Before destructive checkout cleanup, require the already-durable fail-closed record. After acquiring the retention-root lock, authenticate that outstanding durable `retained-worktree` record, then acquire and revalidate the already-open or freshly reopened no-follow stable descriptors for the exact checkout parent directory and the common Git `worktrees` directory (the exact Git-admin parent namespace containing the matching registration); bind and revalidate both directory identities before any removal decision. Probe the two recorded names relative to those descriptors. Recovery is idempotent only while the original cleanup owner or one still-live guardian continuously retains the exact authenticated checkout-parent, Git-admin-parent, and present-root open file descriptions plus the same-open-file-description lock lease. An authenticated `SCM_RIGHTS` handoff may transfer those same descriptions and deletion-progress records without a custody gap; reopening any path is successor recovery, not continuity. Numeric descriptor values, device/inode pairs, owners, modes, link counts, timestamps, or digests are not cross-crash custody capabilities, and `fstatat` followed by `unlinkat` is not an atomic compare-and-unlink. The checkout-cleanup boundary is cooperative same-UID namespace ownership: the checkout and Git-admin roots are owner-private, and every lane-controlled writer is quiescent or excluded for the full traversal. A noncooperating same-UID process that can mutate those directories, including an adversarial swap between `fstatat` and `unlinkat`, is outside this no-container threat model; any observed drift still fails closed before another unlink. If this boundary is unacceptable or cannot be established, mixed state is manual-recovery-only before deletion. If both exact names are absent before targeted traversal starts, skip removal. If both are present with their recorded identities, use the normal bounded sanitized `git worktree unlock <exact-path>` and `git worktree remove --force <exact-path>` branch. Initially authenticate checkout-present/registration-absent by revalidating the checkout root, ordinary `.git` marker and common/admin binding, plus bounded no-follow proof that the exact registration and every alias registration for that checkout are absent. Initially authenticate checkout-absent/registration-present by revalidating the registration root and its `gitdir` binding to the recorded checkout, `.git`, and common directory, plus same-descriptor proof that the checkout side is absent. Before either mixed branch enters destructive traversal, use a no-follow descriptor-relative bounded traversal through those continuously retained descriptions to construct a complete descendant manifest of the present root. For every root and descendant entry, bind its component-wise relative path and exact raw name bytes, no-follow type, device/inode/owner/link count, and, for a symlink, its exact no-follow `readlinkat` byte length and SHA-256 rather than the target bytes. This manifest is same-live-custody revalidation input, not generic successor authority. The manifest payload is never embedded in attempt state. The same lock-custody phase helper must use only the exact reservation-bound sibling control namespace and temporary/published paths beneath the authenticated checkout-parent descriptor, revalidate that parent identity, checkout-root filesystem identity, and `A_checkout`, create the namespace owner-only/no-follow, and serialize the fixed-format canonical binary payload. Construction must remain within `targeted_manifest_entry_bound`, `targeted_manifest_payload_bound`, and the allocation terms of `targeted_manifest_bound`; any excess fails closed before targeted deletion. The helper publishes the external manifest owner-only without replacement, file/directory `fsync`s it, and verifies its exact no-follow path, parent binding, filesystem identity, file identity, length, SHA-256, allocation, and exact-byte readback. It then atomically persists, file/directory `fsync`s, and exactly reads back a small branch-specific `targeted-removal-intent` within the existing fixed control-metadata budget. The intent embeds only the external manifest path, identity, length, and SHA-256—never its payload—and binds the predecessor generation/digest, branch enum, continuous-custody owner/guardian identity, exact open-description identities, present exact name/root identity, both parent identities/containment, missing-side same-descriptor exact-name and alias-absence proof, traversal bounds, and an initially empty same-live deletion-progress set. Only after exact readback of the intent and manifest may the live custodian delete the checkout `.git` marker, registration `gitdir` leaf, or any other entry. The bounded no-fork remover authenticates each manifest entry immediately before descriptor-relative no-follow bottom-up unlink and records each successful unlink in the authenticated live custody channel before continuing. A retry by that same uninterrupted owner/guardian chain first exact-readbacks the intent/manifest and re-enumerates through the same open descriptions. Surviving entries must be an exact manifest subset with unchanged identity; a missing entry is acceptable only when the continuous chain already recorded its successful unlink. An unexplained absence may be a moved-out object and is not deletion proof. Any injected, replaced, recreated, moved-in, parent/root-replaced, or identity-drifted entry fails closed before another unlink. The same-live chain may therefore continue after deleting the checkout `.git` marker or registration `gitdir` leaf, and may enter the double-absence barrier after it recorded removal of the intended root. If that chain dies or loses any exact description/lock after targeted traversal starts but before exact settlement, a successor must record `manual-recovery-required`, retain the non-TTL `retained-worktree`, full grouped checkout charges, `cleanup-warning`, and `blocked-worktree-capacity`, and perform no further unlink or rename; path reopen, numeric device/inode equality, or a manifest subset never authorizes automatic continuation. This manual state does not invalidate an already sealed final artifact. An optional cross-crash backend is opt-in, never the default. It must prove stable durable handles for both parents, the root, and every descendant; authoritative deletion-versus-move evidence for every absent entry; and atomic manifest capture or namespace write exclusion. Unsupported, permission-denied, incomplete, or stale handles fail closed into the same manual state. Never run broad `git worktree prune`, remove sibling registrations, recursively delete an unauthenticated path, or use a quarantine rename. Every successful branch—initial double absence, normal both-present removal, or continuously custodied targeted removal—then `fsync`s both already-open parent descriptors, revalidates both parent identities, and uses those same descriptions to re-prove both exact names and every alias registration absent without following aliases. After that absence proof and before checkout settlement, exact-remove the mandatory manifest temporary and published files and then their empty sibling control namespace through the authenticated checkout-parent descriptor, `fsync` that parent and every affected directory, and exact-readback absence of all three reservation-bound names; cleanup uncertainty retains the full `targeted_manifest_bound` subcharge and blocks settlement. Only then may the same lock-custody phase helper atomically persist, file/directory `fsync`, and exactly read back `checkout_settlement=exact`; checkout charges are released only at that exact settlement readback. Any recovery failure—or manifest construction/publication/readback/cleanup, intent publication/readback, custody continuity, namespace exclusion, targeted traversal/removal, Git removal, helper reap, directory `fsync`, parent revalidation, absence recheck, or settlement uncertainty—keeps the full grouped checkout charges and the already-durable `retained-worktree` plus `cleanup-warning`, reports allocated bytes and `blocked-worktree-capacity`, and blocks every later worktree admission until controlled reconciliation completes this exact protocol; do not age it out under the process-artifact TTL. Safely closed process evidence moves monotonically through `active/unsafe` -> `held` -> `released` -> `reclaimed`; only explicit `resolved` or `handoff-complete` starts its 7-day TTL at `released_at`. Pending-handoff and unresolved evidence has no age-based eviction. Worktree cleanup warnings and manual-recovery state do not invalidate an already sealed final artifact.
   Do not fully read or hash the up-to-128 MiB retained source while holding the independent retention-root lock before supervisor ownership. Admission uses the bounded matching preflight/control records and no-follow metadata for sizing. Start the 30-second handoff clock before one bounded no-fork custody helper acquires the helper state's shared `cleanup.lock` lease and opens the source no-follow; it returns both open descriptions through an authenticated bounded `SCM_RIGHTS` record and is reaped. After the supervisor's authenticated ready record, the outer owner transfers exactly those descriptors over the existing control channel before `handoff=pending`, retains its copies until durable completion, and records their identities in every handoff substate. Incomplete handoff closes both owners' copies; complete handoff leaves the supervisor as sole custodian. Neither descriptor reaches the reviewer, and the potentially large read/digest/copy is charged only to the later 10-minute checkout deadline.
   The isolated profile has no customization exception: it supplies no normal user or organization-managed configuration, Rules, MCP servers, Apps, Plugins, Skills, Hooks, notifications, tracked project content, project instructions, or workspace environment. The strict config/session inventory is an admission requirement, not a best-effort request that managed policy may override.
5. Add the requested logical review shape without removing either PR-readiness Codex gate:
   - explicit double review adds the Claude-family lane;
   - explicit triple review adds the Claude-family lane and requires GitHub Codex review;
   - a request for double/triple review alone remains exactly the named two/three logical lanes; the extra independent/offline gates apply only when the parent also requests PR readiness, full workflow, or merge-ready.
6. GitHub Codex review:
   - default PR readiness treats an absent, non-required review as best-effort skipped;
   - an already-triggered or required review must finish clean on the current head;
   - explicit triple review requires current-head evidence, using repository automatic review or the exact `@codex review` trigger when needed.
7. Process actionable findings, requested changes, unresolved conversations, and required CI. Fix in the parent thread, rerun affected tests, freeze the new head, and rerun every invalidated requested or PR-readiness review lane.
8. Recheck that the PR is current with its base and that both required Codex gates, all requested logical review lanes, required checks, and required conversations are terminal and clean.

### Raw Git LFS Reference-Compatible Pointer Gate

The materializer knows every regular blob's declared size before consuming its payload. Independently of current-head attributes, it must exact-buffer every **nonempty** Git regular-file blob whose declared raw blob size is strictly less than 1024 bytes in at most 1023 bytes and locally apply a `git-lfs` 3.7.1 `lfs.DecodePointer`-equivalent, content-only recognizer. Empty blobs are harmless/pass-through; blobs of at least 1024 bytes are outside this content gate. The recognizer trims outer whitespace, uses Go `bufio.ScanLines` behavior, ignores only zero-length `ScanLines` records, splits every remaining line at the first ASCII space, and rejects an internal whitespace-only line rather than treating it as blank. It accepts the exact v1 aliases `http://git-media.io/v/2`, `https://hawser.github.com/spec/v1`, and `https://git-lfs.github.com/spec/v1`. Core keys must advance in `version` -> `oid` -> `size` order, but reference-valid extensions may appear before or between those core keys and may be unsorted; extensions after `size` are rejected. Validate the main and extension OIDs as exact `sha256:` plus 64 lowercase hexadecimal digits, parse size exactly as a nonnegative base-10 signed-64-bit integer (including upstream-accepted `+0001`, leading zeros, and `-0`), and reproduce upstream extension-key/prefix, priority, duplicate-key overwrite, duplicate-priority, and sorting semantics rather than replacing them with a stricter canonical regex. Therefore outer whitespace, CRLF, true zero-length records, missing final LF, either legacy alias, and reference-accepted noncanonical extension placement/order still block. Any nonempty accepted pointer is prelaunch `blocked-checkout-lfs-pointer` with `review_status=not-run`; invalid version/OID/size/extension near-misses pass this detector only after exact local rejection. A PR cannot bypass it by deleting or rewriting `.gitattributes`. Detection is offline and reads only the already streamed raw blob: do not invoke `git lfs`, consult an LFS object store, download LFS content, perform lazy fetch, or access the network. Failure to complete the required exact check is `inconclusive`; the frozen-head attribute query remains an independent second gate, neither gate substitutes for the other, and attributes are never the sole LFS-pointer detector.

## Review Counting

- The pinned Codex helper or clean-context `reviewer` fallback is one logical Codex lane.
- Claude Code and its Copilot runtime/model fallbacks are one logical Claude-family lane.
- GitHub Codex review is the third logical lane only for triple review.
- `independent-codex-pr-review` and `offline-frozen-diff-review` are separate full-PR-readiness evidence gates. They do not redefine what a standalone double/triple-review request means.
- CI, comments, branch status, model retries, and helper fallback implementations do not increase the named review shape.

## Terminal Report

Report:

- PR URL and current head
- frozen local review range
- independent Codex PR review verified source/snapshot executable and auth-generation metadata; exact requested/observed/effective `gpt-5.6-sol` / `xhigh`; final seal; admission, launch, review, process/checkout settlement, recovery, cleanup, retention, and failure-stage statuses; and exact retained attempt/checkout paths when cleanup is not yet authorized
- offline frozen-diff Codex runtime/model/effort/status
- Claude-family lane runtime/model/effort/status when requested
- GitHub Codex trigger/head/status when requested, already present, or required
- required CI and conversation-resolution status
- branch/base state
- `merge-ready`, `blocked`, or `inconclusive`

Stop before merge unless Joey explicitly asks to merge.
