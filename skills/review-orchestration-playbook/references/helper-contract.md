# Isolated Review Helper Contract

The canonical helper is:

```bash
$HOME/.codex/skills/review-orchestration-playbook/scripts/isolated_review
```

It runs one low-level supplied-diff reviewer against one frozen Git range. It is retained for compatibility, helper-security maintenance, and targeted runtime debugging. It is not the canonical implementation of single, double, or triple review; the parent skill composes named lanes with separate clean Git worktrees and no prepared reviewer diff.

## CLI

Legacy helper Codex foreground invocation (does not satisfy canonical single review):

```bash
isolated_review \
  --repo /path/to/repo \
  --reviewer codex \
  --base-ref <base_sha> \
  --head-ref <head_sha>
```

Stateful:

```bash
isolated_review stateful start \
  --repo /path/to/repo \
  --reviewer claude \
  --egress-consent explicit-claude-review \
  --base-ref <base_sha> \
  --head-ref <head_sha>

isolated_review stateful status --state-dir <state_dir>
isolated_review stateful wait --state-dir <state_dir>
isolated_review stateful final --state-dir <state_dir>
isolated_review stateful admission --state-dir <state_dir>
isolated_review stateful cleanup --state-dir <state_dir>
```

Add `--include-source-wip` only with separate explicit consent to include staged, unstaged, and nonignored untracked source state. Clean committed-head content is the default.

For an approval-gated external helper launch, use the wording that matches the verified variant and substitute the exact values:

- Clean-head approval: `Approve this low-level Claude helper run for <owner/repo> at <base_sha>..<head_sha>. It sends tracked blobs materialized from the frozen head, the complete generated diff, and the prompt/result to Anthropic Claude Code for read-only review. Scope is bound by content_variant=head, snapshot_tree_sha=<snapshot_tree_sha>, and scope_identity=<scope_identity>. It excludes untracked files, unrelated repositories, and broad workspace/home-directory content.`
- Source-WIP approval: `I separately authorize --include-source-wip for this low-level Claude helper run for <owner/repo> at <base_sha>..<head_sha>. It sends tracked blobs plus staged, unstaged, and nonignored untracked contents captured in the digest-bound source WIP snapshot, the complete generated diff through that snapshot, and the prompt/result to Anthropic Claude Code for read-only review. Scope is bound by content_variant=source-wip, snapshot_tree_sha=<snapshot_tree_sha>, and scope_identity=<scope_identity>. It excludes ignored untracked files and source content not captured by the WIP snapshot, unrelated repositories, and broad workspace/home-directory content.`

Do not use the clean-head wording for a WIP snapshot. See [egress-consent.md](egress-consent.md) for the full provider and trusted-processor disclosure.

Synthetic-token catalog inspection is read-only:

```bash
isolated_review synthetic-tokens validate
isolated_review synthetic-tokens list --json
isolated_review synthetic-tokens get <id> --json
isolated_review synthetic-tokens list-exemptions --json
isolated_review synthetic-tokens audit-master --repo <path> --ref <sha> --exemption <id>
```

Review commands retain repeatable `--synthetic-secret-exemption <id>` only as deprecated compatibility syntax. Known IDs may still be validated, but selection must not change the scanner, counter, path rules, or admission outcome. Read [synthetic-token-fixtures.md](synthetic-token-fixtures.md) before changing the catalog or removing this compatibility surface.

Always pass `--state-dir`; it is not positional. `stateful wait --timeout-seconds` accepts only a non-negative finite value, bounds the caller's wait, and does not kill or downgrade a healthy reviewer. `stateful cleanup` explicitly removes a workspace retained by `--keep-workspace` or for helper-specific recovery while preserving state artifacts. A retained supplied-diff/private-Git helper workspace must never be handed to the canonical fresh-context Codex lane.

Canonical PR/master admission and `merge-ready` use the separate direct `isolated_review secret-admission` command, which starts no reviewer. This helper's `stateful final` / `stateful admission` pair is retained only when a low-level helper run was independently requested: collect final first, then query admission against that same state and current head. Those helper results remain independent—final may succeed when helper-state admission reports violations or an inconclusive bounded audit. Helper-state admission exit `0` means `clean`, exit `1` violations, exit `3` pending, and exit `75` inconclusive; none makes this helper a required PR-readiness producer. Foreground mode produces no helper-state admission evidence. A head change invalidates both helper results. Secret admission never delays, suppresses, redacts, or gates reviewer launch; the reviewer continues to receive the original consented tracked scope.

The parent acquires an exclusive runner lock at the first preparation cleanup handoff, before any helper-private secret bytes are written, and passes that same file descriptor to the child for the child's full lifetime. It also passes the selected reviewer and exact optional egress-consent value as child argv. Terminal `_run-state` validates the inherited lock first, requires the path-loaded state to match both trusted argv values exactly, and launches only with those argv-bound values. Replacing `state.json` therefore cannot switch a Codex-only run into Claude or Copilot egress. Cross-process `status` / `wait` trusts the lock, not PID existence, so a reused PID cannot masquerade as the review runner or race cleanup during preparation.

## Low-Level Helper Reviewers

`--reviewer codex`:

1. `gpt-5.6-sol`, `xhigh`
2. `gpt-5.5`, `xhigh`, only after explicit model entitlement/policy denial

This is the legacy supplied-diff Codex runtime. It does not load the normal instruction stack or expose the clean Git workspace required by canonical single review, so its result cannot satisfy a named review shape.

`--reviewer claude`:

1. Claude Code `claude-opus-4-8`, `max`
2. Claude Code `claude-opus-4-7`, `max`, only after explicit organization-policy or model-entitlement denial for Opus 4.8
3. Copilot CLI `claude-opus-4.8`, `max`, only when the secure Claude Code runtime is deterministically absent/unavailable or both Claude Code Opus models are entitlement-blocked; authentication failure never reaches this step
4. Copilot CLI `claude-opus-4.7`, `max`, only after the same explicit organization-policy or model-entitlement denial for Opus 4.8

This helper lane uses a supplied-diff workspace backed by private minimal Git. Even when the actual runtime is Claude Code, it does not satisfy the canonical double/triple Claude lane because it lacks the complete clean-Git/no-prepared-diff contract. Any Copilot result is a low-level fallback result only and never counts as Claude Code.

Claude Code CLI releases are not pinned to one current patch. The Claude runtime runs in safe mode and accepts publisher-verified releases `>=2.1.211,<3.0.0` only when the detected platform, outer sandbox, credentials, launch flags, safe-mode semantics, and structured output contract pass. The `2.1.211` floor trusts current credential-store coordination, refresh behavior, and explicit login-expiry signals; it replaces the old helper-managed freshness warmup without changing the signed-manifest and capability-range model. Codex CLI and Copilot CLI likewise have no exact executable-version pin; their existing identity, capability, and output verification remains authoritative. These CLI policies do not change the explicit reviewer-model policy above: model IDs remain intentionally pinned.

The Copilot IDs follow GitHub's authoritative [supported models matrix](https://docs.github.com/en/copilot/reference/ai-models/supported-models), which lists Opus 4.8 and Opus 4.7 for Copilot CLI. The shorter command-reference example table can lag product availability and is not treated as a runtime allowlist; exact model verification still fails closed if the installed CLI rejects or substitutes any ID.

The low-level Claude helper requires either `--egress-consent explicit-claude-review` or `--egress-consent explicit-claude-with-copilot-fallback`. The helper saves this value in state and writes `egress.json`; it refuses to start the external lane without it. `explicit-claude-review` authorizes Anthropic only. `explicit-claude-with-copilot-fallback` is valid only after the user separately requests and authorizes both the Anthropic review and this helper's compatibility GitHub Copilot fallback. Named single/double/triple phrases are not helper consent markers. No Copilot artifact satisfies the required Claude Code lane, and no consent mode turns an authentication problem into fallback eligibility. Each egress record also binds the verified `content_variant`, `snapshot_tree_sha`, and `scope_identity`. Its `include_source_wip` machine marker must be `false` for `content_variant: head` and `true` only for `content_variant: source-wip`, corresponding to explicit `--include-source-wip`; unknown or inconsistent variants fail closed before reviewer launch. The variant-specific `included` and `excluded` lists must match the same verified scope.

Every new state and `egress.json` record is machine-labeled `review_contract: supplied-diff-private-git` and `named_lane_eligible: false`. `stateful status` exposes those fields, while `attempts[].runtime` remains the authoritative actual backend. Consumers must not infer a named lane from the top-level requested helper reviewer, exit `0`, or the findings-only output of `stateful final`; that command intentionally returns the saved low-level artifact, not a named-lane envelope.

The self-contained `independent-codex-pr-review` supervisor uses the same contract values. Every public JSON command envelope and durable attempt state carries both fields; missing, malformed, or non-boolean-false eligibility fails state authentication. Exit `0`, `overall_status: completed`, `review_status: clean`, a requested model name, or `No findings.` never upgrades this supplied-evidence runtime into a named lane.

The foreground compatibility command likewise prints only the raw helper artifact and does not emit a machine envelope. Its command surface is unconditionally ineligible for named-lane counting. Automation that needs machine-readable contract metadata must use `stateful status`; it must never ingest foreground stdout or `stateful final` as named-review evidence.

Model verification normalizes punctuation only and then requires exact equality. A requested `gpt-5.5` never accepts `gpt-5.5-mini`, `gpt-5.5-codex`, or any other suffix as the same model.

Capacity, overload, rate limits, timeouts, network/5xx errors, missing artifacts, silent model substitution, and review findings never trigger a model downgrade. The helper records every attempt and reports transient failures without switching models.

Fallback classification uses stderr plus explicit structured CLI error events and error-schema fields only. Claude authentication, entitlement, and transient classifications require one exact allowlisted failure-result envelope before any diagnostic text can influence the category. Copilot's model-discovery DNS shortcut additionally requires a nonzero exit and both signals on the same bounded diagnostic line. Partial `result`/`data` payloads, reviewer tool output, and repository text on stdout are never scanned for entitlement or transient substrings.

For Claude runtime preparation, deterministic automatic absence, unsupported platform enforcement, or a cleanly missing non-security capability from an automatically discovered candidate may be eligible for the already-authorized Copilot fallback. Publisher fingerprint/signature/checksum failures, contradictory safe-mode claims, outer-sandbox mismatches, unsafe runtime state, and invalid explicit overrides are blocked security or configuration failures and never authorize fallback. An explicit `CODEX_REVIEW_CLAUDE_PATH` that lacks the required GPG verifier, outer probe sandbox, or trusted review-tool prerequisite is also blocked rather than reclassified as automatic absence. Probe, manifest-download, executable resolve/stat I/O, file-race, and bounded-I/O failures are inconclusive and likewise never authorize fallback.

Authentication is a pause boundary, not runtime availability. Helper precedence is `ANTHROPIC_API_KEY` > `CLAUDE_CODE_OAUTH_TOKEN` > local login; only the winning explicit value is forwarded, and either explicit source bypasses the local-login broker/carrier/catalog/refresh transaction. Access-token expiry alone is not login expiry and does not block a catalogued local-login launch when a valid refresh token is present. A selected local credential that is missing, malformed, unsafe, lacks a usable refresh token, or is actually rejected by Claude as `Login expired`, HTTP 401, or refresh failure produces `blocked-authentication` plus the action `claude auth login`. A rejected API key directs the operator to unset or replace `ANTHROPIC_API_KEY`; a rejected OAuth token directs the operator to unset or replace `CLAUDE_CODE_OAUTH_TOKEN`. The helper stops until the user explicitly retries. This classification never authorizes Copilot under any egress-consent mode and never fabricates a Claude model attempt when launch did not occur. A structured authentication error from a launched review has the same terminal classification even if model metadata is otherwise verifiable. When a local-login rejection coincides with post-attempt refresh-persistence uncertainty, authentication remains primary while the runtime report and operator-facing error retain the validated recovery carrier, artifact, cleanup scope, and persistence diagnostic; a report-write failure inherits the same recovery metadata instead of obscuring it. Model entitlement remains distinct: a strict final-review error with trustworthy requested/effective-model evidence may advance through the pinned Claude model chain and, after both Claude models are entitlement-blocked, to an already-consented Copilot backend.

Credential-source read supervision, loopback setup, and broker construction failures remain fail-closed. The macOS local-login lane requires the reviewed universal broker artifact at its digest-keyed root-owned path before credential selection. Runtime never compiles, installs, repairs, or elevates it. A missing artifact is deterministic secure-runtime unavailability; an unexpected digest, CDHash, native dependency identity, owner, mode, link count, ACL, or ancestor path is blocked or inspection-inconclusive and never authorizes credential access.

Credential selection and its comparison copy enter one outer `try/finally` zeroization owner before runtime locks, events, capability material, durable-stage state, or broker initialization. Process binding is two phase: a deadline-bounded daemon worker performs only pure PID/session/process-group inspection and returns an immutable binding; then the parent thread rechecks the deadline and performs a nonblocking credential-server commit before it alone may authorize exec. The worker owns no service object or server lock, so late inspection is inert and cannot mutate, authorize, or retain credential service state.

Identity-service startup observation and failure cleanup, including shutdown request and joins, share one absolute deadline. Once bind may have occurred, startup failure first runs bounded cleanup; forwarded signals and other control-flow exceptions win over ordinary inspection or cleanup diagnostics, which remain attached. Per-attempt Unix socket identities are verified but never unlinked by name after inspection; whole-run private-directory cleanup owns removal.

The required `broker-reproducibility` `--check` is an ordinary-user GitHub-hosted `macos-26` exact-byte reproducibility gate, not an independent trust root or security boundary, and it establishes no trusted-builder or root-sealed provenance. Required review and branch protection protect changes to the gate; `--developer-check` is local convenience only. The developer and hosted-runner tool digests are separate reviewed pin sets because the hosted `macos-26-arm64` image repackages the same versioned Xcode toolchain; both modes must reproduce the same tracked artifact. Broker rotation must update every coupled pin: `providers.py` runtime SHA-256 and slice CDHashes; installer SHA-256 and size; build-script source/artifact, toolchain, tool-version, and both tool-digest pin sets; and `test_installer.py` expected SHA-256, size, and native identity expectations. Across the broker and both TCP and Unix CONNECT proxies, only an explicit OS policy or socket-capability bind errno is deterministic runtime unavailability. Unknown/resource/capacity/address errors, permissioning, thread/start/serve uncertainty, timeout, output-limit, drain, process, or file races are inconclusive and never authentication denials or fallback reasons.

Local-login refresh persistence is an attempt-time boundary, not deferred best-effort cleanup. On macOS, capability-broker handlers reject token strings that cannot encode as UTF-8 and validate every rotation before any filesystem work. Every structurally valid `W` generation admitted to durable staging atomically reserves attempt-scoped durable-journal quota before any filesystem write. The journal is bounded to eight generations and 8 MiB of payload; its last generation and 1 MiB are reserved for terminal recovery, and each reservation consumes quota for the rest of the attempt even when the subsequent write or publication fails. The generation then commits to a distinct helper-private recovery carrier: the credential file, containing directory, new carrier-directory entries, review-container entry, review-root entry, and final carrier rename are synchronized and the exact payload is read back. Each Darwin synchronization performs `fsync` followed by `F_FULLFSYNC`; an unavailable or failed full sync NACKs the generation. If rename completed first, only exact re-proof of the acknowledged path and payload digest may retain the generation as recovery-only evidence; it is never a host-writeback candidate. Durable commit alone does not authorize success. At the broker's generation linearization point, the handler holds the current-generation lock, rechecks that its generation is still current and the runtime is not abandoned, and only then publishes and acknowledges it. When an update would consume the terminal reserve, the server atomically claims it only for the current pending generation, permanently closes later `W` admission, commits that exact payload into the final slot, and NACKs without publication or host writeback. A superseded candidate consumes no quota or filesystem work, and later requests receive explicit NACKs before callback execution. The terminal generation invalidates any older staged host-writeback candidate, marks credential inspection inconclusive, and pauses without Copilot fallback. Superseded and failed carriers remain in the bounded journal until post-quiescence finalization or failure recovery. Acknowledgement proves successful full-sync calls plus exact recovery staging, not host persistence or an absolute power-loss guarantee; `F_FULLFSYNC` is Darwin's strongest available best-effort barrier and storage hardware may still fail to honor it. After the broker server and every handler quiesce, a single outer-runtime owner re-reads and exact-verifies the newest carrier before removing older journal entries, then persists only the latest acknowledged credential under the artifact-certified primary and legacy locks; Keychain and the `pwd`-home file are synchronized only when their refresh-token fingerprints identify the same logical login. The final recovery carrier is removed only after verified host commit with the same Darwin full-sync boundary on file and parent-directory cleanup. A control-flow interruption while removing an older durable generation stops immediately before host writeback or latest-carrier cleanup. On Linux/WSL2, the verified runtime rejects non-UTF-8-encodable token strings and receives a helper-owned writable `/auth` carrier with config at `/auth/config`, so both the primary config lock and legacy `/auth/config.lock` sibling can be created. Before reading the host credential, the helper opens and retains a no-follow directory-descriptor chain from `/` through the credential parent; every source read, lock, atomic replacement, and directory sync stays relative to that chain, while any symlink ancestor, leaf symlink, edge replacement, or ownership/mode change pauses inspection without fallback. Borrowed refresh-lock anchors are descriptor-only, and the credential owner revalidates the complete chain at commit boundaries. Descriptor-bound cleanup failure never publishes the now-untrusted lexical path as a helper-owned stale lock; it reports that no authoritative pathname is available and pauses for independent identity-safe recovery. A bounded watcher persists each stable staged rotation without holding staged and host locks together, then a synchronous final drain runs after an ordinary watcher stop and before carrier cleanup. Stop linearizes closure of new background-writeback admission before the bounded join, so a candidate whose last blocking read completes after closure cannot begin host writeback. If the initial bounded join times out, the attempt becomes inspection-inconclusive and immediately retains the carrier; the context skips concurrent final drain, payload scrubbing, and cleanup, records the recovery path, and returns or re-raises a deferred control-flow signal without an unbounded wait. The timeout handoff uses an explicit worker-cleanup state transition, so the last side to complete the handoff closes the descriptor chain without waiting behind filesystem inspection. If a background host writeback was admitted before stop and remains in flight, the diagnostic explicitly marks host state ambiguous because that already-started commit may still complete; it never claims an exclusive recovery handoff. The watcher is daemonized only so an uninterruptible filesystem operation cannot keep the helper alive after that bounded failure report; normal paths still join it. If the watcher has joined but Claude left staged locks around a rotated credential, only a normal supervisor return may attest that the sandbox process group is quiescent. Under that proof, the helper preflights the exact primary and legacy staged lock paths as empty current-user `0700` directories, removes them legacy-first, and retries final persistence once. Timeout, process-leak, unsafe-lock, retry, or host-write uncertainty retains the private recovery carrier in the review container, records its path without credential contents, and pauses. Stale shared host locks remain inspection-inconclusive and require controlled cleanup after confirming no writer remains; the helper never reclaims them automatically. Any writeback, lock, carrier-race, or watcher uncertainty pauses without a login prompt or Copilot fallback.

The latest exact-verified macOS generation is always the canonical current carrier. When quota rejection, a malformed update, or durable-stage failure leaves post-quiescence finalization without a staged host-writeback candidate, the owner reports that carrier as the sole current `authentication.recovery_artifact`. It removes every other non-authoritative complete journal entry and reports a failed stale-entry cleanup separately as `authentication.recovery_cleanup_artifact`. If that newest generation cannot be re-proven, the owner claims no current carrier, preserves every generation, and reports only the stable recovery-root cleanup scope. If staged or non-staged control flow leaves any stale carrier unvisited, cleanup reporting is promoted to the macOS recovery-root scope without changing a still-proven canonical current carrier.

On macOS, the helper prevalidates its stable recovery-root scope before exposing the broker. Unquiescent shutdown first irreversibly closes the server's pending-update publication/ACK gate, then boundedly drains handlers already inside the commit critical section. Runtime abandonment latching and pending detachment are independent recorded states, so detachment may be retried without reopening publication. The abandonment callback is event-only and itself deadline-bounded; it never waits for the runtime-state lock. A terminal durable stage racing that event either self-registers or is transferred to the quiescence stage by the already-bounded recovery worker under the runtime lock. Recovery runs for a detached `None` payload as well as a credential payload and must converge that stage. Timed detach uses one deadline across the initial-credential and pending-update locks; a timeout transfers no payload and leaves ownership with the server. Recovery-timeout reporting never waits for the runtime-state lock: a nonblocking snapshot may report an exact proven candidate, while lock contention conservatively reports only the stable macOS recovery-root cleanup scope. Once quiescence is unproven, outer finalization does not wait for or consume runtime state still owned by a late handler or recovery worker. Whenever abandonment recovery observes any durable journal entry or quiescence stage, its final error also reports that recovery root so old generations, a failed quiescence stage, and any fallback retain visible ownership even beside an exact current carrier. After an actual bounded recovery timeout, only the handler/recovery state machine owns in-flight stage cleanup; any late stage or fallback is exact-cleaned by that owner or remains inside the pre-reported root scope. When broker-server and handler quiescence cannot be proven, that handler/recovery state machine retains the latest exact-verified durable carrier and may durably materialize a newer pending structurally valid payload; outer finalization does neither. After proven quiescence, an outer-runtime owner whose complete guarded host writeback fails retains the latest exact-verified durable carrier and may durably replace it with a newer proven pending payload. This post-quiescence case includes Keychain-only bounded retry exhaustion, a first file-carrier write failure, and a partial same-login dual-carrier commit. Each carrier and its `config` directory are current-user `0700`, `config/.credentials.json` is a single-link regular file with exact mode `0600`, and no carrier path is granted to the Claude Seatbelt profile or passed as a child environment value. The carrier is not a new host credential source: the helper reports only an absolute path whose exact payload was created and verified when claiming a current recovery credential, never credential contents, and pauses as inspection-inconclusive without a login prompt or Copilot fallback. An unverified or incomplete file is cleanup-only and may be reported only through `authentication.recovery_cleanup_artifact`. An uncertain shutdown with no `W` generation, or a failed carrier creation that left no verifiable artifact, preserves the shutdown or persistence error without setting current recovery-credential metadata; pre-reported root cleanup metadata remains separate. If recovery replacement or finalization leaves an exact complete recovery update artifact, the helper retains and reports that exact artifact only after post-failure no-follow path-identity and payload verification; a later successful, read-back-verified replacement removes bounded stale recovery update artifacts. An actually existing incomplete update that cannot be removed or safely inspected is cleanup-only and must never be presented as a current recovery credential; a temp that disappears before final verification and a merely precomputed or attempted path are not recovery metadata.

When no `W` generation or verified artifact exists, the helper sets no current `authentication.recovery_artifact`; a pre-reported recovery-root `authentication.recovery_cleanup_artifact` remains permissible cleanup metadata.

Recovery reporting distinguishes the current credential from cleanup residue. Before a verified recovery commit, only a complete uncommitted update whose no-follow path identity and exact payload are reverified after the triggering failure is `authentication.recovery_artifact`. Once a replacement has been fully fsynced and read-back verified at `config/.credentials.json`, that verified file is the current `authentication.recovery_artifact`. Recovery-carrier removal advances cleanup ownership only after each destructive syscall from the credential file to `config`, the carrier, and finally the stable recovery root. Cleanup metadata must name an existing no-follow identity-stable scope: a vanished child is never reported, while entry churn beneath the same directory inode does not invalidate that directory as the cleanup scope. If deletion of an older temp fails, or an unverified or incomplete temp cannot be removed or inspected, report its exact path separately as `authentication.recovery_cleanup_artifact`; never label an older, unverified, or incomplete artifact as newest or current. If the temp disappears before final post-failure path verification, report neither field for that path.

Current recovery-artifact publication is source-content-bound rather than path-derived. The caller supplies the digest from the already exact-verified durable generation or recovery write; marker-time capture must match that digest and freezes the absolute path, strong file identity, and complete root-to-parent no-follow directory-identity chain in one immutable proof. A same-content replacement before marker capture is harmless and becomes the marker-time identity because it contains the exact source-authorized payload with the required owner and mode; a payload-changing replacement is rejected. That frozen proof is the sole authoritative publication snapshot. A compatibility path attribute may mirror `proof.artifact`, but readers never require or trust it, so a stale compatibility value cannot hide a valid proof and a bare legacy path never becomes current recovery evidence. A same-content inode replacement after capture or any ancestor edge replacement fails closed and clears the proof. Propagation, timeout snapshots, cleanup failures, and finalization publish, copy, replace, or clear the proof atomically.

A bounded heartbeat join timeout leaves the lock lease release-started rather
than release-complete. The owner performs one further bounded cleanup attempt
while preserving the first timeout as the primary diagnostic. If both joins
time out, classify cleanup as inconclusive. Intentionally retained shared
refresh-lock directories never authorize a lexical recovery or cleanup
pathname; report only descriptor-bound residue. Pause for controlled operator
cleanup only after confirming that no credential writer remains. Never
silently mark possibly orphaned directories as completed cleanup.

## Snapshot And Safety

- Helper authentication precedence is `ANTHROPIC_API_KEY` > `CLAUDE_CODE_OAUTH_TOKEN` > local login. Opaque-forward only the winning explicit value and remove the lower-priority explicit source. An explicit API key or OAuth token skips every local-login carrier read, credential-lock catalog gate, and refresh transaction. A catalogued local-login attempt owns one host refresh transaction before its first credential read. The same certified primary and legacy lock lease spans carrier selection, runtime credential exposure and network refresh, durable recovery staging, process-group or broker quiescence, the Linux/WSL2 watcher and final drain, and verified host commit. macOS persistence and snapshot checks and Linux/WSL2 writeback reuse the outer lease rather than nesting another host acquisition; staged-carrier locks remain independent. A second helper cannot copy or expose the host credential until the first transaction ends, and it must read the post-transaction host state after acquiring its own lease. A safe no-rotation attempt or a fully verified latest rotation releases normally. Unproven writer quiescence, an unpersisted rotation, retained carrier, or cleanup uncertainty abandons the lease: heartbeat and owned descriptors stop, the shared lock directories remain as a stale fence, and the lane pauses without automatic deletion, login guidance, or Copilot fallback. Every intentionally retained shared refresh lock reports descriptor-bound residue only; path-owned and borrowed-descriptor anchors alike provide no lexical recovery or cleanup pathname.
- The helper requires `--base-ref` and `--head-ref`, resolves both to commits before launch, and rejects the range unless base is an ancestor of head. A diverged range reports the merge base to use instead of silently reviewing a two-endpoint diff with unrelated target-branch changes.
- It materializes a helper-owned detached worktree backed by a private minimal Git database (`private-minimal-Git`). That database contains the supplied diff plus the bounded endpoint tree/blob closure needed for helper inspection and exposes no remote URL or networked Git behavior. The modern review root lives below the canonical system temporary root `/tmp`, outside the source checkout, in a private namespace owned by the current effective UID and bound to the canonical source path. The source checkout is read-only input and is never edited.
- Committed-tree materialization streams raw Git tree blobs into the helper-owned detached workspace; Git archive attributes, checkout filters, hooks, and repository configuration cannot rewrite that content. Before writing each regular blob, it enforces a 64 MiB per-file limit plus a 512 MiB/100,000-entry total snapshot budget, and caps streamed tree metadata at 128 MiB. It also limits complete changed-path and raw-diff metadata to 128 MiB and 100,000 logical paths and streams a binary metadata-only `--submodule=short` artifact for the exact range through a 128 MiB output cap. Every materialized path must fit the cleanup recursion capacity, including the extra materialized directory level for a gitlink; an over-depth entry is rejected before materialization. Snapshot, tree, diff, and materialization budget exhaustion fails prelaunch and removes the partial container. Each secret discovery or count pass retains its 512 MiB blob-input admission limit. Each logical assignment proof remains capped at 4 MiB; within one streamed path or blob value, overlapping physical proof ranges are unioned and only newly covered bytes consume the 64 MiB aggregate proof-coverage budget. An independent 512 MiB cumulative proof-work budget charges every logical proof attempt, including already covered bytes and discarded speculative scans. Streaming speculation transactionally clones the event budget, coverage budget, and range ledger, while every clone shares the monotonic work counter. Temporary speculative coverage debt is discarded on incomplete replay, while a complete scan cannot commit negative coverage. Coverage and work capacity are checked before either is deducted; committed coverage and ranges must reconcile exactly. The ledger fails closed above 100,000 disjoint ranges, and distinct path or blob values remain separately charged. Exhaustion makes secret admission `inconclusive` and must not remove an otherwise valid workspace or delay, suppress, redact, or rewrite reviewer input.
- Without `--include-source-wip`, preparation requires a clean committed endpoint. With that explicit flag, the helper creates a private composite from original source `HEAD` plus staged, unstaged, and non-ignored untracked state. It records the original source `HEAD`, exact private snapshot tree, WIP digest, and source-to-snapshot path evidence. A source mutation, WIP deletion or reversion, digest mismatch, unsupported special file, or ambiguous source state fails closed. WIP evidence is diagnostic only and cannot satisfy PR-readiness or merge-ready exact-commit gates.
- The helper rejects a range when either base or head uses its reserved top-level `.codex-review` control path. Every head snapshot also rejects any path component beginning with the helper-owned `.codex-review-cleanup-` quarantine prefix so tracked content cannot collide with cleanup recovery state; a base-only path deleted by the reviewed range remains available in the trusted raw diff and the ephemeral complete-path proof until preflight scrubs it.
- The rendered review prompt is capped at 64 KiB before any reviewer starts. This keeps Copilot's required `--prompt` argument below per-argument execution limits and prevents an oversized override from reaching any external lane.
- Snapshot preparation runs Git with a cleaned environment, replacement objects disabled, disabled hooks/fsmonitor/external diff, and explicit `--no-ext-diff --no-textconv`. It does not checkout files through repository filters, keeps materialization failures in bounded typed diagnostics, and removes a partial review container on ordinary errors, forwarded signals, or `KeyboardInterrupt`. Completed snapshot ownership is handed to the caller while forwarded signals are blocked, before preparation returns. If removal fails before the state path is published, the terminal error names the retained container and cleanup failure instead of silently losing recovery evidence.
- The helper never initializes or fetches submodules. Source preparation accepts a gitlink only when its raw path, mode, and object ID match exactly between the source `HEAD` tree and active index; any staged gitlink addition, deletion, object update, rename, or type replacement fails before source status or WIP capture, and clean preparation rechecks the complete index after status. Top-level source queries ignore initialized submodule worktree state and never read nested content or local Git configuration. Gitlink changes in the selected review range publish only the path, mode, and base/head commit object IDs; the frozen diff never includes nested submodule logs, diffs, or content, even when the source checkout already has an initialized submodule.
- Every reviewer receives the same bounded findings-only prompt and primary diff file inside a helper-owned `.codex-review/` directory in the frozen snapshot. Custom prompt placeholders are expanded in one pass so placeholder-shaped text inside generated paths is never interpreted again.
- The frozen helper workspace has working metadata only for its helper-private minimal Git database. Reviewers receive bounded read-only Git/source access to that restricted scope; parent paths are not readable, writes and network are unavailable, and the supplied diff remains the primary evidence surface. The runtime does not copy a Git shim or other helper source into reviewer-visible scope. Private Git commands disable lazy fetching, terminal prompts, askpass helpers, hooks, remotes, and unsafe repository configuration, so a missing object fails closed instead of contacting a promisor remote or waiting for authentication.
- Every child receives a runtime-specific minimal environment allowlist instead of the parent's complete environment. Uppercase and lowercase proxy variables (with lowercase overrides taking precedence) plus the shared standard/custom CA bundle pointers are preserved so configured routing and TLS trust still work. Claude and Copilot authentication variables are never present in each other's process. Codex may receive `OPENAI_API_KEY` for headless authentication, but model-proposed shell commands cannot inherit it. `NODE_EXTRA_CA_CERTS` is a Claude-only exception rather than a shared reviewer input: Codex, Copilot, the pre-provenance Claude candidate probe, GPG, and security-sensitive host tools do not inherit it. `SSL_CERT_FILE` remains an independent caller input and never causes the Node variable to be set. This input/forwarding separation is not a claim that the final Linux sandbox maintains two trust domains: when Node extra CA is explicit, the validated material intentionally converges into the single private bundle described below. Arbitrary `NODE_*`, `NODE_TLS_REJECT_UNAUTHORIZED=0`, mTLS private-key inputs, and private-key material remain unsupported.
- Claude never mounts a caller-provided TLS path directly. A configured CA file, including `NODE_EXTRA_CA_CERTS`, is copied from a stable no-follow descriptor through the same absolute-path, owner/mode, size, PEM-only, private-key rejection, and atomic private-`0600` gates. A configured CA directory is enumerated through a fixed descriptor with bounded entries; bounded OpenSSL hash-link chains may cross into normal system or enterprise certificate locations, but every traversed link/directory and final regular file is owner/mode/identity checked around the read. Only certificate blocks are retained, and hash basenames become private `0600` regular files. On macOS, the proxy snapshot digest binds exact captured file bytes rather than only normalized certificate blocks. The final merged bundle is atomically materialized in helper-private storage and changed to exact `0400` mode; the helper then revalidates its exact path, mode, and digest while constructing the Seatbelt profile and immediately before `sandbox-exec` launch. This detects ordinary path and content drift at the helper boundary but does not claim protection from a malicious same-euid host process after the final check. Linux/WSL2 builds one helper-private bundle and captures plus revalidates its complete resolved path identity while serializing the read-only `bubblewrap` command. Node-only configuration retains default system trust; configured replacement inputs (`CURL_CA_BUNDLE`, `GIT_SSL_CAINFO`, `REQUESTS_CA_BUNDLE`, `SSL_CERT_FILE`, or `SSL_CERT_DIR`) preserve the existing replacement semantics before Node certificates are appended and deduplicated. The sandbox sets `NODE_EXTRA_CA_CERTS` only when the caller explicitly configured it, points it to `/etc/ssl/certs/ca-certificates.crt`, and adds no host mount. This freezes the resolved source consistently with runtime-library mounts; it is not an FD-bound atomic mount handoff against another same-euid process.
- On macOS, Seatbelt grants `file-read*` only to the exact helper-owned Node CA copy; it grants no parent-directory subpath read, and the caller path is absent from the profile and launch arguments. Literal ancestor metadata checks remain part of safe path traversal.
- The helper's upstream HTTPS proxy has a separate CA trust domain. It snapshots only the caller environment's CA inputs into a dedicated helper-owned tree once per Claude model chain, keeps the original proxy precedence, and initializes one in-memory TLS context from the captured bytes; it never reuses the macOS Claude bundle containing publisher or host trust roots. Replacement directories contribute only canonical lowercase OpenSSL hash-index names that are contiguous from `.0` for each hash and whose captured certificate blocks all match that subject hash according to the fixed root-owned OpenSSL client. Subject-hash verification and replacement-context initialization have one shared 20-second deadline and a 512-certificate budget across the complete replacement-directory snapshot; duplicate captured material is verified once, and the deadline is rechecked after every subprocess and before acceptance. The in-memory context explicitly enables strict X.509 verification and partial-chain trust-anchor support without loading ambient default roots. With no replacement input, the helper captures the compiled-in OpenSSL CA file through a stable no-follow descriptor. On Linux, a symlinked compiled-in CA file is not followed; the helper instead uses the compiled-in CA directory through the same bounded snapshot and hash-validation path when available. Every upstream connection reuses the prepared context without reopening caller or helper snapshot paths, so later same-user path replacement cannot change proxy authentication or expose `Proxy-Authorization` to a newly trusted endpoint.
- On macOS, caller CA files and directory entries must be owner-only, single-link regular files read through bounded no-follow descriptors; symlinks, hardlinks, FIFOs, growth/identity races, and any extended ACL entry block. An allocated empty ACL is valid, while an unknown ACL result fails closed. The first attempt snapshots certificate blocks into helper-private state, retains an exact raw-byte binding for launch preparation, and rejects private-key material; later attempts revalidate both bindings. The signed executable must contain exactly one bounded Bun `root_certs.cpp` store; every member must be a cryptographically self-signed CA whose KeyUsage, when present, permits certificate signing, while unrelated embedded PEM data is never promoted. Before every Claude model attempt, the helper revalidates the publisher-signed executable snapshot and its exact embedded root set, re-exports user/admin/system trust settings with bounded regular-file output, gives explicit deny precedence, omits constrained or invalid additional roots, and rebuilds the full merged bundle. Across trust domains, a later blocked policy failure outranks an earlier inconclusive or unavailable export, and an inconclusive failure outranks an unavailable export. A root excluded by host policy may not re-enter through the system, caller, additional, or bundled source; a policy-excluded root still present in Claude's bundled store blocks the lane. `claude-trust-policy.json` records a sanitized terminal state for complete, denied, blocked, unavailable, or inconclusive preparation.
- Fixed Security/OpenSSL trust helpers are unavailable only after deterministic executable or required-capability absence. Trust export recognizes the exact no-settings response only with `security`'s known normal status `1`; signals, other unexpected nonzero exits, unknown help output, and failed certificate export or verification operations are inspection-inconclusive and return exit 75. None authorize Copilot fallback.
- Codex runs with a custom permission profile: only platform-minimal paths, the frozen helper workspace, and its private minimal Git metadata/object paths are readable; tracked `.codex`, `.agents`, and environment files are intentionally readable as repository context, while network and writes are unavailable. Clean mode exposes only committed endpoint content; explicit WIP mode exposes only the digest-bound private composite. Neither mode exposes the source checkout, host home, unrelated repositories, or reviewer/runtime credential sources. Codex ignores user config and execpolicy rules, sets `project_doc_max_bytes=0` so the reviewed head cannot inject `AGENTS.md` instructions, uses `approval_policy=never`, and gives model-proposed shell commands a fresh environment with an empty helper-owned `HOME`. The helper-owned review prompt is the only project-level review contract. The terminal artifact is rejected unless the persisted Codex turn context exactly matches the expected platform-minimal read, frozen-root/private-Git reads, restricted network, and never-approve policy; any extra path or glob entry and any legacy or managed `sandbox_mode` override fail closed as `permission-mismatch`.
- Claude Code CLI compatibility is version-range and capability based. A release version `>=2.1.211,<3.0.0` is eligible only after the fixed Anthropic GPG fingerprint verifies its per-version signed manifest and the manifest checksum matches the installed platform binary. The helper does not pin `latest` or one patch release. Native Mach-O/ELF format, execute permission, identity output, architecture, and Linux/WSL2 libc compatibility reject scripts, interpreter wrappers, and incompatible artifacts; those checks never establish publisher provenance. macOS accepts an official thin arm64 or x64 artifact and selects the signed manifest entry from that artifact's architecture; x64 may run through Rosetta on Apple Silicon, so exact host-CPU equality is not required. Linux/WSL2 still requires a matching host architecture and libc. macOS code signing and notarization are not current acceptance gates. They may be added later as optional defense in depth, but cannot replace the signed manifest. Read [claude-runtime-trust.md](claude-runtime-trust.md) for the complete ordering and trust anchors.
- The fixed-path GPG source is a native host-trust dependency, not Anthropic publisher provenance. Its resolved executable must be a root- or current-user-owned regular native file, executable and not group/world writable; the helper verifies stable file and path identities. Parent directories must be root/current-user owned and non-group/world writable, except that canonical Homebrew parents on macOS may be group-writable only by the exact `admin` group, whose members are explicitly part of the host-tool TCB. After inspection, the helper keeps a stable source descriptor, copies the main executable from that descriptor into a fresh current-user `0700` GPG home directly below an explicit helper-owned `0700` root in the isolated review state, atomically publishes a mode-`0500` execution snapshot, and uses only that snapshot for key dearmoring, fingerprint listing, and detached-signature verification. The main snapshot fixes the executable file, not its loader dependencies. On macOS, fixed root-owned `/usr/bin/otool` recursively collects a bounded dependency closure; the main executable must contain exactly one `LC_LOAD_DYLINKER` naming `/usr/lib/dyld`, dependencies must not contain that command, and relative dyld names, rpaths, dyld environment load commands, and paths outside sealed `/usr/lib`/`/System/Library` or canonical Homebrew `opt`/`Cellar` roots are rejected. Every non-sealed lexical/resolved path identity is revalidated before each GPG call. A safe `otool` that cannot start or returns nonzero is inspection-inconclusive, not clean runtime unavailability. On Linux/WSL2, automatic selection is limited to root-owned `/usr/bin/gpg{,2}`. Stable-descriptor ELF inspection first proves that the loader-visible `PT_DYNAMIC` address, memory, and file-backed ranges map consistently through exactly one `PT_LOAD` at byte and loader-page granularity, including page-offset congruence and rejection of every other page-rounded mapping over the dynamic table; only then does it reject `DT_RPATH`/`DT_RUNPATH` and `DT_AUDIT`/`DT_DEPAUDIT` in the snapshot before any loader process starts. Host GPG must name the architecture's canonical glibc interpreter. The helper captures and proves that loader's lexical/resolved root-owned identity, Linux-native mount provenance, host architecture, ELF64 `ET_DYN` type, absence of a second interpreter or unsafe dynamic tags, and floating glibc version `>=2.27,<3.0`; musl and unknown host-GPG loaders are unavailable. It then invokes only `loader --list <snapshot>`, never an implementation-variable `ldd` script, captures every reported lexical symlink chain and resolved root-owned endpoint, rejects unsafe dependency tags/types and any interpreter other than that same canonical glibc loader after the proven list trace but before GPG, revalidates the old closure, and rebuilds and requires exact closure and loader-version equality before each GPG call. Only the private snapshot path may traverse a root-owned exact-`01777` system-temp ancestor; its non-final directory identities retain device/inode/type/mode/owner/group anchors while ignoring expected entry-count/time churn as GPG creates keyrings and locks. The final snapshot executable, glibc loader, and dependency chains keep complete metadata identities and remain non-writable. Ambient temporary-directory variables do not select the private root. WSL2 batch-validates both old and refreshed snapshot, loader, lexical dependency, and resolved endpoint paths against bounded mountinfo; Windows-backed provenance is invalid, while unavailable or malformed mount evidence is inconclusive. `claude-runtime.json` still records the inspected source path as `gpg_verifier`, with `gpg_verifier_trust: fixed-path-native-host-tool`, separately from `publisher_provenance: anthropic-signed-manifest`.
- Every untrusted identity and version probe runs before provenance in a helper-owned bootstrap sandbox with no credentials, frozen review workspace, host configuration, writes, or network. macOS uses a deny-by-default Seatbelt profile. Linux and WSL2 use a synthetic-root `bubblewrap` sandbox that exposes the candidate plus only narrowly selected root-owned, non-group/world-writable system library roots; it does not run `ldd` on the unverified binary. After the signed checksum passes, trusted root-owned `ldd` may collect the exact root-owned, non-group/world-writable dynamic dependencies for read-only mounting in the final Linux sandbox. Every collected library and each path component is identity-captured, ownership/mode checked, and revalidated before command construction and final mounting. WSL1 and native Windows are unsupported. Probe homes and output files are helper-owned, and time plus both output streams are bounded. A timeout, output-limit breach, drain failure, file race, or leaked descendant is inconclusive and cannot authorize Copilot fallback.
- GPG and Linux security-sensitive host tools use explicit minimal environments rather than inheriting the caller's process environment. The allowlist sets only fixed home, locale, path, and temporary-directory values needed by each tool, so dynamic-loader variables, shell startup configuration, compiler flags, and toolchain overrides cannot change GPG, glibc-loader probes, post-provenance `ldd`, `bubblewrap`, `socat`, `rg`, compiler probes, or launcher compilation.
- Immediately after the signed manifest, size, and SHA-256 checks pass, the helper copies the source candidate into a digest-keyed executable file inside a current-user-owned `0700` snapshot directory. The copy is rehashed against the signed artifact, published atomically with mode `0500`, and revalidated before reuse. The original candidate is never executed again: the same helper-owned verified snapshot is captured before the Claude model chain and runs bounded `--help`, post-provenance dependency discovery, authentication-bearing preparation, and every final model attempt. This closes both the source-path replacement window and any attempt-to-attempt rediscovery window.
- Capability validation accepts only the supported release range, requires every public flag used by the helper's real Claude commands to appear uniquely in bounded `--help` output, requires the permission-mode help to advertise `dontAsk`, and parses the required safe-mode semantics without exact-matching the complete help string. Safe mode must unambiguously disable user/project instructions, skills, plugins, hooks, MCP servers, custom commands and agents, and other local customizations while preserving authentication, model selection, built-in tools, and permissions. The customization sentence must match one bounded whole-sentence positive grammar with a known-item enumeration and a terminal disabled state; temporal reversals, extra predicates, unknown enumeration clauses, negation, duplication, contradictions, or weakening block the lane. Every sentence in the safe-mode option block is default-denied unless it independently matches one bounded grammar for customization disablement, managed-policy preservation, runtime preservation, the exact environment assignment, a positive safe-mode self/anaphoric claim, or a bounded documentation/information topic that remains available online. Harmless wrapping and the official bounded troubleshooting-purpose suffix remain accepted. Missing required behavior may make an automatic candidate unavailable; unsafe semantics never do. There is no credential-free fixed-input behavioral canary: the final real invocation and strict structured-output, effective-model, error-state, and terminal-artifact validation provide the behavioral acceptance check. macOS retains the hardening-compatible `default` permission mode and `Read,Grep,Glob`. Linux and WSL2 use `dontAsk`, expose only `Read`, explicitly allow only cwd-relative `Read(./**)`, reject ASCII `@` file mentions in the exact prompt bytes before authentication and again before each attempt, and deny `Grep`, `Glob`, and every non-workspace synthetic-root mount. Immediately before each sandbox command is serialized, they repeat a bounded no-follow workspace symlink audit: stable relative links that remain inside the frozen workspace are supported, while absolute links, `/proc` magic links, transiently escaping `..` paths, chain escapes, loops, races, and inspection failures fail closed. All platforms carry explicit model and effort arguments, disable hooks and setting sources, and omit browser, slash-command, inherited MCP, and session-persistence surfaces.
- The helper uses layered enforcement. The helper-owned outer sandbox exposes only the frozen workspace, helper-owned home/temp/control paths, required runtime files, the restricted review-tool path, validated certificate copies, and the platform credential carrier; it enforces host filesystem, process, write, and network isolation. The helper-controlled proxy permits only `api.anthropic.com:443` and `platform.claude.com:443` during the final review so the trusted runtime can complete API work or refresh inside the temporary carrier; the helper performs no separate OAuth refresh warmup. On Linux and WSL2, the publisher-verified Claude permission engine is an additional trusted boundary because the same Claude process must read its credential carrier while model-visible file tools must not. Command construction requires exact `dontAsk`/tool/allow/deny settings, double-slash absolute root and recursive denies for every non-workspace mount, and no separate mount below `/workspace`; any future uncovered mount fails closed.
- Local-login credentials need only be structurally valid, safely sourced, and contain a usable refresh token when the final Claude process launches; the helper does not require the access token to remain valid for the 30-minute attempt. Local-login refresh writeback additionally requires an exact version/platform/SHA-256 artifact entry from Anthropic's signed manifest in the credential-lock protocol catalog. The initial catalog contains the six supported Darwin and Linux artifacts for `2.1.211`; WSL2 uses the matching Linux entry. An uncatalogued local-login artifact fails inspection-inconclusive before credentials or review data are exposed. On macOS, after verified executable, restricted-tool, and TLS preparation, the parent reads the current-account `Claude Code-credentials` Keychain item through Apple's trusted client and independently inspects `.claude/.credentials.json` beneath the home returned by `pwd.getpwuid(os.getuid())`, never caller-controlled `HOME`. It does not read or import `~/.claude.json`. The file source uses stable no-follow identity, current-user ownership, exact mode `0600`, bounded size, and JSON-shape checks. The file is an empirically compatible Claude Code source on macOS, not an officially guaranteed storage contract. From the structurally valid candidates, the helper selects the credential with the later access-token expiry even when that access token is already expired; the refresh token, not future access-token lifetime, preserves the login. The parent retains a logical snapshot of both carriers' existence, payload and refresh-token digests, plus the file identity. The selected payload, regardless of source, is copied into a capability-authenticated temporary broker that exposes only the fixed Claude credential operations. The final Claude process cannot execute `/usr/bin/security` or access either host source directly, but it may refresh inside that temporary carrier.

  Every structurally valid broker `W` generation admitted to durable staging atomically reserves attempt-scoped durable-journal quota before any filesystem write, then commits to a distinct helper-private recovery carrier with synchronized file, directory, rename, and exact-readback state. On Darwin, every such sync runs `fsync` followed by required `F_FULLFSYNC`; failure NACKs before publication, while an already-renamed exact payload may be retained only as recovery-only evidence after acknowledged-path re-proof. The journal is bounded to eight generations and 8 MiB of payload; the last generation and 1 MiB are reserved for a terminal exact recovery copy, and a reservation is never refunded during the attempt. At the locked generation linearization point, only a still-current, non-abandoned ordinary generation is published and acknowledged. The current update that first enters the terminal reserve atomically closes later broker writes, commits into that reserve, and NACKs without publication; superseded contenders consume nothing and later requests are explicitly NACKed before callbacks. The terminal generation invalidates older staged host-writeback state and pauses inspection-inconclusive without fallback. The latest exact-verified generation is always the canonical current carrier. After server and handler quiescence, one owner removes non-authoritative complete entries only after proving that canonical carrier, then guarded-writes the latest acknowledged credential under the certified locks and removes the final recovery carrier only after verified host commit through the same full-sync boundary. If no staged host candidate remains, it reports the canonical carrier as the sole current recovery artifact; stale cleanup failure is separate, and a control-flow interruption that leaves any stale carrier unvisited promotes cleanup reporting to the recovery-root scope. Same-refresh-token carriers remain one logical login, supported Claude writers serialize through the audited lock window, and unrelated writers that bypass both locks remain outside the compare-before-write guarantee.

  Prevalidate the stable macOS recovery-root scope before broker exposure. Unquiescent shutdown irreversibly closes the pending-update publication/ACK gate before boundedly draining handlers already inside the commit critical section. Runtime abandonment latching and pending detachment are independent recorded states, so detach may be retried without reopening publication. The event-only abandonment callback and timed detach are each bounded; detach timeout leaves payload ownership with the server. A terminal durable stage racing abandonment either self-registers or is transferred by the bounded recovery worker under the runtime lock. Recovery runs even for detached `None` and converges that stage. Timeout reporting takes only a nonblocking runtime-state snapshot; while a newer terminal durable stage is unresolved it drops an older current-carrier claim and reports only the stable macOS recovery-root cleanup scope. Once quiescence is unproven, outer finalization does not wait for or consume runtime state still owned by a late handler or recovery worker. Any durable journal entry or quiescence stage makes the final abandonment error report that root, including beside an exact current carrier. After an actual recovery timeout, only the handler/recovery state machine owns late stage/fallback cleanup; it exact-cleans the artifact or leaves it within the pre-reported root scope. Current recovery metadata requires post-failure no-follow path-identity and exact-payload verification; unverified or incomplete artifacts are cleanup-only, and a vanished temp is unreported. Any quiescence, guarded-writeback, recovery, or cleanup uncertainty pauses without a login prompt or Copilot fallback.

  Recovery replacement and cleanup keep the complete current credential separate from stale residue. A verified replacement may remove bounded stale update artifacts; any incomplete or unverified artifact is cleanup-only. A staged or non-staged control-flow early stop that leaves stale carriers unvisited promotes cleanup reporting to the recovery root and never downgrades the canonical exact-verified carrier.

  On Linux and WSL2, every model launch independently accepts only the current user's non-symlink regular credential file with exact mode `0600` and non-empty UTF-8-encodable access and refresh tokens, copies it into a new helper-owned writable `/auth` carrier with private config at `/auth/config`, and mounts only that carrier; the host credential itself is never mounted. The same parser rejects a staged rotation containing an unpaired surrogate before writeback. The layout permits both the primary config lock and legacy sibling `/auth/config.lock`. Before mounting the carrier root, validation requires the exact canonical `.../<carrier>/config` shape and rejects any equality or ancestor/descendant overlap with helper home or temporary directories, so the same host directory cannot be exposed under both `/auth` and a general writable role. The staged carrier may change only through the trusted runtime's authentication path and all of `/auth` remains denied to model-visible file tools. A bounded watcher persists every stable rotation without nesting staged and host locks; after the watcher stops, a synchronous final drain validates the latest candidate and host baseline before cleanup. An explicitly exported `ANTHROPIC_API_KEY` or `CLAUDE_CODE_OAUTH_TOKEN` skips local credential selection, staging, and the internal lock-protocol gate, so any publisher-verified compatible release remains usable in the corresponding explicit mode; `/proc` and `/dev` are denied so the model cannot read the key through process environment or descriptor aliases. Lock contention, stale locks, carrier races, credential I/O uncertainty, and an uncatalogued protocol are inspection-inconclusive rather than authentication failures and never authorize Copilot fallback or a `claude auth login` prompt; stale locks are not automatically reclaimed. Positive WSL2 classification requires an explicit `wsl2` or `microsoft-standard` marker in the kernel release/version. `/run/WSL`, any nonempty raw `WSL_INTEROP` value (including an invalid or missing endpoint), distro environment, binfmt evidence, and a generic `microsoft` kernel identity are conservative weak WSL-presence signals. They do not authorize WSL2 and may classify ambiguous or spoofed state as unsupported WSL1. A guest that removes both the marker and every weak signal is observationally indistinguishable from native Linux and follows that classification. The discovered Claude executable, credential source, frozen review/state container, private GPG temporary root, and every helper runtime path—including the verified snapshot—must live in the WSL Linux filesystem. The common Linux/WSL guard rejects positive DrvFS/Windows provenance from bounded `/proc/self/mountinfo`, even when a markerless guest was classified as Linux. Positively identified WSL2 also keeps the fast literal `/mnt/<drive>` rejection and requires every path to prove local native Linux backing. Known DrvFS/Windows provenance, including custom automount, bind-mount, and alias paths, is blocked. Only WSL's local ext4 `/dev/sdX` storage and backing-free tmpfs are currently proven by the strict WSL2 inspection; overlay, FUSE, 9p, virtiofs, loop/device-mapper/network-backed, and unknown filesystems are inspection-inconclusive rather than guessed safe. Unreadable, malformed, oversized, non-canonical, or non-covering mount information also fails closed. A temporary carrier limits direct host-source mutation exposure but does not hide the credential from the trusted Claude process or provide atomic coordination with unrelated external writers that bypass both locks; stronger isolation would require an external authentication injector.
- Mountinfo mount points and ordinary filesystem roots must be absolute
  canonical paths. Exact `nsfs` records may instead carry one strictly bounded
  kernel namespace handle such as `net:[4026531840]`; this unused metadata field
  never participates in path coverage or Windows-provenance decisions. Opaque
  roots for every other filesystem and malformed namespace handles fail closed.
- Linux helper runtime directories follow create-or-validate semantics. A missing path is created once; an existing path must already be a real current-user directory with exact mode `0700` where private or otherwise no group/world write, and must retain the same no-follow descriptor/path identity across validation. The helper never follows a symlink or uses `chmod` to repair an attacker-controlled pre-existing path.
- The fixed no-shell Linux launcher blocks `SIGTERM`, `SIGINT`, `SIGHUP`, and `SIGQUIT` across proxy/workload fork and process-group publication, inspects pending signals before each handoff, restores default child signal state, forwards cancellation to both process groups, and terminates/reaps leftovers. This closes the signal-arrival windows that could otherwise strand the relay or review workload.
- `claude-runtime.json` distinguishes source discovery from accepted execution. It records `source_executable`, the helper-owned `verified_executable` snapshot, manifest/signature/checksum and the fixed-path GPG source as `gpg_verifier`. macOS records the selected credential mode without persisting the credential or bearer-capable token metadata, then writes `runtime-launching` and `sandbox-auth-staged` only after the temporary broker and Seatbelt profile are prepared. Linux/WSL2 writes `runtime-ready` and `sandbox-auth-staged` only after credential staging and the real isolation probe. A missing, malformed, unsafe, refresh-token-less, or actually rejected credential writes `blocked-authentication`, includes the source-appropriate operator action (`claude auth login` for local login, unset/replace `ANTHROPIC_API_KEY` for API-key mode, or unset/replace `CLAUDE_CODE_OAUTH_TOKEN` for OAuth-token mode), and never creates fallback eligibility; access-token expiry alone does not. Successful guarded writeback records only the carrier kind and outcome, never token metadata. A bounded supervisor timeout, output overflow, drain failure, or retained descendant during a launched Claude attempt writes `attempt-inconclusive` with a stable failure class. `attempt-complete` records final category/return code and requested/effective model and effort. No phase claims future token validity.
- Copilot runs with a fresh helper-owned `COPILOT_HOME`, so persisted permission grants and configuration cannot broaden the lane. It uses plan mode with `-C` pinned to the frozen workspace, a model-visible tool allowlist limited to `view`, `glob`, and `grep`, no `--add-dir` or `--allow-all-paths`, explicit shell/write/URL denial, temp-directory denial, and disabled custom instructions, built-in MCPs, bash environment loading, experimental features, and remote session export. Excluding the `skill` and delegation tools prevents project skills or agents from injecting reviewed-head instructions. Before every attempt, the helper runs a logged `copilot help permissions` probe with a 20-second deadline and 64 KiB per-stream cap, then confirms that availability filters control which tools the model can see, default file access is cwd-only, `--disallow-temp-dir` removes the implicit temp root, and deny rules override `--allow-all-tools`; otherwise the fallback fails closed. Secret-like auth variables are withheld from its tools.
- Claude raw stdout is accepted only through the same canonical preflight-bound strict stream validator used by the named direct lane. The helper supplies its exact compatible version, selected authentication source, requested model, child return code, host workspace binding, and stream-reported runtime cwd as distinct inputs. Linux and WSL2 bind the reported runtime cwd to `/workspace` while retaining the exact host frozen-workspace path for host-side validation; the macOS helper uses that exact host path for both. The named-direct structured-tool path scope remains bound to the host clean worktree rather than any helper sandbox projection. The validator applies the closed `legacy-base` or `extended-2x` init/intermediate/terminal profile and returns a trusted result artifact only with `classification: accepted`. Preserve an accepted `findings` string verbatim, then pass it to [`review_result.py`](../scripts/review_runtime/review_result.py) for canonical post-acceptance disposition; acceptance alone does not determine whether the review is clean. Preserve `blocked`, `blocked-authentication`, and `inconclusive` machine classifications exactly. Broker/carrier success, prose inspection, partial result text, or a helper-only parser never supplies a result artifact or authorizes model/Copilot fallback. Copilot keeps its separate strict JSONL contract: every session/message/usage model must agree, structured errors clear result text, and malformed, partial, or silently substituted output is never accepted.
- Before any network-backed Codex, Claude Code, or Copilot run, the helper verifies the frozen repository/range, rejects absolute or escaping symlinks, and validates reviewer-visible control artifacts under the existing size, identity, digest, ownership, mode, no-follow, stable-read, cleanup, and retention contract. Those egress-boundary checks remain reviewer-launch gates. Secret-delta findings do not: the trusted reviewer receives the frozen tracked diff, necessary tracked context, and explicitly supplied prompt in their original form, including tracked repository secrets, once the consented scope is safe to send. Secret inspection is retained only to produce a separate PR/master admission result. A violating or inconclusive admission result must not be recast as failed reviewer egress. Diagnostics remain bounded and control-safe; text additions expose raw path plus one-based head line, while new-path and binary fallbacks use `line: null` and symlink targets use line `1`. Unchanged occurrences are omitted, and incomplete location mapping is explicitly `location_status=inconclusive`. Before releasing the inherited runner lock, the stateful runner securely exact-reads the completed bounded `preflight.json` through the preparation-bound container, validates its public secret summary, and advances the schema-v5 marker with a runner-sealed receipt containing that artifact's exact byte length and SHA-256. Receipt publication alone is not terminal evidence: while the runner lock remains held, every admission query returns pending even if the receipt and a clean summary are already readable. `stateful admission` trusts the summary only after the lock is released and the current no-follow bounded read matches that sealed receipt. A replacement, mutation, malformed receipt, or terminal state without a seal is inconclusive rather than clean. Receipt-only field, shape, excessive-nesting, or duplicate-key failures are retained as admission evidence errors: admission returns structured `preflight-invalid` / exit `75`, sealing refuses to overwrite the bad receipt, and separately validated `status`, `wait`, reviewer `final`, and cleanup remain usable. Duplicate, malformed, or excessively nested non-receipt lifecycle fields remain hard state errors.

- PR/master admission uses one rule for every tracked exact secret outside the approved authoring pool, including formerly cataloged legacy values and unregistered discoveries. The helper must recover one stable exact raw byte value, completely enumerate the bounded base/head Git trees, and count that value globally across raw Git path bytes (including gitlink paths without reading submodule content), regular-file blob bytes (modes `100644` and `100755`), and symlink-target bytes (mode `120000`). Declared rules continue to govern authoring-fixture acceptance, but the counter has exactly one descriptor per raw byte value; a cataloged legacy descriptor takes precedence when another scanner rule rediscovers the same bytes. The rendered diff and prompt are not extra count surfaces. Admission requires only `head_count <= base_count`. Equality, deletion, and movement across path, surface, mode, or byte offset pass; copying passes only when another removal prevents global growth. A first appearance or any growth blocks PR/master admission. No unembedded counter, per-occurrence provenance, path-specific absolute deny, canonical Base64, or other derived encoding participates. The helper must not synthesize URL-encoded, hex, escaped, hashed, or otherwise transformed candidates. Consequently, an encoded/transformed form is not linked to a raw secret unless that exact byte string independently becomes a scanner candidate; evidence records this limitation. A dynamic expression that does not yield one exact byte value is outside the counter rather than a blocker. A scanner-recognized shape that cannot be extracted only in base is treated as a permitted deletion; an unextractable head-side shape, inability to enumerate/read the full bounded scope, loss of count integrity, or any other genuinely incomplete scan makes admission `inconclusive`. Violation evidence retains every bounded positive-delta candidate up to the full catalog/evidence capacity, names only newly added locations, uses one-based head lines where text evidence exists, and never lists unchanged occurrences. If repeated secondary count rows would exceed the fixed evidence byte bound, those duplicate rows may be omitted while the complete authoritative violation list remains `blocked`; location omission likewise changes only `location_status`, not the proved violation status. This admission status does not delay or suppress the trusted reviewer launch.
- A catalog-only count manifest that cannot fit one 64 KiB artifact may be stored as complementary public and helper-private shards without changing the logical manifest schema. Fixed metadata must match, combined rows must be duplicate-free and completely cover the catalog, and a unique SHA-256 commitment in the public control-state-bound limitations binds the canonical private rows. The loader verifies that commitment before merging and then reruns the normal full count and violation consistency checks; missing, duplicate, mutated, or incomplete rows fail closed. After this complete validation, oversized optional `accepted`, `legacy_counts`, and `secret_reductions` audit rows may be omitted from the public preflight while the authoritative `secret_delta`, including bounded addition locations, remains intact. The 64 KiB compact bound applies to the retained secondary synthetic-token section; the complete preflight remains independently bounded to 128 KiB and may switch from pretty to semantically identical compact JSON only when needed to stay within that same limit.

- Private cleanup identity binding supersedes the earlier fixed-name shorthand in the preceding preflight bullet. Preparation precreates both helper-private files as empty exact-`0600` slots before workspace materialization, captures their device/inode identities directly from the creation descriptors, syncs both files and the container directory, and then performs one complete identity handoff. Stateful preparation holds the runner lock while durably publishing a schema-v5 `preparing` marker with that full identity set, an initially absent preflight receipt, and the exact external layout binding `/tmp/codex-isolated-review-uid-<effective-uid>/<sha256(canonical-source-path)>/isolated-review-*`. After complete workspace ownership transfers, the marker advances to `ready`, records the canonical source root, retains the same digest-derived external namespace, and later gains the runner-sealed preflight receipt without changing its workspace or runner-lock bindings. Source-local `.codex-tmp` remains only a schema-v1-to-v4 legacy compatibility layout and is never the current schema-v5 destination. A payload writer reopens the fixed slot only with no-follow and nonblocking flags and without create or truncate, validates the same identity, owner, link count, mode, and initial empty size before writing, syncs the payload, and revalidates file/path identity afterward. A crash before the marker can therefore leave only empty slots and no materialized workspace, while any crash after helper-private bytes begin has complete durable recovery identities. Marker JSON is written by atomic replacement relative to the already-open preparation-bound container descriptor, then exactly read back and directory-synced before both the canonical parent path and canonical container path are revalidated. A container or parent swap therefore either writes the original bound object and restores its canonical path or fails the handoff without writing the replacement. A missing `state.json` with either phase can therefore recover the full identity-bound partial container after the runner lock is released. The in-memory/serialized review workspace, state marker, and control state carry matching immutable bindings. Cleanup accepts only those identities, quarantines and revalidates each file before removal, then atomically persists a monotonic per-file removal receipt through the same open container descriptor. A missing preparation-bound container, missing or replacement objects without receipts, and unresolvable corrupted-state layout paths all fail closed; a valid independent state marker still authorizes the bounded fixed-name scrub after layout-resolution failure. Recorded names that reappear are preserved, and one invalid artifact does not prevent scrubbing the other valid identity. The receipt-bearing control state remains until other container content is gone. Corrupted-state cleanup requires the independent marker binding to match that control state. A crash or forced termination after quarantine begins but before receipt persistence is intentionally ambiguous and fails closed on retry; it may leave the bound artifact under its quarantine name or leave a missing-name ambiguity that requires manual recovery from the retained container. This deliberately favors identity safety over automatic crash recovery. Schema-v4 markers remain compatible with `status`, `wait`, `final`, and cleanup, but cannot provide admission evidence because they have no runner-sealed preflight receipt; `stateful admission` fails closed as inconclusive. Malicious same-euid processes remain part of the host TCB: portable POSIX cannot exclude a peer with the same account from mutating owner-private namespaces, moving inodes away before cleanup, or replacing both an artifact and its path-loaded marker. The receipt and argv bindings close cooperative/path-replacement races inside the helper contract; they are not a same-euid hostile-process isolation boundary.
- The helper-owned finite synthetic-token catalog remains authoritative only for approved authoring fixtures and compatibility metadata. An authoring entry may suppress exactly its declared scanner rule after the existing exact-value and complete-RHS validation; it does not create a general namespace. Historical `legacy_exemptions` no longer select a separate admission policy: their decoded exact raw values, when present, use the same global `head_count <= base_count` rule as every other tracked exact secret, without unembedded counts, occurrence provenance, encoded-variant matching, or path-specific denial. `--synthetic-secret-exemption` remains accepted only as deprecated compatibility syntax and must not change the counter or authorize additional bytes. Prompt and tracked-review bytes remain integrity-bound trusted reviewer input outside admission counting. A completed admission audit is required before PR/master admission, not before reviewer execution; no separate offline/independent Codex preflight gate is created.
- The artifact-only Codex path admits the complete rendered prompt before any app-server process activity. Raw evidence and prompt limits remain necessary but are not treated as final transport limits: admission constructs the exact `turn/start` request shape with the actual prompt plus worst-case legal path, model, request ID, and thread ID fields, then applies the same canonical `ensure_ascii=True` JSONL encoder and 8 MiB record limit used by the live protocol. The protocol and admission paths share their request and parameter constructors. JSON escaping expansion therefore fails closed at evidence admission rather than after launch, and future request-shape changes cannot bypass the prelaunch budget without changing the shared constructor and its regression tests.
- Checkout materialization installs worker-owned cancellation handlers before it starts Git. Every bounded Git command and `cat-file --batch` process uses an independent session, binds the exact leader identity, and keeps signal delivery deferred to controlled checkpoints until its original group is proven empty and its leader is reaped. A cleanup failure retains the live `Popen` handle and group anchor for one fresh worker-owned retry. While closure is unproven, sanitized-view deletion is disabled and the view remains inside the retained worktree. If closure remains unproven, the worker emits an explicit closure gap, the parent reaps that worker before interpreting the record, and the attempt stops without a failure-state writer or worktree cleanup. The same rule applies during final worktree cleanup: a failed fresh retry stops before manual-recovery state is written. Selector, pipe, and signal-scope finalization cannot replace the receipt-bearing closure marker.
- Direct state, prompt, custody, settlement, and authorization helpers receive two bounded owner cleanup attempts. If a post-fork identity check fails, the parent retains a partial `SpawnedProcess` receipt, performs bounded PID and original-group settlement while the leader remains unreaped, and exposes a receipt-bearing exception if both attempts fail. Any final signaling, waiting, or reaping failure, including incomplete attempt-supervisor handoff cleanup, retains that receipt in a process-wide closure latch. The latch bars every later direct helper and worktree cleanup in that process; the marker crosses the review boundary, retains runtime custody, and prevents failure-state, recovery, custody-deletion, and destructive-cleanup work before the process exits inconclusive.
- Reviewer stdout/stderr stream directly to complete per-attempt files. Only a bounded head/tail capture is retained in memory for generic error classification; the middle is never buffered in the runner. Every final Codex, Claude, and Copilot review launch has a 30-minute supervisor deadline and a 64 MiB limit on each output stream; timeout, overflow, drain failure, or retained descendants terminate the process group and are reported as inconclusive rather than leaving the stateful runner lock held indefinitely. The failed runtime/model plus its log paths are persisted before returning the inconclusive state. Copilot's complete JSONL is validated directly from that file with a one-pass, constant-state parser and a 4 MiB per-record cap, so aggregate output larger than the memory capture does not corrupt terminal/model verification. Timeout-bearing helper calls must use this logged process-group path, every output-limit configuration must include a finite timeout, and the simpler captured-output path rejects timeout configuration before launch. Bounded-output arguments are validated before existing log files are opened. Prompt stdin uses an interruptible nonblocking writer, and output drains use interruptible nonblocking pipe reads; all I/O threads start inside a forwarded-signal mask and are registered only after successful startup, so a detached descendant cannot keep timeout cleanup blocked by retaining a pipe. Stateful status tails seek and decode only a bounded suffix instead of loading complete logs.
- Security-tool commands that write a named regular file use the same supervisor plus an exact logical file budget. A child wrapper raises an inherited soft `RLIMIT_FSIZE` only up to the existing hard limit, reserves one byte as an overflow sentinel, blocks before launch when the hard limit cannot preserve that sentinel, and normalizes `EFBIG`/`SIGXFSZ` without treating an arbitrary numeric exit code as a signal.
- Logged reviewer processes run in their own process groups. The helper forwards `SIGTERM`, `SIGINT`, `SIGHUP`, and `SIGQUIT`, reaps the reviewer, and terminates leftover descendants before restoring signal handlers; a stateful runner persists `128 + signal` as its terminal exit code. Foreground mode installs the same cancellation boundary before snapshot preparation and blocks signals across final cleanup, so preparation/preflight cancellation cannot strand a tracked-tree container.
- `stateful start` remains cancellable during workspace preparation. It defers a termination signal only across the narrow `Popen` handoff, checks it before publishing, and stops plus cleans any unpublished runner. The state becomes published only after the caller successfully flushes its path. The background runner keeps signal handling installed across preflight, executable discovery, model-attempt gaps, and child execution, then records `128 + signal` under a blocked finalization step.
- Claude/Copilot structured output is accepted only when no explicit `error`, `failed`, or `failure` state is present; partial text attached to an error result cannot become a clean final artifact.
- If Claude reports auxiliary model usage, effective-model verification selects the explicitly requested model entry rather than treating the first auxiliary entry as the reviewer model.
- Executable discovery validates `--version` identity and never trusts arbitrary repository `PATH` entries. It checks bounded system and user install locations. Codex and Copilot may use identity-checked native or interpreter-backed installations through a validated runtime path; Claude Code must resolve to a native Mach-O or ELF executable, so shebang scripts and interpreter wrappers are rejected before provenance checks. Explicit absolute overrides are `CODEX_REVIEW_CODEX_PATH`, `CODEX_REVIEW_CLAUDE_PATH`, and `CODEX_REVIEW_COPILOT_PATH`; an invalid explicit override or an explicit Claude override missing its GPG, probe-sandbox, or trusted review-tool prerequisite blocks the lane even when an automatically discovered absence could be runtime unavailability.
- Source files are never edited. The detached workspace is removed after `stateful wait` unless `--keep-workspace` is set or the legacy helper Codex runtime exits `127` after a matching successful preflight. That state is machine-visible in `stateful status`; it permits helper-specific diagnosis only and cannot satisfy or supply the canonical fresh-context Codex lane. Run `stateful cleanup` after diagnosis. A finite wait deadline covers runner completion, cleanup-lock acquisition, and workspace removal; `stateful final` applies its own bounded cleanup deadline. Timed cleanup runs in an independent worker that inherits the held cleanup-lock descriptor; lock ownership is handed off while forwarded signals are blocked, so a timeout or interrupted parent cannot let another waiter start a concurrent `rmtree`. Cleanup state-directory identity binds path and open descriptor with stable device, inode, mode, and owner metadata; entry churn beneath the same directory must not invalidate that binding, while path replacement must still fail closed. Cleanup-only legacy compatibility admits an empty owner-owned mode-`0664` `cleanup.lock` only through no-follow descriptors after proving a non-group/other-writable owner-owned `.codex-tmp` root and an exact-mode-`0700` state directory; after the exclusive lock is acquired, it revalidates both directories and the lock identity/mode before `fchmod(0600)`, `fsync`, and exact mode-`0600` validation. Every other group/other-writable or nonempty legacy lock fails closed without workspace removal. A daemon reaper collects the worker while long-lived callers continue. A successful worker clears any stale cleanup-error artifact while it still owns the cleanup lock.
- Logs, preflight evidence, attempts metadata, and `final.txt` remain in the state directory after workspace cleanup. Every newly created bound runtime artifact, control-state temporary, private slot, attempt stream, compatibility lock, and credential-update temporary is forced to exact mode `0600` on its held descriptor before publication or identity handoff, independent of a caller umask that masks owner permissions. A newly created `attempts` directory is likewise forced to `0700` inside the already-bound owner-private container before descriptor reopening, so an owner-masking umask cannot make the first launch inaccessible; a pre-existing name is opened and validated before any descriptor-based mode normalization. The stateful runner likewise forces both runner stdout/stderr descriptors to exact `0600` before spawning the child. Existing locks and artifacts are validated rather than chmod-repaired. Cleanup acquires the preparation-bound container directory lock before it creates, opens, or validates `cleanup.lock`, then acquires the compatibility lock; this fixed order serializes first-use initialization without weakening no-follow or path-identity checks, and timed worker handoff preserves the same descriptor order. Runner-liveness observers use shared nonblocking probes against the runner's exclusive lease, so concurrent `status` or `wait` probes cannot impersonate a live runner while a real runner still blocks cleanup. `attempts.json` is atomically rewritten after every completed model attempt through the same preparation-bound, exact-readback runtime-artifact writer used for the state marker, so cancellation during a later entitlement fallback preserves the earlier model/category/effective-runtime evidence without following a replaced container or parent path.

## Terminal States

### Reviewer Final

- exit `0`: a non-empty terminal final artifact exists
- exit `75`: transient/capacity or explicitly inconclusive evidence; retry only the same runtime/model if the parent policy permits
- other nonzero: blocked or failed; inspect `stateful status`, `attempts.json`, and bounded logs
- `stateful final` prints only the saved terminal artifact on success
- exit `127` with `fallback_workspace_retained: true` means preflight succeeded but the legacy helper Codex executable was deterministically unavailable; inspect or clean that helper state, but never use the retained `.git`-free scope as the canonical Codex lane
- workspace cleanup failure is terminal nonzero even when the reviewer produced a clean artifact; the error and retained state directory remain visible for recovery

### Helper-State Secret Admission

- Run `stateful admission --state-dir <state_dir>` only as the second current-head check after harvesting the reviewer artifact with `stateful final`.
- exit `0`: the bounded public `secret_delta` summary is valid and `clean`, and the exact `preflight.json` bytes match the schema-v5 runner-sealed receipt; this is a successful optional helper-state check, not the required PR/master/merge-ready admission producer.
- exit `1`: the valid bounded summary reports one or more violations.
- exit `3`: the runner lock is still held, so the state is pending regardless of whether a preflight receipt has already been published; no terminal admission evidence exists until lock release.
- exit `75`: the valid bounded summary reports an inconclusive audit, the state is terminal but its preflight was not sealed, a schema-v4 marker lacks a receipt, or safe bounded admission evidence cannot otherwise be established.
- A successful reviewer final remains valid review evidence when admission exits `1` or `75`; neither result may be reclassified as reviewer failure. Foreground output is not admission evidence, and any head change invalidates both checks.

Each attempt records runtime, requested/effective model, requested/effective effort when observable, category, exit status, and log paths. Claude entitlement evidence comes only from a launched final-review invocation with strict structured-error and requested/effective-model verification. For Codex, the helper resolves the emitted thread ID to its persisted rollout and requires matching `turn_context` model and effort before accepting the final artifact. Missing verification on a successful result is `runtime-unverified`; any observed model, effort, or permission mismatch overrides even an otherwise entitlement-shaped failed attempt and stops the lane. None of those conditions is entitlement evidence, so none enters the fallback path.

Claude Code publisher provenance is anchored to Anthropic's release-signing primary-key fingerprint `31DD DE24 DDFA B679 F42D 7BD2 BAA9 29FF 1A7E CACE`. For the exact detected version, the helper verifies `manifest.json.sig` over `manifest.json` from `downloads.claude.ai`, then requires the installed platform binary to match the manifest checksum. Native format and platform compatibility checks reject script/interpreter wrappers and incompatible artifacts but cannot distinguish every native launcher. macOS platform signing and notarization are possible future defense-in-depth checks, not current acceptance gates, and never substitute for the signed manifest. No Claude candidate may receive credentials or tracked review content until provenance and bounded capability probes pass.

## Deliberate Omissions

The helper no longer supports generic `auto` lanes, OpenCode, Cursor Agent, `gh-copilot`, `codex-parallel`, live working-tree snapshots, reviewer-visible Git shims, arbitrary child argv, report sinks, or legacy helper names. These surfaces caused ambiguous review counting, model drift, or unnecessary runtime code exposure.
