# Codex Review Workflows

Public review orchestration, synthetic fixture selection, and local delivery gate skills.

A legacy unreceipted artifact never becomes the selected completion basis.

Independent-review mutable state lives below the current account's home-derived
review-runtime root rather than an immutable installed release. The shared
regular-file openers request nonblocking, no-follow descriptors before
validating file type, ownership, mode, link count, and identity;
content-sensitive callers then reread bounded bytes from those descriptors.
`O_NONBLOCK` avoids FIFO peer-rendezvous blocking, so FIFO substitutions fail
closed without waiting for a peer. Device nodes fail regular-file validation
only after `open()` returns; arbitrary device-driver open latency remains
unbounded.

`review-orchestration-playbook` is the single entrypoint for named single, double, and triple review plus PR readiness. The canonical Claude Code lane admits stable publisher-verified releases in `>=2.1.211,<3.0.0`; that compatibility range is defined once in [claude_version_policy.py](skills/review-orchestration-playbook/scripts/review_runtime/claude_version_policy.py). Claude Code `2.1.212` remains the audited per-version stream-schema baseline, not a global eligibility pin. A selected release must still pass its exact signed per-version manifest, digest, and executable-identity checks, the advertised-capability contract, and strict fail-closed stream validation before it can supply findings.

The canonical Claude lane captures bounded raw stdout outside the model-visible worktree and must pass it through the trusted bundle's `named_lane_guard validate-claude-stream` profile, launched by the recorded absolute Python interpreter with `-I -B -S`. The profile raw-loads the manifest-bound [validate_claude_stream.py](skills/review-orchestration-playbook/scripts/validate_claude_stream.py), compatibility profile, audited schema baseline, and capability-contract evidence, then binds the selected release and all referenced source digests. It checks the parent-private accepted preflight result, exact cwd, model, fixed local-login authentication source, and child return code; selects the reviewed closed legacy or extended stream profile for the preflight-selected version; and validates every admitted intermediate event against the selected closed, session-bound contract before checking the terminal result. The preflight evidence must be a current-user-owned, single-link regular file outside the reviewer worktree, with no group or world permission bits. Direct execution of the validator script is compatibility-only. Only `classification: accepted` with a zero child exit supplies findings; prose inspection, partial output, or an ad hoc parser does not satisfy named double/triple review, and acceptance does not prove the final merged sandbox.

Artifact trust and review meaning are separate. The validator continues to return the accepted terminal result verbatim; only afterward does the trusted bundle's manifest-bound `named_lane_guard classify-review-result` profile execute the bound [review_result.py](skills/review-orchestration-playbook/scripts/review_runtime/review_result.py) bytes to record `review_outcome` and `presentation`. Exact sentinel-only output is `canonical-clean`, while a concise non-actionable coverage summary ending with exact `No findings.` is `extended-clean`. Actionable content overrides a contradictory sentinel, and uncertain or nonconforming prose never becomes clean. Presentation alone never triggers an automatic rerun; reruns require invalidated range/head evidence, a finding fix on a new head, or an explicit request.

## Named Review Shapes

For the GitHub lane, terminal findings remain blocking negative evidence; terminal clean remains artifact-level classification only.

A parent-owned audit separates request production policy from provider-evidence
consumption. The orchestrator waits for both required local lanes to become
terminal, may post at most one third-lane request for an unchanged head, and
must never post a second or third request. Record `early-request-observed` for
an early request and `duplicate-observed` when more than one same-scope request
exists, including an overlapping or still-pending extra request. These warnings
are outcome-neutral. A lone compliant pending request is not a warning;
positive completion remains pending unless a complete `thumbs-up-clean`
reaction basis is accepted. Do not post another request to repair either warning.

Every request admitted by the request or reaction plane also needs one closed
parent-owned request-time scope sidecar. It binds raw pre/post PR-detail and
compare responses plus the exact `201` POST response one-to-one to the request's
eight normalized fields, including closed `user: {login, type}` actor identity,
and immutable `(repository, PR, merge base, head)` tuple. The sidecar is a
sibling of the raw discovery/current inventories; raw
transcript schema version 4 and its fetch kinds remain unchanged. Missing,
malformed, extra, or unmatched receipts make `request_policy` `unknown`, forbid
another POST while same-scope history is unproved, and disable the affected
reaction path, but do not veto a separately trustworthy terminal payload. A
valid old-head receipt remains old-epoch audit evidence and does not count as a
current-head request; an old-epoch-only request/reaction seeded scope produces
no current result entry and remains audit-only. A valid
same-head/different-merge-base receipt is
not this audit-only exception: it instead takes `base-changed-same-head` and
forbids a replacement request. The sidecar neither establishes
request/run lineage nor proves continuous stability: equal pre/post point reads
cannot exclude an intermediate ABA transition.

Every terminal-looking exact-provider artifact admitted to current-scope
precedence separately carries one singular closed `artifact_scope_receipt` of
kind `parent-recorded-terminal-artifact-scope-v1`. Its exact fields are `kind`,
`pre_artifact_scope_receipts`, `artifact_get_receipt`, and
`post_artifact_scope_receipts`. Raw pre/post pull+compare responses bind the
artifact-time head and merge base; the exact artifact GET binds repository/PR,
channel/native ID, provider identity, semantic time, body/digest, grammar, and
artifact commit. Clean and malformed evidence require the exact current tuple;
an ancestor finding preserves its artifact-time head while normalized
`scope.head` remains current and local ancestry proves applicability.
The server-time envelope is `pre Date < artifact semantic time <= artifact GET
Date <= post Date`; whole-second equality at the pre edge is inconclusive.
Lifecycle remains independently re-read. A previously persisted identical
receipt may be reused, but an artifact that does not strictly follow every
trustworthy pre observation cannot be scoped retroactively and is
`triple-inconclusive`. This receipt is independent of request sidecars and
creates no request/run/artifact lineage. Missing request sidecars still affect
only request/reaction authority; missing or unstable artifact receipts block
the wrapped terminal artifact. Neither receipt's point reads prove an ABA-free
interval.
A truly absent pre-v1 receipt is the narrow audit-only exception: keep the
strictly older, otherwise well-formed artifact raw, exclude it from normalized
receipt-bound wrappers, and admit it only through the closed
`legacy_unreceipted_audit` partition. It never supplies positive authority or
becomes the selected terminal-classification basis. A later accepted receipt-bound result
may still have a non-null `evidence_basis` that carries the item in
`legacy_unreceipted_artifacts`; the legacy item does not by itself veto that
result when the partition, time, stability, precedence, and thread gates all
close.
The frozen reaction-history `as_of_server_time` bounds historical artifact
semantic time, not receipt collection time. Exact artifact GET and post-scope
response `Date` values may be later when gathered during the bounded
decision/final reread; observation time is not silently turned into semantic
evidence time.
A strong current `terminal-payload` or `mixed` artifact may likewise arrive
after declaration discovery during the bounded provider wait. The frozen
as-of bounds historical samples and the separate current reaction-only basis
for `thumbs-up-clean`, not strong current terminal evidence.
The machine-readable
[`base-only-retarget-state-machine.json`](skills/review-orchestration-playbook/references/base-only-retarget-state-machine.json)
is version 3: it preserves version 1 request-sidecar event semantics and
version 2's independent terminal-artifact scope plane, then adds
`scope_assurance`, `whole_pr_completion_action`, `clean_action`, and
`negative_evidence_action`.

For the third lane, re-read a complete authenticated current-scope provider snapshot and classify it under [GitHub Codex Evidence Authority](skills/review-orchestration-playbook/references/github-codex-evidence-authority.md). Record `request_policy`, `provider_profile`, and `evidence_basis`. The provider profiles are `terminal-payload`, `mixed`, `thumbs-up-clean`, and `unknown`. A trustworthy current-scope terminal payload controls artifact classification precedence for `mixed` regardless of reaction ordering: a later `+1`, `eyes`, or other reaction does not overturn it, and `mixed` never accepts reaction-only clean. `eyes` is liveness-only and can block only the weaker reaction-only fallback when it is at or after the selected `+1`.

A receipt-bound terminal payload supplies artifact-level clean/findings classification only. Its `evidence_basis.scope_assurance: artifact-publication-only` records artifact-publication scope; it does not attest the provider's internal input merge base or request/run/artifact lineage and cannot complete triple or make the PR merge-ready. Terminal findings remain blocking negative evidence, while a terminal clean classification is audit-only for readiness. Only a complete `thumbs-up-clean` reaction basis can complete the lane. Any future positive terminal-payload authority requires a predeclared provider-authenticated input-base or request/run/artifact binding; the current accepted provider-input-binding schema set is empty.

Only `thumbs-up-clean` can reach the authority's complete weak `+1` fallback. Its 3–10 historical samples come from distinct non-current scopes; the exact current scope is validated independently and never counts toward that minimum. Each historical sample and the separate current snapshot binds one selected exact `@codex review` parent request, its exact request-time scope sidecar, and its individual child exact-bot `+1` by IDs, URLs, immutable scope, server times, and strict ordering, enumerates every accepted same-scope request plus fully paginated reactions, and binds provider declaration identity/digest.

Schema-version-4 bounded dual-source discovery combines an updated-desc pull boundary, a fully paginated since-cutoff repository issue-comment feed, and exact current/declaration anchors. The frozen as-of bounds semantic history rather than later raw observation time: future-prefix rows remain raw and budget-charged and seed complete pull/compare/comments/reviews/inline/thread/reaction detail traversals. Each traversal retains its complete local union. Canonical ordinary-issue `@codex review` comments are validated, retained, and budget-charged as raw-only non-seeds; mismatched or ambiguous PR-like routing fails closed. Canonical decimal page and native-ID tokens are limited to 39 digits and 128 bits before integer conversion; overlong values fail closed without raising. REST Link page relations are validated semantically against the fixed HTTPS host, path, and non-page query map; omitted page and a literal canonical `page=1` are equivalent, while each raw `rel=next` URL is followed exactly. A terminal GraphQL page requires typed `hasNextPage == false`; `endCursor` may be null or a non-empty string, and a retained terminal cursor never triggers another fetch. Stable pull identities exclude volatile `updated_at`, raw row digests, and endpoint order; closed `retained_pull_scope_audit` items bind every local-union PR's pull/base/head/merge-base/lifecycle identity, including request/anchor-only and record-free scopes. Pull-list `state` must match pull-detail lifecycle state.

A future-prefix-only scope enters `future_prefix_omission_eligibility_audit` only after full detail proves no request-feed or anchor co-seed, no in-window or provider/policy-bearing semantic record, and only removable confirmed-different post-as-of activity, if any. An empty scope can be eligible. The eligibility audit is a closed subset of `retained_pull_scope_audit` identities, and the per-traversal parser still retains that scope and its normalized evidence. Only the initial/final joint coordinator may omit it from the derived stable comparison, and only when the PR appears in exactly one complete local union and is eligible there. A PR observed in both traversals is always retained and compared exactly, so later unrelated human activity cannot erase an already retained scope; eligibility identities shared by both traversals must also match exactly. After both traversals independently prove complete, the joint stable comparison treats `window-boundary-complete` and `natural-end-complete` as equivalent complete termination forms. Each raw-derived/stored projection keeps its exact stop reason; only that transport label is excluded from the derived comparison, and incomplete or malformed pagination remains fail-closed.

The pre-coordination raw union counts toward the 512 seeded-PR cap; boundary witnesses and cumulative old PRs do not. Every universe candidate's complete pagination, stable snapshot, and scope-final ordering basis is validated before the newest 10 are selected, including candidates outside that set. Apply the cutoff only after complete schema, native-ID, canonical-URL/parent-join, timestamp, and actor validation. A confirmed-different non-request record wholly after the cutoff may remain raw-only; a controlled `@codex review` request, exact-provider or ambiguous/provider-like record, exact/ambiguous child, cross-cutoff edit, scope/lifecycle drift, or incomplete source/detail page remains fail-closed. Transcript v4 has no inline-child timestamp with which to infer a removable later reply. The reaction `evidence_basis` embeds independently complete initial/final inventories whose jointly coordinated stable views and complete candidate arrays are type-preserving identical. Raw pages, opaque cursors, volatile pull rows, and a proved one-sided eligible scope may differ. Complete current snapshots are also required. The selected `+1` parent is the unique latest request by semantic time, so a newer request, old-epoch reattachment, or cross-parent conflict prevents weak clean.

Every independent historical discovery traversal and current raw endpoint traversal carries a parent-owned `resource_budget` sibling beside, never inside, transcript v4. The exact closed profile is `{profile: github-codex-evidence-resource-budget-v1, schema_version: 1, max_seeded_pull_requests: 512, max_controlled_requests: 512, max_fetch_attempts: 8192, max_retained_pages: 4096, max_records: 20000, max_page_body_bytes: 8388608, max_retained_utf8_bytes: 67108864, deadline_seconds: 900}`. Endpoint, request-scope-sidecar, and terminal-artifact-scope-receipt validation use three non-borrowing ledgers under one inventory start/deadline. Pre-count each sidecar or artifact wrapper. An ordinary terminal artifact wrapper plus five raw scope/artifact responses costs six records. A lowercase 10-hex clean wrapper adds two independent resolution responses, for seven raw responses and eight records. The raw short carrier remains `clean-pending-resolution` and non-authoritative until its exact resolution companion joins artifact ID, scope, ref, and full current head; current, complete-history, and sidecar-blind historical paths all require that join, and sidecar-blind may ignore request sidecars but never the resolution companion. Action parity here is limited to 10/40 carrier lengths and the short carrier's fail-closed exact-repository REST outcome. Lowercase-only refs, exact marker spacing and boundary, closed grammar, and independent parent-recorded initial/final receipt evidence remain stricter playbook-local rules. The artifact ledger is created once per decision pass; each immutable wrapper is charged and validated once, then its memoized result is reused by ordering, audit, profile, outcome, and report consumers. A per-candidate/scope/recomputation reset, repeat charge, or prefix acceptance is forbidden. Charge before request or accumulation under a monotonic deadline; endpoint or aggregate artifact overflow selects `unknown`, while request-sidecar overflow closes request/reaction authority only. The 512 cap applies to the discovery union/detail traversals, not cumulative repository history or old boundary witnesses. The 20,000-record, 8-MiB response, and 64-MiB run magnitudes align with the pinned released Action; the remaining caps are playbook extensions.

Duplicate, overlapping, or pending same-head requests do not require request/run attribution and do not by themselves negate trustworthy current-head terminal evidence. Their complete raw pages remain required audit input, but a request or reaction added between the final reads changes only request/reaction authority; it does not overturn an independently stable receipted terminal classification. The complete terminal provider-authored payload from exact-bot review/comment evidence still supplies the authority's strict artifact-level clean/findings classification when it matches the fixed clean/finding/inline-parent grammar and carries its artifact-time receipt, but it supplies no whole-PR completion authority. In normalized current evidence, `scope.head` remains current: clean must bind that head, while a finding preserves its own current-or-proved-ancestor artifact commit and remains in the complete projection. A proved non-ancestor remains raw audit-only; its presence in normalized active or unresolved finding lists is a projection mismatch selecting `unknown`. A later current-head clean may supersede a projected ancestor top-level finding for classification precedence, but an unresolved applicable thread still blocks. Every other terminal-looking exact-provider payload is malformed. Fetch the selected review body plus every fully paginated associated inline review comment, or the complete terminal issue-comment body, and include relevant thread state in the final re-read. A clean-looking `APPROVED` review classifies clean only when its fully paginated exact-provider selected-review target-child set is present and empty; a valid target child is findings, an unread or incomplete associated-inline inventory or malformed target join is inconclusive, and fully fetched human, unrelated-bot, null-parent, and unrelated-only records remain audit context. When no complete `thumbs-up-clean` reaction fallback is accepted, missing terminal evidence or an unestablished reaction-only authority classification remains pending while bounded waiting is meaningful; after exhaustion, ambiguous identity, scope, payload, pagination, or classification is `triple-inconclusive`. An exact-App current-head check/run is service-start evidence only; it never completes triple or proves clean/no-findings, even when `completed` / `success`. A same-App check may be unrelated, and check success can coexist with provider review findings. A base-only retarget after a request still invalidates the old whole-PR evidence and does not authorize another same-head request: readiness is `blocked-input` (`base-changed-same-head`) and the requested triple is `triple-inconclusive` until a separately authorized ordinary change produces a new head. Do not manufacture an empty or anchor commit to escape this blocker.

The `thumbs-up-clean` declaration is authoritative only as the canonical GitHub REST issue-comment resource that the parent reads directly and re-fetches unchanged during final revalidation, with exact `user.login == "chatgpt-codex-connector[bot]"`, `user.type == "Bot"`, `performed_via_github_app.slug == "chatgpt-codex-connector"`, exact repository/PR/API-URL/HTML-URL bindings, and the exact `If Codex has suggestions, it will comment; otherwise it will react with 👍.` line. Both bounded dual-source discovery passes must include its bound PR as an explicit anchor, fully traverse it, and find that exact raw record once. Declaration authority and terminal classification are orthogonal roles: only an independently nonterminal declaration record is audit-only. If that same exact-provider comment also matches clean, findings, or malformed terminal grammar, retain and rank its terminal role normally. A declaration-only PR is a confirmed non-candidate, other exact-provider free-form prose fails closed, and in-window terminal-looking malformed prose remains a candidate. A fully parsed malformed record at or before the exclusive lower boundary remains audit-only confirmed-non-candidate evidence. Generic issuer/source fields, caller-supplied records, and self-hashed paraphrases are invalid.

Evidence-consumption policy is pinned atomically to source
`JoeyTeng/codex-review-gate@16366aa81270ad2c875d2ceb8ce194f5b2308af6`,
released Action
`JoeyTeng/codex-review-gate-action@2a7f9d8cd98f90cb56dc1540bf54d9dc7484afc6`,
common tree `d03de9035d20f285e6a93986d436403b4a30e9bc`, the complete 15-path blob
manifest, and the result-present decision rationale in the authority reference.
A branch/tag, prose-only comparison, or partial runtime diff is not an
anti-drift substitute. Provider-result parsing authority and request/run independence
come from that pinned Action baseline; artifact-publication scope receipts,
ancestor-finding projection, raw thread/lifecycle proof, and conditional `+1`
fallback, including the stable future-prefix discovery projection, remain explicit
playbook extensions and must not be silently attributed to or removed in
favour of the Action.

The authority's report matrix is normative. `request_policy.status` is `compliant`, `warning`, `unknown`, or `not-applicable`: a proved no-PR or unsupported-host/identity path is `not-applicable`, while unprovable early-request timing is `unknown` without a warning code. A pre-provider blocker uses null profile/basis; an eligible wait uses the computed profile or `unknown` with null basis. An artifact-level terminal clean/findings classification uses `terminal-payload` or `mixed` plus its selected basis but does not complete the lane; accepted weak reaction clean uses `thumbs-up-clean` plus its complete reaction basis and is the only positive completion path. An inconclusive result uses its computed profile or `unknown` and a stable blocking basis when one exists, otherwise null. See the linked authority for the field-level matrix.

- **Single:** exactly one clear/fresh-context Codex `reviewer` agent launched with `fork_turns="none"` or the platform-equivalent zero inherited turns, in a separate read-only clean detached Git workspace. Immediately before spawn, the parent invokes trusted `materialize-worktree` and then `validate-worktree` through a recorded absolute guard path from an independently trusted bundle outside the candidate range. The materializer requires a full non-shallow, non-promisor source with no alternate dependency; proves `base_sha` is the sole merge base and an ancestor of `head_sha`; and derives the exact source commit scope as `{base_sha} ∪ (base_sha..head_sha)`. It initializes an owner-private repository, exposes only the validated source object store through a temporary destination-scoped alternate, creates exactly one materializer-owned shallow boundary at `base_sha`, and imports the complete commit/tree/blob snapshot closure for every scoped commit. Source config/refs/hooks, clone/fetch/upload-pack, persistent alternate/promisor dependencies, and remote transport are excluded; hooks, fsmonitor, system/global attributes/config, submodule recursion, executable filters, and external diff commands are disabled before detached checkout. `git worktree add` and every pre-validator status query are forbidden. Candidate `validate-worktree` requires that same frozen `--base` and `--head`; before performing the first status query it revalidates both lane refs, exact `BASE+LF` shallow content, endpoint commits, the unique merge base, and exact range topology. It then rejects hidden index bits, ignored artifacts, materialized or initialized submodules, unsafe direct configuration, and a direct `core.fsmonitor` other than unset or Git-false, while permitting an absent or empty uninitialized gitlink. Both guards deliberately ignore unrelated `mtime`/`ctime` churn and do not snapshot ordinary file contents. Every reviewer Git invocation uses the exact parent-supplied sanitized argv prefix and every diff-producing command adds `--no-ext-diff --no-textconv`. The agent loads applicable skills, scoped `AGENTS.md` files, and project guidance, then obtains and reviews the frozen `base_sha..head_sha` itself; its prompt never embeds or attaches a prebuilt full diff.
  The source marker/admin/worktree root must agree exactly so Git cannot select a sibling `.bundle` / `.git`; source and target ancestry are fenced. Every scoped commit other than `base_sha` must name only parents inside the scoped commit set, so a merge or other graph shape that exposes an outside parent fails closed rather than adding another shallow boundary. The guard rejects arbitrary, missing, additional, or changed destination shallow state; proves the imported commit set equals the source scope and the total destination object inventory equals the exact manifest; and only then runs bounded `fsck`, local completeness checks, and detached checkout. The existing 250,000-object, 2 GiB logical-object, 100,000-entry, and 2 GiB checkout-occurrence ceilings remain unchanged; the exact-range compressed-pack ceiling is 768 MiB. Copied commit-graph and multi-pack-index caches are disabled in every prefix and lane-local config. The resolved fixed-path Git executable is an explicit parent trust root whose version is behavior-checked, not a publisher-identity claim.
- **Double:** single plus an actual Anthropic Claude Code process launched directly in another independent read-only clean detached Git workspace over the same frozen range. The exact revalidated Claude executable is the direct child of the same absolute trusted guard's process-only `run-claude` supervisor, which supplies a finite deadline, bounded streams, cleanup for the initial supervisor process group and inherited streams, an allowlisted account/locale/UI/proxy/CA launch environment, and forced non-fetching/non-prompting Git controls without preparing a diff or becoming a reviewer. Its test-oriented CLI overrides may only retain or tighten the production caps of 1,800 seconds, 64 MiB per stream, and a 256 KiB prompt; direct Python callers cannot raise them either. Private output leaves must be absent and non-symlink beneath their caller-supplied canonical real parent directories. The active canonical launch accepts only ordinary local login in trusted real `HOME`; it exposes no API-key or OAuth-token environment interface, so prohibited refresh or unavailable local login is `blocked-authentication`. Validator/schema recognition of `ANTHROPIC_API_KEY` is compatibility evidence only and does not make that launch mode available. This is not a process-tree sandbox: a descendant that deliberately escapes the initial group with `setsid()` or `setpgid()` and closes inherited streams is outside that guarantee. The supplied-diff/private-Git `isolated_review` helper is diagnostic-only; GitHub Copilot requires a separate explicit request and never satisfies or increases the named double-review shape.
- **Triple:** double plus exact `@codex review` on exact host `github.com` and complete authenticated current-scope GitHub Codex evidence accepted under the authority above. Every other host, including `sqbu-github.cisco.com` and every GitHub Enterprise host, and operating identity in `{hoteng, hoteng_cisco}` is unsupported; report `effective double`, not triple. Only review/comment evidence from exact REST bot `chatgpt-codex-connector[bot]` with type `Bot` and the fixed terminal grammar can supply a terminal payload; that payload supplies only artifact-level classification, while only a complete `thumbs-up-clean` reaction basis completes triple. Exact-slug `chatgpt-codex-connector` checks can prove service start only. Unknown identities are inconclusive. The request comment is not completion, and request-policy warnings do not determine the review outcome. The fixed authority baseline has no accepted no-start body grammar and an empty accepted structured capability/installation schema set, so integration/service uncertainty and free-form provider prose cannot prove unavailability. When no complete `thumbs-up-clean` reaction fallback is accepted, missing terminal evidence or an unestablished reaction-only profile remains pending while bounded waiting is meaningful; after exhaustion, generic failure or an unstable profile is inconclusive.

For a selected PR, the orchestrator independently reads authenticated `baseRefName`, `baseRefOid`, and `headRefOid`, requires locally complete endpoint commits with lazy fetching disabled, and requires `git merge-base --all pr_base_oid pr_head_oid` to yield exactly one `pr_merge_base`. A selected PR's explicit frozen range satisfies PR-specific readiness or triple completion only when `base_sha == pr_merge_base` and `head_sha == pr_head_oid`. A same-head/different-base range is `blocked-input` (`scope-mismatch`): preserve the caller's range, do not silently rewrite it, and never describe its local review results as whole-PR coverage. Explicit-range-only standalone single/double with no selected PR is unaffected.

Every selected PR, including an explicitly named PR, must have exact lifecycle `state == "open"`, `merged == false`, and `merged_at == null`. Missing or contradictory lifecycle evidence is `pr-lifecycle-unverified`; closed-unmerged is `selected-pr-closed`, and merged is terminal `already-merged` / `selected-pr-merged`. Revalidate this tuple at selection, before the GitHub request, before accepting a result, and before readiness/merge. An observed closed or merged state at any mandated snapshot after request/service start invalidates the evidence and remains triple-inconclusive. These point-in-time snapshots do not prove that no intermediate close-and-reopen occurred between them.

When this repository reviews a change to its own review policy, candidate-head Markdown remains visible as review subject and scoped guidance, but candidate-head Python and shell never bootstrap the review. The parent records an independently trusted external bundle's absolute source path, version, and SHA-256 digest for the reviewer profile, prompt, materializer, validator, launcher, compatible-version/provenance preflight, and Claude stream validator/schema. The non-executable guard is invoked through a parent-validated absolute Python interpreter with `-I -B -S` and directly compiles only its manifest-bound raw-source closure for the selected profile without adding the bundle to ordinary import resolution or accepting bytecode or native-extension substitutes. Use the prior trusted policy and its prior guard interface for this migration review; merge and release the candidate before activating its exact-range materializer or mandatory-`--base` validator contract.

PR readiness layers required CI, review-conversation resolution, and branch/base checks onto the effective shape. It does not add retired extra Codex gates. The legacy `isolated_review` Codex helper may remain documented as low-level compatibility or diagnostic infrastructure, but it does not count toward single, double, or triple review.

Repository automation must not auto-start or require a separate Codex PR-review gate for ordinary PR events. The GitHub Codex lane begins only from the exact `@codex review` request in an eligible named-triple flow; single, double, and effective-double PRs have no hidden Codex reviewer. The temporary `codex/review-gate` compatibility workflow runs only its trusted base definition and posts the exact legacy commit status to the PR head while repository and organization rulesets are migrated. It checks out no PR code, posts no review request, starts no reviewer, and provides no review evidence or named lane. PR readiness observes it only as a legacy required status.

After the compatibility workflow reaches the default branch, a maintainer must run its input-free `workflow_dispatch` once. Each migration-backfill pass fully paginates all pull requests in creation order through the authenticated API, validates and canonically sorts every record, and buffers the API-reported open heads without publishing. Using the non-removing `state=all` membership prevents open/close transitions from shifting later pages; only after two consecutive canonical `(pull request number, current head SHA)` snapshots match does the workflow publish `codex/review-gate=success` for exactly that stable snapshot. Page-shift duplicates are retried, and missing or malformed evidence fails closed. Failure to stabilize within six passes publishes nothing and fails. A stable authenticated empty open-PR snapshot publishes nothing and succeeds, and the workflow never accepts a caller-supplied SHA. This bounded convergence covers every open head in the agreed snapshot without a checkout, reviewer, or review request; later opened, reopened, or synchronized heads remain covered by the ordinary event path. Delete the entire compatibility workflow and its backfill as soon as both repository and organization rulesets stop requiring the legacy context.

A bare named-review request is report-only. It does not authorize branch creation, an anchor commit, push, PR creation, or PR branch/metadata changes. Use `blocked-authorization` when the intended scope includes dirty or untracked state that would require an unauthorized anchor commit; use `blocked-input` when a clean checkout lacks the required range, PR, or target/base selector. Bare triple may post only the scoped `@codex review` request on an already-existing supported PR. If no PR exists and an explicit committed range or target/base supplies the local range, run the two local lanes and report effective double. An existing unsupported PR still requires intended-head/current-head alignment before its concrete unavailability reason reduces the shape to effective double. If a still-eligible PR's head does not match the frozen head and PR mutation is not separately authorized, report requested triple/effective triple-inconclusive with GitHub lane status `blocked-authorization`; do not change the PR. For the same mismatch on an already unsupported PR, keep `requested: triple`, `effective: double`, and report readiness `blocked-authorization`; do not treat the mismatch as making the already-unavailable lane triple-inconclusive or as permitting readiness to continue. PR mutation requires a separate explicit PR or delivery-workflow request.

For the named direct Claude lane's accepted real-`HOME` native-sandbox design, “read-only workspace” describes required reviewer behavior and requested write denial, not a global host-read whitelist. Launch must request global `denyWrite` and critical-sensitive-root `denyRead`; those requested controls define the native-sandbox enforcement boundary, while sandboxed Bash may technically read another host path not covered by `denyRead`. The prompt therefore forbids outside-workspace reads. Record them as requested configuration because capability output and `system/init` evidence—including Claude Code 2.1.212 output—do not prove the final merged sandbox or managed permission arrays. The publisher-verified CLI may still update ordinary authentication/runtime state in trusted real `HOME`, including authentication refresh and necessary CLI cache or tool-result artifacts; `--no-session-persistence` does not make that control plane immutable, and these accepted CLI side effects are not model-authorized review mutations.

## Low-Level `isolated_review` Helper Only

Everything below this heading through the synthetic-fixture note documents the low-level `isolated_review` helper. It records `review_contract: supplied-diff-private-git` and `named_lane_eligible: false`, materializes its supplied diff and bounded Git objects in a helper-owned detached worktree backed by private minimal Git, and defaults to a clean committed head. Only explicit `--include-source-wip` may capture a digest-bound composite of staged, unstaged, and nonignored untracked state; that WIP result is diagnostic and never PR-readiness evidence. Its executable snapshots, outer sandboxes, `/auth` carrier, credential brokers, credential-lock catalog, guarded writeback, recovery artifacts, and historical supplemental fallback are not requirements or guarantees of the canonical direct Claude lane and cannot satisfy named double or triple review. The canonical lane instead follows the ordinary real-`HOME` authentication/runtime control-plane and selected-deny contracts linked from the named review shapes above.

The helper's production app-server input contains the complete authenticated primary diff plus a deterministic bounded set of changed regular UTF-8 text blobs from the head tree. It inspects at most the first 128 size-eligible candidates in raw Git-path order, skips binary content and paths the evidence format cannot represent, and retains at most 32 files, 64 KiB per file, and 512 KiB total. The checkout worker authenticates every retained blob in the evidence manifest, and the reviewer launch reopens and verifies the exact worktree files before passing their paths as nearby context; any retained manifest, ordering, content, or identity drift fails closed.

The helper's external ChatGPT auth carrier records paired wall-clock and suspend-aware monotonic baselines when it authenticates the owner-only source. Linux and WSL use `CLOCK_BOOTTIME`; Darwin uses `mach_continuous_time`. Every later sample and state update occurs under one evidence lock. The high-water record rejects any monotonic rollback and wall rollback beyond the one-second sampling-skew tolerance, commits the latest effective wall time before checking expiry, and never drops a high water after a failed lifetime check. Time spent asleep is therefore charged, and neither retry order nor the tolerance can restore or extend token lifetime.

For catalogued local-login artifacts, that low-level helper serializes the complete host refresh transaction, not only final writeback. It acquires the certified host locks before reading either macOS carrier or the Linux/WSL2 credential file, then keeps the same lease across credential exposure, network refresh, durable staging, supervised-process or broker quiescence, final drain, and verified host commit. A second helper waits before credential exposure and re-reads the host state only after acquiring the lease. Safe completion releases the lease; unproven quiescence or persistence deliberately leaves the shared lock directories as a stale fence and pauses with recovery diagnostics. Helper authentication precedence is `ANTHROPIC_API_KEY` > `CLAUDE_CODE_OAUTH_TOKEN` > local login; either explicit source is opaque-forwarded and bypasses this local-login transaction.

After its separate helper-only workspace, sandbox, and authentication preparation, the helper passes captured raw Claude stdout through the same canonical strict stream validator. It binds the selected compatible version and parent-selected authentication source, applies the closed legacy or extended init/intermediate/terminal profile, and accepts findings only from `classification: accepted`; helper-only broker or carrier evidence never substitutes for this output contract.

Publisher verification means the fixed Anthropic signing key, signed per-version manifest, and matching binary checksum. Native Mach-O/ELF checks reject script/interpreter wrappers and incompatible platform artifacts, but cannot distinguish every native launcher; publisher identity comes only from the signed manifest and exact artifact digest. Before that verification, the candidate's self-reported version runs with a fixed credential-free environment that does not inherit proxy URLs, custom CA paths, authentication material, or review metadata. After verification, the helper copies the candidate into a private, checksum-keyed executable snapshot. Every model attempt reuses only that snapshot for capability probes, post-provenance dependency inspection, credentials, review data, and the final Claude process.

The fixed-path native GPG source is a separately validated host-trust dependency, not evidence of Anthropic publisher provenance. The helper holds its stable file descriptor, copies the main executable into a fresh current-user `0700` home below an explicit helper-owned private `0700` root, publishes that copy as a `0500` execution snapshot, and uses the snapshot for all key conversion, fingerprint listing, and signature-verification calls. That main-file snapshot does not by itself freeze dynamic libraries: on macOS, sealed system endpoints terminate traversal as platform TCB while every non-sealed Homebrew branch is recursively captured and rechecked under the explicit `admin` boundary; the main executable must name exactly the standard `/usr/lib/dyld` loader and dependencies may not name another loader. Linux/WSL2 accepts only root-owned `/usr/bin/gpg{,2}` by default, proves that the loader-visible ELF `PT_DYNAMIC` table maps consistently through exactly one file-backed `PT_LOAD` at byte and loader-page granularity, rejects other page-rounded load mappings over the dynamic table, rejects `DT_RPATH`/`DT_RUNPATH` and audit-module tags, validates the architecture-specific canonical glibc loader, invokes that loader directly with `--list`, captures both loader-visible symlink chains and resolved root-owned endpoints, and rebuilds the exact closure through the same loader before every call. The host-GPG boundary never executes an implementation-variable `ldd` script. Security-sensitive host tools receive fixed minimal environments rather than inherited loader, shell, or compiler controls. Positive WSL2 classification requires an explicit WSL2 kernel identity marker and bounded `/proc/self/mountinfo` provenance checks. `/run/WSL`, `WSL_INTEROP`, distro environment, and binfmt evidence prove only WSL presence because those surfaces also exist under WSL1. A custom kernel without a recognizable WSL2 marker is unsupported whenever any WSL-presence signal remains. If every such signal is also removed, the guest is observationally indistinguishable from native Linux and follows the Linux classification; the common Linux/WSL mount guard still rejects positive DrvFS/Windows provenance through custom automounts, bind mounts, or aliases. Positively identified WSL2 also keeps the fast literal `/mnt/<drive>` rejection.

Linux and WSL2 use a separate inner file-tool boundary because the trusted Claude runtime must read either its staged `/auth/config` credential or an API key while model-invoked tools must not. The helper uses `dontAsk`, exposes only `Read`, allows only `Read(./**)`, rejects file-mention syntax in the review prompt, and denies every non-workspace synthetic-root mount with absolute double-slash rules such as `Read(//auth/**)` and `Read(//proc/**)`. The dedicated writable `/auth` carrier root lets the verified runtime create both its primary lock inside `/auth/config` and its legacy sibling `/auth/config.lock`; no host credential is mounted there. Command construction fails closed if a future mount lacks deny coverage or is added below `/workspace`. The outer `bubblewrap` sandbox still enforces host filesystem, process, write, and network isolation; the staged credential carrier is not a confidentiality boundary from the Claude process that authenticates with it.

Low-level helper Claude authentication is validated without an OAuth warmup or a future-attempt lifetime reservation. An expired access token alone is not login expiry. On macOS, the helper safely reads both the documented Keychain item and the empirically compatible credential file under the current account's `pwd` home, selects the structurally valid credential with the later expiry, and exposes either source only through the same capability-authenticated temporary broker. When both carriers contain the same refresh token, they are treated as one logical login even when their access tokens or expiry metadata differ. Every `W` generation admitted to durable staging atomically reserves space before filesystem work in an attempt-scoped journal bounded to eight generations and 8 MiB of payload. The last generation and 1 MiB are reserved for terminal recovery, and every reservation remains consumed for the attempt even when later write or publication fails. Each admitted generation commits to a distinct helper-private recovery carrier whose credential, directories, synchronized rename, and exact readback are verified. If rename completed before a sync or identity failure, only exact acknowledged-path and payload-digest re-proof may retain that carrier as recovery-only evidence. Ordinary still-current generations are published and acknowledged only at the broker's locked linearization point. The current update that first enters the terminal reserve atomically closes later `W` admission, commits its exact payload into the final slot, and returns a NACK without publication or host writeback; superseded contenders consume no quota or filesystem work, and later writes receive explicit NACKs before callbacks. The terminal generation invalidates older staged host-writeback state, marks credential inspection inconclusive, and pauses without Copilot fallback. Superseded and failed carriers remain within the bounded journal for the attempt rather than being deleted by a later publication. Acknowledgement proves durable recovery staging, not host persistence, so helper `SIGKILL`, fatal failure, or power loss after success still leaves the carrier. On shutdown, the server first irreversibly closes its pending-update publication/ACK gate, then boundedly drains handlers that already entered the commit critical section. Runtime abandonment snapshots and pending detachment are tracked independently, so detachment can be retried without reopening publication or repeating a successful snapshot. A terminal durable stage racing the abandonment request self-registers as the quiescence stage under the runtime lock. Recovery executes even when the detached pending payload is `None` and still converges that stage. The first bounded runtime abandonment snapshot failure immediately pre-reports the stable macOS recovery-root cleanup scope; if both snapshots fail, the helper pauses with the snapshot failure rather than misclassifying it as recovery timeout. An unresolved terminal stage suppresses any older current-carrier claim and reports only that stable root. Whenever abandonment recovery observes any durable journal entry or quiescence stage, the final error also reports that recovery root, so old generations, a failed quiescence stage, and any fallback retain visible ownership even when an exact current carrier is separately reported. If bounded recovery itself times out, the handler/recovery state machine remains the sole owner of in-flight stage cleanup. A late stage or fallback is exact-cleaned by that state machine or remains within the pre-reported recovery-root cleanup scope; it cannot silently become an unreported current credential. Recovery metadata names a current carrier only after exact payload verification; any unverified or incomplete file is cleanup-only. Uncertain shutdown without a `W` generation reports no current recovery credential; cleanup-root ownership remains separate. After the broker server and every handler quiesce, one outer-runtime owner detaches and guarded-writes only the latest acknowledged credential, proves the newest durable carrier before removing older journal entries, and deletes the final recovery copy only after verified host commit. The latest exact-verified generation is always the canonical current carrier. When terminal recovery, a malformed update, or durable-stage failure leaves no staged host-writeback candidate, finalization reports that carrier as the sole current `authentication.recovery_artifact`, removes every other non-authoritative complete journal entry, and reports any failed stale-entry cleanup separately as `authentication.recovery_cleanup_artifact`. A staged or non-staged control-flow early stop that leaves any stale carrier unvisited promotes cleanup reporting to the recovery-root scope without changing the canonical current carrier. A file-first dual-carrier commit reads back an ambiguous Keychain result, retries one exact file-new/Keychain-old state once, and otherwise preserves the rotated file credential while pausing as inspection-inconclusive; it never rolls back to a refresh token that rotation may already have invalidated. If broker or handler quiescence cannot be proven, or required post-quiescence host writeback cannot be completed, the outer-runtime owner retains the newest verified durable carrier or durably materializes a newer pending structurally valid payload before reporting its path, then pauses without Copilot fallback. If recovery replacement or finalization fails before a verified commit and leaves an exact complete uncommitted update file, the helper reports that file in `authentication.recovery_artifact` as the current recovery credential. A later replacement may remove only bounded stale update files after the new credential has been fully fsynced and read-back-verified at `config/.credentials.json`; once verified, that file is `authentication.recovery_artifact` even if deleting an older update fails, so the helper reports the older exact temporary file separately as `authentication.recovery_cleanup_artifact` and never as the newest or current recovery credential. On Linux and WSL2, the helper stages a private copy of the validated host credential and a bounded watcher persists each stable rotation during the attempt, followed by a synchronous final drain before cleanup. If Claude exits after rotating the staged credential but before releasing its helper-owned locks, a normal supervisor return plus watcher join proves writer quiescence; only then may the helper preflight the exact empty `0700` staged primary and legacy lock directories, remove them in reverse acquisition order, and retry the final drain once. A timeout, process leak, unsafe lock, failed retry, or guarded host-write failure instead retains the private recovery carrier inside the review container, reports its path without credential contents, and pauses without Copilot fallback.

Low-level helper local-login refresh writeback is enabled only for exact signed artifacts in the helper's credential-lock protocol catalog. The initial catalog covers Claude Code `2.1.211` for Darwin arm64/x64 and Linux glibc/musl arm64/x64; WSL2 reuses the matching Linux artifact. Within the shared publisher-verified `>=2.1.211,<3.0.0` range, the low-level helper remains usable with an explicit API key or OAuth token, while an uncatalogued helper local-login artifact pauses as inspection-inconclusive before credentials or review data are exposed. For a catalogued artifact, the parent keeps Claude's primary `.oauth_refresh.lock` and legacy sibling lock fresh with the certified 5-second heartbeat, captures both macOS carriers' existence/digest plus the file identity, rechecks both under the locks, and performs guarded writeback only when the complete logical snapshot still matches. Linux/WSL2 applies the same certified lock protocol to its staged and host files. Concurrent supported Claude Code login/refresh writers therefore serialize with the commit window, observed host changes win, and refresh-token rotation is normally retained. A stale shared host lock is never deleted automatically; the helper pauses for controlled cleanup only after the operator confirms that no writer remains. The narrowly scoped staged-carrier recovery above does not apply to a host credential directory. This is not an atomic compare-and-swap guarantee against unrelated external writers that bypass both Claude locks.

For the low-level helper, missing, malformed, unsafe, refresh-token-less, `Login expired`, HTTP 401, or refresh failure is `blocked-authentication`: the helper pauses instead of falling back to Copilot, and tells the operator to run `claude auth login` for local login or unset/replace the winning explicit `ANTHROPIC_API_KEY` or `CLAUDE_CODE_OAUTH_TOKEN`, as applicable. Only stderr and structured primary error fields are failure-classification evidence; repository-controlled partial result text can never authorize a model or Copilot fallback. Lock contention, carrier races, I/O uncertainty, and an uncatalogued lock protocol are inspection-inconclusive and do not imply a bad login. The low-level helper's compatibility Copilot backend requires a separate explicit supplemental request and never satisfies named double review.

An incomplete or exact-readback-unverified macOS recovery temp is cleanup residue, not a recoverable credential. If it cannot be removed or safely inspected, the helper reports its exact path only as `authentication.recovery_cleanup_artifact`; only a fully verified complete uncommitted update or a verified committed replacement may be `authentication.recovery_artifact`. A pre-reported recovery-root cleanup scope is cleanup metadata and does not claim that a current recovery credential exists.

`synthetic-token-fixtures` selects exact authoring values from the review helper's fixed finite catalog. The helper catalog remains the only enforcement authority; skill templates contain placeholders only.

## Test

The helper requires Python 3.10 or later; CI pins the minimum supported runtime.

```bash
python3 -B -c 'import pathlib, sys; [compile(pathlib.Path(path).read_bytes(), path, "exec") for path in sys.argv[1:]]' skills/review-orchestration-playbook/scripts/isolated_review skills/review-orchestration-playbook/scripts/named_lane_guard skills/review-orchestration-playbook/scripts/review_runtime/*.py
python3 -B -m unittest discover -s skills/review-orchestration-playbook/tests
```
