---
id: 20260805-ca401f
title: Claude Numeric API Error Status Authentication
status: completed
created: 2026-08-05
updated: 2026-08-06
branch: codex/claude-api-status-auth
pr: 91
supersedes: []
superseded_by:
---

# Claude Numeric API Error Status Authentication

## Summary

- Accept a true integer `api_error_status` from `100` through `599` as bounded error metadata on Claude failure envelopes.
- Classify numeric `401` authentication evidence as `blocked-authentication` through the canonical direct lane while keeping successful envelopes with numeric status fail closed.
- Align only the clean-marker carrier lengths and fail-closed short-ref REST resolution outcome with the pinned Action while retaining the playbook's stricter lowercase, spacing, boundary, grammar, and independent-receipt rules.
- Preserve strictly older PR #91 native-finding and unresolved-short-clean records as closed audit-only legacy evidence so the newer fully receipted current-head clean can exercise the already approved result-authority policy.
- Keep the generated private CI fixture responsible for running the canonical project-journal suite as well as the review-orchestration suite.

## Current State

- The provider compatibility classifier accepts exact login text plus numeric `401` only when the failure envelope and requested-model `modelUsage` binding are otherwise verified; `_record_attempt` records `auth` with `structured-authentication`. Any other bounded numeric status conflicts with that authentication fallback and remains inconclusive unless its own recognized category controls.
- The canonical stream validator maps bounded integer status metadata into the existing strict failure classifier. Numeric `401` produces `terminal.authentication-error`; other bounded statuses remain classified evidence, while booleans, non-integer numbers, objects, and values outside `100..599` are malformed.
- Success still accepts only absent, `null`, or whitespace-only `api_error_status`; a numeric status contributes `terminal.success-with-error` and yields `inconclusive`.
- The aggregate runtime profile and its validator mirror use `null_or_whitespace_string_or_http_status_integer`. The audited Claude Code 2.1.212 baseline remains unchanged.
- Clean GitHub Codex issue comments retain a full `parsed_commit`. A 40-hex marker must already equal the current head; a raw lowercase 10-hex live-provider marker remains `clean-pending-resolution` and non-authoritative until the exact companion joins its artifact ID, scope, ref, and full current head through independent initial and final exact-repository commit receipts. Current, complete-history, and sidecar-blind historical paths use the same join; sidecar-blind may ignore request sidecars but never the resolution companion. The receipt dates prove `artifact GET <= initial resolution <= post-scope snapshot(s) <= final resolution`, with same-second equality allowed. Findings URLs, native review commit IDs, and inline comment commit IDs remain full-SHA-only.
- GitHub Compare scope receipts now match the real REST response schema. Pull detail supplies full base/head OIDs, the exact derived Compare request URL binds that pair, and the Compare body repeats base and supplies the unique merge base. The parser does not require the nonexistent `head_commit` field, ignores any unknown field with that name, and never derives head from a possibly paginated or empty `commits` array.
- Legacy migration recognizes exactly two raw-internal audit carriers; an ordinary unreceipted current-grammar clean or finding cannot enter. `legacy-finding-native-review-v1` reports role `finding` only for exact provider identity, `COMMENTED` or `CHANGES_REQUESTED`, the closed `### 💡 Codex Review` layout, one same-repository full-SHA blob URL equal to native `commit_id`, the fixed `P0/red`, `P1/orange`, `P2/yellow`, or `P3/lightgrey` badge, and bounded title/prose containing neither `www.` nor a URI-scheme prefix whose colon is immediately followed by a non-whitespace character. After newline normalization, every physical disclosure line is trimmed and blank lines are dropped; the remaining lines exactly equal the closed nine-line disclosure. It is separated by either no padding line or exactly one line of four ASCII spaces, with no other title/prose trailing whitespace or blank line before it, and the review has no associated inline child. This grammar is enabled only by the raw migration classifier and never by receipt-bound current/provider authority. An exact older short clean remains role `clean-pending-resolution` with its raw lowercase 10-hex ref. A non-current prefix requires history-top-level `initial_legacy_short_commit_resolution_receipts` / `final_legacy_short_commit_resolution_receipts`, whose exact seven-field entries completely cover the raw-derived pending-prefix set, are unique and `raw_prefix`-sorted, enumerate exactly one prefix-matching full object, record exact-object `git cat-file -t` success plus `object_type == "commit"`, prove ancestry with return code `0`, and remain type-preservingly identical; an ancestry array cannot self-attest the mapping. An annotated tag that peels to a commit is still a tag and fails closed. A terminal report retains both arrays under `evidence_basis.current_raw_authority.local_git_prefix_resolution_receipts`; a reaction report uses `evidence_basis.current.local_git_prefix_resolution_receipts`, but never promotes the local applicability proof into REST resolution or a full audit-item commit.
- Each initial/final legacy-prefix phase is produced only by the independently trusted bundle's manifest-bound default-profile `named_lane_guard legacy-short-prefix-receipts`. Its exact source, distinct phase-unique absent owner-private temporary path, full head, phase, and complete repeatable prefix set yield only the closed `named-lane-legacy-short-prefix-receipts-v1` success envelope with cleanup complete and sorted seven-field receipts. The producer applies materializer-grade source marker/admin/common/object identity and access-policy checks, rejects alternate/HTTP-alternate, common/admin and per-worktree shallow, promisor/partial-clone, bitmap, unsafe config, and incomplete-object state, and isolates `.git/info/grafts`, ambient config, source refs/hooks, replace refs, lazy fetch, prompting, and commit-graph/multi-pack-index consumption. Against that view, phase-level `git cat-file -t <head>` and `git rev-list --objects --missing=error --quiet <head> --` controls prove the exact head commit and complete reachable object closure, including for an empty prefix set; they do not count among the exactly three disambiguation, exact-object type, and ancestry receipt queries per prefix. Direct imports, private workspace helpers, ad hoc wrappers, and source Git-directory queries never count. Semantic failure is `inconclusive` without partial receipts; source/view/control/process/revalidation/cleanup uncertainty is `blocked-safety`, also without partial receipts. A self-policy migration cannot use candidate-head Python: if the prior trusted bundle lacks the producer, formal review and adjudication use prior policy until merge/release, then activate the released manifest-bound guard.
- An output-limit exception does not identify whether stdout or stderr overflowed, so it is safety uncertainty rather than proof that a prefix matched multiple objects. The producer returns `blocked-safety` in that case; only complete parsed zero/multiple-OID stdout is the semantic `legacy-prefix-not-unique` result. Git subprocesses share the phase deadline and bounded output/process supervision, while synchronous filesystem revalidation remains fail-closed but is not claimed as an interruptible wall-clock guarantee.
- Those legacy items are tolerated only when strictly older than both selected-artifact pre-scope `Date` receipts and stable across complete initial/final raw inventories. They never become provider carriers, candidate bases, or superseding evidence. A selected current-head 10-hex clean still needs its own version-1 scope receipt and dual exact-repository REST full-SHA resolution receipts and never enters the local legacy-prefix arrays. Multiple old refs are supported; after the next substantive PR #91 head, the expected raw-derived ancestor set includes both `9abfd559e9` and `06264ac0a0`. Unresolved threads, truly malformed/unknown legacy records, bad ancestry, equal/newer contrary evidence, missing current receipts, incomplete pagination, or scope/lifecycle/final-read drift still blocks.
- Old request sidecars are not reconstructed. Their absence may yield `request_policy: unknown`, which forbids another same-head POST and disables reaction-only authority, but that producer/audit field alone does not null an independently complete current provider clean.

## Decision Rationale

- HTTP status is bounded transport metadata rather than a free-form error payload, but its value still constrains classification: only `401` may corroborate the exact authentication result, while a different numeric status cannot be ignored in favor of stderr authentication prose.
- Exact `type(value) is int` checks exclude JSON booleans even though Python booleans are integer subclasses; inclusive `100..599` bounds preserve the HTTP status domain and reject unreviewed numeric shapes.
- Numeric metadata is admitted only into failure classification. Success retains its stronger empty-error invariant, preventing a contradictory success envelope from supplying findings.
- The 10-hex clean-marker rule is not an optimistic prefix comparison. The fixed `JoeyTeng/codex-review-gate@16366aa81270ad2c875d2ceb8ce194f5b2308af6` and released `JoeyTeng/codex-review-gate-action@2a7f9d8cd98f90cb56dc1540bf54d9dc7484afc6` baselines establish only the 10/40 carrier lengths and the fail-closed repository-commit resolution outcome used here. The playbook intentionally remains stricter on lowercase-only refs, exact marker spacing, the exact-two-LF/nonblank boundary, closed lead/tagline/disclosure/native grammar, and independent parent-recorded initial/final raw receipt evidence; it does not claim complete grammar parity with the Action. Those raw receipt fields, digests, joins, and temporal comparisons are contract evidence, while a prose assertion that the parent performed the calls is not.
- The private CI fixture is canonical source, so project-journal validation belongs there rather than as a private-only workflow edit that the next overlay sync would erase.
- A live PR #91 pre-request probe proved that GitHub's Compare response has `base_commit` and `merge_base_commit` but no `head_commit`. The minimal receipt projection therefore binds head through pull detail plus the exact Compare URL. The pinned Action's exact shared `src/gate.mjs` blob `e0b974b27ebd64e412eaef1d069789b5f6bd76ba` likewise does not read `head_commit`; its separate ancestry helper validates a complete status/count/pagination/identical-range contract. Copying only its conditional final-commit check would be unsound, so the playbook keeps its own smaller closed scope-receipt extension.
- The legacy correction preserves the same pinned Action evidence-authority boundary. It does not treat arbitrary prose or a `P2` substring as a finding, and it does not turn an old short marker into clean. It merely prevents two strictly older recognized audit records from controlling ordinary precedence after a newer independently complete result exists. Only newer/equal malformed evidence controls ordinary terminal selection; a truly malformed older near-miss still fails the closed migration partition. This keeps “result present means pass” aligned with the named fixed-Action regressions without weakening the playbook's stricter carrier, thread, scope, lifecycle, pagination, ancestry, or final-stability gates.
- A fresh named-single review showed that the prior wording specified sanitized-view receipts but exposed no manifest-bound callable producer; the only concrete implementation pointer was a private workspace helper that did not establish the named-lane guard's source-repository hostile-state boundary. The correction deliberately makes receipt production a public success-only guard operation, not a documentation recipe for reconstructing the same commands. This prevents direct imports or source Git queries from silently becoming evidence, binds cleanup before success, and keeps semantic rejection distinct from safety uncertainty. It also preserves the self-policy anti-circularity rule: candidate-head code can be unit-tested, but it cannot produce control evidence for its own formal review; activation waits for a prior-policy-reviewed merged release.
- The badge/color grammar has separate exact-provider REST provenance: `P0/red` from `openai/openai-cookbook#2915` inline `3707792981`/`3707792985`; `P1/orange` from `ykylee/Devhub_example#592` inline `3409533388`/`3409533389` (also `andreame-code/netrisk#154` `3140609225`); `P2/yellow` from PR #91 review `4863163875`; and `P3/lightgrey` from `Joey-Tools/codex-debug-triage#5` inline `3676232685`/`3676232690`. Each observed record had exact `chatgpt-codex-connector[bot]` / `Bot` identity. The pinned Action does not parse this badge mapping, so it remains an independent provider-payload baseline rather than claimed Action parity.

- A private workspace helper never supplies receipt evidence. A direct import never satisfies this contract. Neither counts as receipt authority. The producer does not materialize or snapshot the entire object store. Source container identity/access policy and full-OID/type/ancestry ordered point-query semantics are protected; continuous stability of selected loose-object or pack bytes is not. Same-current-UID concurrent object-store content mutation, prefix-inventory churn, and intra-phase or inter-phase ABA are not excluded. Initial/final equality is two point-in-time observations, not atomicity.

## Review Finding Provenance

- A named single review of `Joey-Tools/codex-private-workflows` PR 146 reported that generated overlay `providers.py` downgraded exact login text plus numeric `401` to `unverified-auth-failure-envelope`.
- Canonical-source adjudication assigned the fix to `Joey-Tools/codex-review-workflows` and identified the direct stream validator as a second independent rejection point; fixing only the provider compatibility layer would not have changed the complete named Claude lane outcome.
- GitHub Codex review of canonical PR 91 identified the converse conflict: a non-401 numeric status could be ignored when stderr supplied authentication prose. The provider compatibility layer now rejects that combination while retaining numeric `401` and recognized transient-status behavior.
- A fresh whole-range named single review identified that `docs/PROJECT_STATE.md` still pointed to the older 2026-08-03 workstream. The recovery pointer and its contract assertion now select this completed workstream.
- Live revalidation of PR 91 produced exact-provider issue comment `5191165370` for head `9abfd559e955503fdd3233ecd16073918423fc7a` with the provider's 10-hex marker `9abfd559e9` and whitespace-varied official disclosure. The then-current playbook classified the live carrier as malformed even though the pinned Action resolved it to the exact full head. This observable anti-drift failure triggered the narrow carrier/resolution compatibility correction. The playbook's closed disclosure handling remains an independently reviewed local rule, and no complete grammar parity or widening of finding/review-native grammar is claimed.
- GitHub Codex review of private overlay PR 146 identified that generated CI omitted the project-journal suite. The canonical private CI fixture now owns that check so resync cannot reintroduce the omission.
- The final PR #91 pre-request scope probe exposed the synthetic `compare.head_commit.sha` assumption before any `@codex review` request was posted for head `f481ee35cbdbb3f54eb9433a1a610b7048c8df97`. Request, artifact, and history receipt parsers plus their fixtures and documentation were corrected together; anti-drift assertions now require all authority mirrors to state the pull-body / exact-URL / Compare-body division.
- Final PR #91 live validation then exposed the pre-v1 migration mismatch. Exact-provider review `4863163875` at `832edd60e50392c7f000d67ed856d20959c0d5b2` used the old native URL/P2-badge/prose finding form; issue comment `5191165370` carried unresolved old marker `9abfd559e9` for ancestor `9abfd559e955503fdd3233ecd16073918423fc7a`; and issue comment `5195502331` carried current marker `06264ac0a0` for `06264ac0a06240634f896e606b5744f747dd825f` with the complete current scope and dual-resolution evidence. The first two now remain complete audit-only history, while the third supplies the provider clean. The intended consumer outcome is passed with `provider_profile: terminal-payload`; unrecoverable old request sidecars may independently leave `request_policy: unknown` and do not authorize another POST.
- No production live evaluator CLI exists in this repository. The PR #91 adjudication therefore remained manual and receipt-backed; the executable contract now locks these exact three live records plus near-miss, thread, ancestry, receipt, timing, pagination, and drift failures so a future evaluator cannot silently restore the old inconclusive outcome.
- A final whole-range named single review found that `git cat-file -e '<sha>^{commit}'` and `merge-base --is-ancestor` both accept an annotated tag that peels to a commit. The recovery receipt now records exact `git cat-file -t` output as `object_type == "commit"` and rejects the annotated-tag near-miss. Follow-up hostile-repository audits showed that replacement refs and `.git/info/grafts` can falsify type or ancestry when the source Git directory is queried directly, and that borrowing its object store still consumes a hostile multi-pack-index unless explicitly disabled. That finding established the isolated-view requirement; the public manifest-bound producer below now owns its validated construction and forces both `core.commitGraph=false` and `core.multiPackIndex=false`.
- The next fresh named-single review found that this sanitized-view rule still lacked a manifest-bound public producer and that the private workspace implementation did not close source alternate, HTTP-alternate, promisor, common/admin shallow, or per-worktree shallow state. The formal producer is now `named_lane_guard legacy-short-prefix-receipts`; it owns those source checks, phase-unique temporary custody, the phase-level exact-head and reachable-closure controls, the exactly three bounded read-only receipt queries per prefix, closed success schema, cleanup-before-success, and no-partial-receipt failures. The private helper and direct source-directory queries are explicitly non-authoritative.
- Independent implementation/test audits found that the first producer draft converted every bounded-capture overflow into semantic prefix ambiguity even though the shared exception did not identify the overflowing stream, and that its documentation overstated the subprocess deadline as a strict wall-clock bound over synchronous filesystem checks. The final contract keeps unclassified overflow as `blocked-safety` and explicitly limits the deadline claim to supervised Git subprocesses.

## Next Steps

- Downstream private-overlay sync and release consume the canonical squash merge.

## Evidence

- Final legacy provider-recovery documentation and terminal-grammar contracts: 3 tests passed, including the post-evidence journal rerun. Isolated-PyYAML skill validation, project-journal validation, and `git diff --check` passed.
- Focused provider regression: 2 tests passed in 0.986 seconds.
- Complete stream-validator suite: 109 tests passed in 6.584 seconds.
- Focused stream schema/authentication/bounds regression, including float, object, and blank-string cases: 3 tests passed in 0.329 seconds.
- Canonical documentation contract anchor: 1 test passed in 0.008 seconds.
- Floating aggregate schema contract case: 1 test passed in 0.003 seconds.
- PR 91 non-401 conflict regression, canonical documentation anchor, and unchanged direct-stream numeric-status regression passed.
- Sanitized legacy-prefix regressions covering exact object type, local replace refs, `.git/info/grafts`, stale commit-graph data, and a corrupt source multi-pack-index: 5 tests passed in 1.748 seconds.
- Complete review-orchestration suite on the final legacy-recovery, Compare-schema, short-reference history, manifest-bound legacy-prefix producer, and receipt-ordering state outside the restricted sandbox with `/opt/homebrew/bin/python3.13` 3.13.12: 2,868 tests passed in 1,472.643 seconds; skipped=6.
- Complete named-lane suite on the final producer state: 189 tests passed in 144.111 seconds. The focused unclassified-output-overflow safety regression passed after the final classification correction.
- Complete contract suite on the final journal snapshot with Python 3.13.12: 111 tests passed in 136.687 seconds. The earlier corrected-state run passed the same 111 tests in 124.258 seconds; focused producer-contract and unclassified-output-overflow regressions also passed.
- The restricted sandbox run reached 1,218 passing tests and 7 skips before `test_claude_identity_socket_requires_same_canonical_socket` failed at setup because Unix-socket `bind(2)` returned `EPERM`; it never entered the tested logic. The exact full command passed outside that sandbox as recorded above. The relocatable uv Python 3.14.2 test copy's missing relative `libpython3.14.dylib` was likewise an environment-only diagnostic covered by the final host Python 3.13.12 validation.
- Two independent read-only producer audits confirmed that unclassified output overflow is `blocked-safety`, the deadline claim is limited to supervised Git subprocesses, both reported P2 findings are resolved, and no P1/P2 remains.
- Ruff lint/format, aggregate-schema JSON parsing, skill validation, project-journal validation, and `git diff --check` passed.
