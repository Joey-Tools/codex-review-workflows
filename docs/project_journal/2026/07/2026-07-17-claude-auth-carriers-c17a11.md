---
id: 20260717-c17a11
title: Trust Claude Authentication Carriers
status: completed
created: 2026-07-17
updated: 2026-07-18
branch: codex/claude-auth-carriers
pr: https://github.com/Joey-Tools/codex-review-workflows/pull/58
supersedes:
  - 20260703-b4e9d1
  - 20260715-7c1501
  - 20260716-662f2c
superseded_by:
---

# Trust Claude Authentication Carriers

## Summary

- Claude authentication now trusts the publisher-verified current CLI's credential-store coordination and refresh behavior instead of enforcing a helper-defined OAuth access-token freshness window.
- The supported signed-manifest and capability range starts at `2.1.211` and remains bounded below `3.0.0`; API-key reviews keep that range, while local-login writeback is enabled only for exact signed artifacts whose refresh-lock protocol has been audited.

## Current State

- Provenance, capability, outer-sandbox, file-tool, escaping-symlink, sensitive-content, bounded-output, effective-model, and terminal-artifact gates remain mandatory before a result can count as review evidence.
- The helper performs no fixed-input authentication warmup and does not require an access token to cover a 30-minute attempt. Access-token expiry alone is not login expiry when a usable refresh token remains.
- On macOS, the parent safely reads the current-account Keychain item and the credential file under the account home returned by `pwd`; it never trusts caller-controlled `HOME`. The file is an empirically verified compatibility source for current Claude Code releases, not an officially guaranteed macOS storage location. The structurally valid candidate with the later access-token expiry is loaded into the restricted temporary broker regardless of whether that access token has already expired. Carriers with the same refresh-token fingerprint are one logical login. Broker handlers validate rotations and order them by generation. Before any filesystem write, every structurally valid `W` generation atomically reserves attempt-scoped durable-journal quota. The journal is bounded to eight generations and 8 MiB of payload, and each reservation consumes generation and byte quota for the rest of the attempt even if the write or publication later fails. The generation then commits to its own private recovery carrier with file/directory synchronization, a synchronized carrier rename, and exact readback; only afterward, at the broker's locked generation linearization point, is a still-current, non-abandoned generation published and acknowledged. Reaching either journal cap NACKs the generation, invalidates any older staged host-writeback candidate, marks credential inspection inconclusive, and pauses without Copilot fallback. Superseded and failed carriers remain in the bounded journal until post-quiescence finalization or failure recovery. Once the server and every handler have quiesced, one outer-runtime owner proves the newest carrier before removing older journal entries, detaches only the latest acknowledged rotation, acquires the certified locks, rechecks the full carrier baseline, and performs guarded writeback. A control-flow interruption during older-generation cleanup stops before host writeback or latest-carrier cleanup. The final recovery carrier is deleted only after verified host commit and synchronized no-follow cleanup. If a file-first dual write gets an ambiguous Keychain result, the parent reads back both carriers, accepts an already-complete commit, retries one exact partial state once, and otherwise preserves the rotated file credential while pausing as inspection-inconclusive rather than rolling back to a potentially invalid old refresh token.
- The macOS Keychain broker explicitly tracks every handler, closes active sockets, and bounds serve-loop, handler, and recovery shutdown. Unquiescent shutdown first irreversibly closes the server-owned pending-update publication/ACK gate, then boundedly drains handlers already inside the commit critical section. Runtime abandonment snapshots and pending detachment are independent recorded states, so detachment may be retried without reopening publication or repeating a successful snapshot. A terminal durable stage racing the abandonment request self-registers as the quiescence stage under the runtime lock. Recovery runs even when detachment returns `None` and must converge that stage. The first bounded snapshot failure immediately pre-reports the stable macOS recovery-root cleanup scope; if both snapshots fail, the lane pauses with the snapshot failure instead of misreporting recovery timeout. Whenever abandonment recovery observes any durable journal entry or quiescence stage, its final error also reports that recovery root, covering old generations, a failed quiescence stage, and any fallback even beside an exact current carrier. After an actual bounded recovery timeout, the handler/recovery state machine uniquely owns late stage/fallback cleanup; it exact-cleans the artifact or leaves it within the pre-reported root scope. A late handler cannot acknowledge, mutate host state, or overwrite the authoritative recovery candidate.
- The latest exact-payload-verified generation is always the canonical current carrier. When quota rejection, a malformed update, or durable-stage failure leaves no staged host-writeback candidate, post-quiescence finalization reports that carrier as the sole current `recovery_artifact`, removes all other non-authoritative complete journal entries, and reports stale cleanup failure separately as `recovery_cleanup_artifact`. If staged or non-staged `ForwardedSignal`, `KeyboardInterrupt`, or `SystemExit` control flow leaves any stale carrier unvisited, cleanup reporting is promoted to the recovery-root scope while the canonical current carrier remains unchanged. Broker, snapshot, persistence, and cleanup uncertainty pause the lane and never authorize Copilot fallback.
- On Linux and WSL2, the current-user exact-mode-`0600` host credential is copied into a helper-owned writable `/auth` carrier with config at `/auth/config`. The original host file is never mounted and `/auth` remains denied to model-visible file tools. A bounded watcher persists multiple stable rotations during the attempt; after an ordinary stop, a synchronous final drain runs before cleanup. Stop closes new background-writeback admission under the same state lock used to admit a commit, so a candidate returning from a late blocking read cannot start host writeback after stop. If a normally reaped Claude process leaves helper-owned staged locks after rotating its token, the parent uses the supervisor's process-group-quiescence handoff plus the stopped watcher to preflight and remove only the exact empty private staged locks, then retries the final drain once. Timeout, process-leak, unsafe-lock, retry, or guarded host-write uncertainty instead retains the private recovery carrier under the review container and reports its path without credential contents. A watcher join timeout is inspection-inconclusive and retains the carrier immediately, without concurrent final drain, cleanup, or an unbounded wait. If stop observes an already-admitted background host writeback in flight, the error also reports that host state is ambiguous because the commit may still complete; it does not claim an exclusive recovery handoff.
- The restricted final runtime may refresh only inside its temporary carrier. For local login, the helper requires an exact version/platform/SHA-256 match in its signed-artifact lock catalog before reading host credentials. At each commit point, the parent acquires the audited CLI's primary `.oauth_refresh.lock` and legacy sibling lock, renews both leases every five seconds, rechecks the complete macOS Keychain-plus-file snapshot or the Linux/WSL2 file snapshot, and performs guarded writeback only while the observed host state still matches the current baseline. This serializes supported Claude Code login/refresh writers, normally preserves refresh-token rotation, and refuses observed concurrent changes, but is not an atomic compare-and-swap guarantee against unrelated external writers that bypass both locks. Stale shared locks are never reclaimed automatically and instead pause for controlled cleanup after confirming that no writer remains.
- Refresh-lock shutdown publishes the stop transition without waiting for heartbeat filesystem I/O, retries one transient heartbeat join or operation-quiescence timeout once, and acquires a bounded operation gate before touching lock paths or descriptors. Two timeouts, or an interruption after cleanup starts, permanently mark that lease cleanup-inconclusive: exact helper-owned paths remain visible through combined operation errors and forwarded signals, later release calls cannot silently delete them, and only controlled operator cleanup after writer-quiescence proof is allowed.
- Missing, malformed, unsafe, refresh-token-less, `Login expired`, HTTP 401, or refresh failure is terminal `blocked-authentication`. A Keychain client that disappears after initial capability validation is credential-inspection-inconclusive rather than runtime-unavailable, and a completed authentication rejection remains primary if final credential inspection also fails. The workflow pauses and instructs the operator to run `claude auth login` for local login, or unset/replace an explicit `ANTHROPIC_API_KEY`, as applicable; authentication and credential-inspection uncertainty never authorize Copilot fallback.
- Only stderr and structured primary errors are classified. Partial result text is repository-controlled and can never authorize authentication, model, or Copilot fallback, so a partial review cannot forge a backend switch.
- With existing `double-review` or `triple-review` consent, Copilot fallback remains limited to deterministic secure-runtime absence/unavailability or a strictly verified Claude model-entitlement path.
- The Keychain update transport-size limit is enforced only when the selected source or same-login synchronization will write Keychain; a distinct, unselected Keychain login remains validated and snapshotted but cannot block an independently usable file login on that transport-only limit.

## Next Steps

- None for this workstream.

## Evidence

- Policy and implementation contract: `skills/review-orchestration-playbook/SKILL.md`, `references/helper-contract.md`, and `references/claude-runtime-trust.md`.
- Repository contract coverage: `skills/review-orchestration-playbook/tests/test_contracts.py`.
- macOS Claude Code `2.1.211` carrier smoke: isolated `claude auth status --json` probes reported `loggedIn: true` with only a pwd-home credential file and, separately, with only the restricted `security` carrier.
- Python compile checks passed for the helper scripts and complete runtime tree.
- The complete helper suite passed after review fixes and integration with the latest `master`: 913 tests run, 4 skipped. The provider suite passed 368 tests with 3 skips, and focused Keychain broker tests also passed with real loopback access, covering blocked handler timeout/signal/recovery, latest pending-generation preservation, callback-time supersession, serve-loop failure propagation, shared recovery-candidate ownership, in-flight durable-stage abandonment, and unquiescent-handler recovery.
- Strict C syntax checks passed for the Linux launcher with its production POSIX feature macro and for the Keychain broker with its production clang flags.
- Fixed-range review and follow-up concurrency audits found and closed credential classification, deeply nested malformed-JSON handling, repository-controlled partial-result fallback, lock-coordination, heartbeat, whole-snapshot, exact-artifact, descriptor-cleanup, partial dual-write reconciliation, generation ordering, post-quiescence commit ownership, pre-acknowledgement crash durability, staged-lock crash recovery, stale shared-lock isolation, real-process-start ownership, forwarded-signal handoff, watcher-shutdown/process-exit, recovery-artifact cleanup, and non-regular credential-carrier blocking defects. Credential and cleanup paths open potentially attacker-replaceable files non-blockingly before validating their type, so a FIFO cannot stall authentication inspection or private-carrier cleanup. Credential inspection now has a distinct inconclusive path with no Copilot fallback, missing or actually rejected credentials remain blocking, and exact audited artifacts share a heartbeat-backed writeback commit lock. A macOS broker handler can acknowledge only a current generation already committed to a synchronized private carrier; one owner persists the latest rotation after broker quiescence, while unquiescent or failed persistence retains and reports the newest complete private recovery artifact. An unpersisted Linux/WSL2 rotation is either recovered after proven writer quiescence or retained the same way.
- Final broker review additionally closed pending-publication fail-closed ordering, bounded draining of entered commit sections, terminal-stage self-registration under the runtime lock, independent abandonment-snapshot and retryable-detach state, first-snapshot-failure root pre-reporting, `recover(None)` quiescence-stage convergence, and timeout cleanup ownership. Abandonment recovery now reports the root whenever any journal entry or quiescence stage exists, while canonical-current selection remains separate. The review also closed staged and non-staged control-flow root promotion across quota, malformed-update, and failed-stage finalization. The attempt journal still enforces its eight-generation and 8 MiB reservation caps before filesystem work without refunding failed reservations.
- Current-head GitHub review prompted a focused recovery-temp audit. Only a fully written and exact-readback-verified uncommitted temp may be reported as the current recovery credential; an unverified, incomplete, or uninspectable temp is cleanup-only, and successful unlink followed by directory-fsync uncertainty never reports a path that no longer exists. Focused failures cover temp fsync, metadata inspection, close, unlink, and cleanup-fsync behavior.
- A later fixed-range review found that validation, state-lock acquisition, and snapshot copying preceded the refreshed-credential recovery boundary. The boundary now begins before those operations; regressions cover all three control-flow exception families, the state-lock window, an existing earlier persistence error, and simultaneous recovery-candidate generation failure.
- `project_journal.py validate` passed for the repository journal set.
- The isolated `uv --with pyyaml` fallback reported `Skill is valid!` for `review-orchestration-playbook`; the preferred wrapper could not import local PyYAML.
