---
id: 20260719-crt001
title: Claude Refresh Transaction Lock
status: completed
created: 2026-07-19
updated: 2026-07-21
branch: codex/claude-refresh-transaction-lock
pr: https://github.com/Joey-Tools/codex-review-workflows/pull/67
supersedes: []
superseded_by:
---

# Claude Refresh Transaction Lock

## Summary

- Serialize each catalogued Claude local-login attempt before the first host credential read and through runtime refresh, quiescence, final drain, and verified host persistence on macOS, Linux, and WSL2.
- Retain shared lock directories as a fail-closed stale fence when rotation persistence or writer quiescence remains uncertain.
- Keep API-key mode independent of local-login carrier coordination.

## Current State

- The process supervisor exposes a post-cleanup quiescence callback only after the child is reaped and its supervised process group is absent.
- macOS carrier selection, broker execution, snapshot validation, and host persistence reuse one outer refresh-lock lease.
- Linux/WSL2 credential staging, watcher writeback, final drain, and cleanup reuse one borrowed-descriptor host lease while private staged-carrier locks remain separate.
- Linux/WSL2 release and retain-only cleanup re-read the lease terminal state after every interruptible release or abandonment result, preserve the first control-flow winner, and use the same deletion-prohibited non-destructive abandonment path when repeated entry interruptions require recovery.
- Abandoned leases publish an irreversible deletion-prohibition latch before interruptible diagnostic work, stop their heartbeat, close owned descriptors when quiescence permits, retain every shared lock directory that was created or may have been created, and cache a descriptor-bound recovery diagnostic that never points at lexical paths. A later public release can only resume non-destructive abandonment settlement and descriptor cleanup; it can never reopen directory deletion.
- Abandonment cleanup tracks `NOT_STARTED`, `RESUMABLE`, and `SETTLED` independently from its cached diagnostic. It consumes each descriptor at most once, settles close failures or reuse ambiguity as retained residue, and exposes one atomic snapshot that distinguishes lifecycle terminality from verified descriptor closure. A private publication slot remains bound to a distinct, never-promoted descriptor fallback through every retained state, including `SETTLED`, and becomes empty only after release is published. Linux/WSL2 reads that slot directly instead of retaining a mutable diagnostic alias or inferring completion from `fstat`; an already-frozen control-flow winner remains descriptor-only even after settlement.
- Abandonment and destructive release acquire the operation `RLock` through a lease-published five-state handoff that covers both the callee and caller result boundaries. Unresolved ownership survives the interrupted call and is reconciled before every later terminal-state return, including normal context exit after physical lock cleanup; only the original thread object may release it, another thread fails closed without replacing it, and terminal handoffs are cleared by identity. The public `released` flag reports only physical lock-directory and descriptor cleanup, while the atomic retention snapshot remains nonterminal until any published handoff is cleared. Abandonment records raw error chronology before classification, recovers the earliest control-flow from a bounded 16-node active context chain, immediately binds descriptor-only recovery evidence, and uses a method-level second selector so one later cleanup signal cannot replace that winner.
- The irreversible deletion-prohibition latch is published before `RESUMABLE` becomes visible. A release caller that already passed its pre-lock checks therefore cannot enter destructive cleanup after an asynchronous interruption between abandonment state stores.
- Refresh-lock heartbeat threads inherit the forwarded-signal mask so the main thread retains control-flow priority while it attaches recovery evidence.
- Linux/WSL2 refresh-lease acquisition, watcher startup, and deferred cleanup use caller-owned forwarded-signal mask handoffs. The previous mask is published before the helper return boundary, an interruption after the blocking syscall either rolls back inside acquisition or transfers restoration ownership to the caller, and cleanup still releases or abandons an already-acquired host lease before propagating the first control-flow error.
- Linux/WSL2 starts a dedicated cleanup coordinator while forwarded signals are blocked and before acquiring the host refresh lease. The coordinator inherits that mask, owns the explicit-context lease from acquisition through one linearized `NORMAL_RELEASE`, `RETAIN`, or `CANCEL` decision, and atomically publishes coordinator completion together with a separate cleanup-terminal proof bit and cached recovery diagnostic. Missing signal-mask ownership, watcher-start uncertainty, carrier-cleanup failure, repeated control-flow interruption, or exhausted terminal proof therefore retains the private carrier and shared locks instead of entering destructive cleanup or waiting forever. Pre-thread startup failure has a distinct terminal cancel path, while decision-wait interruptions are recoverable inside the masked worker.
- If cleanup signal masking fails after the Linux coordinator thread failed before entry or before acquiring a lease, unmasked cleanup now selects the coordinator's terminal `CANCEL` path instead of retrying an impossible `RETAIN` decision forever. Coordinators that own a lease make bounded attempts to prove terminal retention and return an inspection-inconclusive result when that proof remains unavailable.
- Linux/WSL2 bounds the outer unmasked fail-closed cleanup callback to eight attempts. Persistent callback failure keeps the staged credential carrier and descriptor-bound shared-lock evidence attached to an ordinary diagnostic, so cleanup pauses without hanging or deleting uncertain state.
- The coordinator-less unmasked fallback also bounds each watcher stop, quiescence check, source-anchor handoff, abandonment, release, and descriptor-settlement transition to two attempts. The masked coordinator uses one five-phase state machine (`CREATED`, `WORKER_ENTERED`, `STARTUP_READY`, `CLEANUP`, `TERMINAL`), accepts only a genuinely published cleanup decision after interrupted condition waits, keeps waiter-local control flow out of the shared worker chronology, and delivers the first worker control-flow object to at most one clean waiter.
- Lease publication, worker entry, synchronous cleanup claims, and publication-gate closure share one state lock. A late native worker and a `Thread.start()` failure therefore cannot run two cleanup owners; publication before gate closure is adopted into fail-closed retention, while publication after closure is rejected before heartbeat startup. One waiter takes over a dead nonterminal worker and performs only non-destructive cleanup. Snapshot and descriptor-settlement proof each have a fixed retry budget; a successful settlement is itself sufficient terminal proof, while exhaustion publishes coordinator `TERMINAL` with cleanup-terminal proof false and descriptor-bound recovery evidence. Callers then return or raise inspection-inconclusive without deleting paths, closing uncertain descriptors, reconciling a foreign operation handoff, or performing another terminal snapshot.
- Linux/WSL2 source retention becomes a sticky coordinator requirement as soon as either a stable lease or staged watcher is captured, so later interrupted `owner.lease` reads cannot falsely skip source convergence. The retained source anchor publishes one persistent `OPEN`, `TRANSFERRED`, `CLOSED`, or `DESCRIPTOR_RESIDUE` disposition before forgetting descriptors. An unknown close result is never retried by numeric descriptor, a stopped watcher cannot leave `TRANSFERRED` as terminal, and descriptor residue remains lifecycle-terminal but explicitly not verified closed.
- Coordinator retention bounds heartbeat-quiescence recovery to two abandon/release cycles. A heartbeat that remains live is settled as terminal descriptor-bound residue without closing descriptors or unlinking shared lock directories, so callers receive a deterministic fail-closed result instead of waiting forever.
- Watcher stop and coordinator source-anchor retention bound every start-state, stop-admission, stop-event, deadline, quiescence, and descriptor-handoff transition to two attempts. Exhausting one phase still advances the remaining fail-closed phases; an unproven handoff leaves the source descriptor coordinator-owned, the carrier retained, and a deterministic diagnostic instead of spinning the cleanup worker.
- Source-anchor terminal proof now gives both watcher handoff and residue settlement independent two-attempt budgets. Exhausted handoff publishes a no-close latch before interruptible diagnostic or lock work, retains the original descriptors as process-lifetime residue, and makes descriptor reads, stability checks, and both cleanup roles reuse one descriptor-bound diagnostic without calling `close`; the first fallback observer and the settlement thread cannot replace each other's published identity. Terminalizer failures before publication exit after the fixed budget, and a failed dead-worker takeover releases its synchronous claim so a later waiter can retry instead of spinning on poisoned state.
- The Linux/WSL2 staged-credential watcher publishes a local `UNKNOWN` start state before the interruptible native `Thread.start()` boundary and promotes it to `CONFIRMED` only after startup returns or `_started` is observed. Stop admission closes before one sticky deadline is published; UNKNOWN confirmation and join share that remaining budget. A timeout, confirmation error, join error, or unreadable start handoff leaves quiescence unproven, hands the source anchor to the late watcher, retains the private carrier, and abandons the host lease with descriptor-bound evidence. A transient false `is_alive()` result before `_started` publication can never authorize cleanup.
- The common logged-process supervisor completes fail-fast command/environment validation and descriptor preparation before publishing caller-owned `UNKNOWN`, then uses a prestarted claim/cancel worker to retain the eventual `Popen` handle across every Python result boundary. POSIX launches first enter an isolated `-I -S` gate with an empty bootstrap environment; the target environment crosses the private control pipe as a bounded `CGR1` byte frame and cannot activate `LD_*`, `DYLD_*`, Python startup hooks, or target code before the single-byte `G` commit. `CONFIRMED` is published only after the status descriptor proves the target `exec`; pending signals or the shared operation deadline close the gate first, and an unclaimed worker owns eventual termination and reaping without reusing its inherited descriptor numbers. Constructor failure, pre-commit cancellation, or an interrupted claim therefore leaves Linux/WSL2 and macOS cleanup conservative without silently starting an authenticated reviewer.
- macOS refresh observation and verified host commit use matching monotonic write generations; a later incomplete `W` invalidates an earlier commit proof and retains the stale fence.
- A verified host commit is not the terminal proof: the final carrier snapshot records its own matching generation only after revalidation succeeds, so a changed snapshot, ordinary inspection error, or forwarded signal retains the stale fence even after writeback completed.
- macOS terminal settlement treats signal-mask acquisition failure as unsafe, prepublishes a caller-owned handoff before blocking forwarded signals, and commits mask ownership only after the previous mask is stored. Interruptions before that commit roll back once inside acquisition; interruptions after it leave pending-signal consumption and the single final restore to the outer handoff. The handoff blocks signals only across state inspection and unsafe-lock abandonment, binds pending-signal recovery and first-primary selection before restoring the caller mask, and never consumes a pending signal already owned by an outer mask; a safe terminal state still releases normally.
- A required terminal signal-mask handoff now fails closed when invoked outside the main thread or when the platform cannot return a real previous mask. Worker-thread macOS settlement abandons the refresh transaction with descriptor-bound recovery instead of publishing `None` as owned mask state and releasing an unprotected shared lock.
- The outer macOS local-login lease is retain-only through its public API and exposes no releasable callback or mutable authorization. Its coordination body exits normally only after terminal settlement proves process quiescence, persistence, and the final carrier generation; that normal exit synchronously removes the locks before the terminal signal mask is restored, while every exceptional exit retains them. A safe reviewer error is deferred until after the normal-exit release and then propagated during signal handoff completion.
- Every direct refresh-lock acquisition publishes an empty cold lease to a mandatory caller-owned handoff object before acquiring either shared lock, registers a pending acquisition before each `mkdir`, and adopts each inspected lock before it crosses an internal return boundary. The caller confirms transfer only after local assignment, so interruptions at raw `mkdir`/`open` results, internal resource assignments, the callee return, or the caller `STORE_FAST` boundary retain a descriptor-bound recovery fence or safely release a fully owned unexposed lease on macOS, Linux, and WSL2. Ambiguous non-`FileExistsError` `mkdir` failures remain pending and fail closed instead of assuming that no directory was created.
- If control flow interrupts abandonment-diagnostic publication, a preconstructed descriptor-bound fallback remains available while bounded shutdown and descriptor settlement continue; the winning error therefore reports descriptor-bound recovery without silently leaving a stale fence or attributing a retargeted lexical path to the retained directory.
- A final carrier proof records both proof presence and its exact write generation. Generation zero is valid only after an explicit snapshot proof, and a started reviewer cannot release the outer lease after a failed or interrupted zero-update revalidation.
- Every auxiliary-thread and carrier-coordination signal-mask crossing prepublishes a caller-owned mask owner, queries the caller mask before applying forwarded-signal blocking, and restores exactly once after the complete acquisition/start/quiescence lifecycle. Thread startup atomically publishes one immutable caller-owned `NOT_STARTED`, `UNKNOWN`, or `CONFIRMED` outcome snapshot with its selected error; all eight production callers execute cancel, close, quiescence, unlink, scrub, or retention through safety `finally` paths before merging handoff and cleanup errors. Post-start control flow therefore cannot be hidden by a cached clean result or a caller `CALL`-to-assignment interruption, and control-flow winners remain acyclic on Python 3.10 and newer; validation control flow retains a generic persistence-failure diagnostic without copying unverified recovery paths.
- Common process execution and Claude carrier coordination retain caller-owned signal-mask ownership until restoration succeeds. Both acquisition result boundaries are covered, transient restore failures receive one bounded retry without clearing ownership early, and Linux/macOS quiescence merges restoration failures behind the first control-flow winner instead of replacing it.
- Python opcode safety probes normalize the owned and borrowed fast-local load forms introduced by Python 3.14 only where both forms preserve the asserted handoff semantics. Owned load-to-store boundaries remain strict and do not accept borrowed loads.
- Concurrency interruption regressions do not infer context-manager boundaries from CPython `with` opcodes. Instrumented release locks, heartbeat events, and prearmed retention-snapshot boundaries now exercise the same ownership and lifecycle transitions across supported Python runtimes.
- Broker and proxy serve threads start with cancellation prearmed, clear it only on successful admission, and release their serve gates through independent nested `finally` paths. An interruption at either the cancellation call entry or result therefore still wakes, quiesces, closes, unlinks, and safely scrubs or retains the associated credential state.
- A macOS broker teardown `BaseException` is converted into an unquiescent shutdown result at the credential-server boundary. The existing fail-closed path then abandons the runtime, detaches pending publication when possible, performs bounded recovery, marks handler quiescence unproven, and only then propagates the original control-flow error so the shared refresh locks cannot be released on an incomplete teardown.
- macOS NACKs a callback-entry superseded generation before validation, quota reservation, terminal admission, or filesystem work; a complete durable generation superseded only after admission remains journaled through handler quiescence, so a later truncated `W` cannot erase the sole recoverable rotation.
- A declared macOS cleanup residue keeps the outer refresh transaction abandoned when no-follow identity validation succeeds, is inconclusive, or is interrupted; control-flow errors are propagated only after the stale fence and recovery diagnostic are attached.
- If refresh-lock abandonment is itself interrupted while another control-flow error is already primary, the original primary identity wins while descriptor-bound lock recovery metadata is copied onto it before cleanup notes or an acyclic Python 3.10 fallback graph are attached; a detached secondary node preserves the cleanup signal and every independent cause/context branch without copying the edge back to the primary.
- Python 3.10 fallback-graph inspection visits at most 32 unique exception nodes; a deeper, wider, or cyclic graph fails closed to a detached diagnostic, while exact-boundary, shallow, and shared-DAG inputs terminate deterministically without losing safe identity.
- macOS starts the refresh-lock heartbeat under the forwarded-signal mask and revalidates the lease after carrier selection but before broker exposure.
- Linux/WSL2 revalidates the complete credential path anchor after lock acquisition and before the first credential read.
- Linux/WSL2 keeps the borrowed host refresh lease through payload scrubbing and verified staged-carrier cleanup; cleanup failure or interruption abandons the lease before propagating the primary error.
- Recovery diagnostics for an intentionally retained lock set never publish exact lock paths, even after a descriptor/no-follow identity proof and regardless of whether the original anchor owned a path or borrowed a descriptor, because an ancestor may be retargeted after proof and before the diagnostic is consumed. The isolated proof helper remains testable, but it is not a publication authority.
- Staged-carrier crash recovery establishes a caller-owned forwarded-signal mask before the first descriptor open, opens every absolute carrier component from the filesystem root with no-follow directory descriptors, and opens `config` only relative to the retained carrier descriptor. Ancestor symlinks and carrier/config retargeting fail closed before either helper-owned lock can be removed. A parent-close outcome that becomes unknown is never retried by numeric descriptor, blocks lock removal, and publishes only descriptor-bound recovery evidence; all other known-owned descriptors settle before the signal mask is restored.
- On runtimes without `BaseException.add_note`, cleanup diagnostics render the secondary error's bounded type/message detail in one new visible diagnostic ahead of the pre-existing primary chain without mutating or retaining the caller-owned secondary graph. Descriptor-bound evidence takes priority over stale path metadata, while shared tails, cycles, graph-budget exhaustion, or hidden recovery evidence produce explicit bounded fail-closed text instead of an exception cycle or an unsafe path. Common process cleanup, Linux cleanup, provenance-deadline cleanup, provider persistence, and detached provider error graphs copy an implicit context only when the source exception already exposed it; `raise ... from None` remains hidden through every fallback transformation.
- On runtimes without `BaseException.add_note`, direct refresh-lock recovery attachment also inserts one typed diagnostic ahead of the existing cause chain, and staged crash recovery inserts an unresolved signal-mask restoration diagnostic behind the first control-flow winner. Python 3.13 fixtures disable `add_note` explicitly so both Python 3.10 fallback contracts remain covered without requiring a second local interpreter.
- Path-bearing structured standard exceptions are sanitized through an effective visible root that every caller stores, returns, or raises. `SyntaxError` formatting fields are cleared, while `ExceptionGroup` and `BaseExceptionGroup` are rebuilt without raw children under a hard 32-node output budget; the last node becomes a typed truncation placeholder, and already-consumed control-flow children retain their identity and interruption semantics.
- A timed-out macOS recovery publishes its visible exception root through an `OPEN` to `SEALED` handoff guarded by one per-root reentrant lock. Seal performs the last permitted structured-root replacement, revalidates the actual retained proof under that lock, and binds the broad recovery-root cleanup scope whenever proof or cleanup remains incomplete. Selectors, terminal handoff, slot readers, and late direct merges all preserve the sealed identity; late control-flow, proof, and cleanup results are reduced to fixed bounded notes and counters without relinking raw exceptions, and any later proof loss restores the broad fail-closed scope.
- Every persistence, refresh-lock recovery, reporting, and terminal-handoff boundary now treats either sealed operand as an effective-root selection instead of a metadata-copy request. Sealed reports remove all concrete recovery-path claims and remain generic, while abandonment outcomes carry control-flow priority as data until callers have left active exception handlers; the delivered root therefore keeps its exact identity, proof revision, and empty cause/context graph under late recovery, signal-mask, and abandonment failures.

## Next Steps

- None.

## Evidence

- The pre-fix macOS and Linux tests failed because credential exposure happened before the host transaction lock was held.
- Event-driven concurrency regressions require the second reviewer to wait before reading host credentials and then observe the first reviewer's persisted rotation.
- Fixed-range and dirty-diff reviews found and closed write-generation, final-carrier revalidation, terminal signal-handoff abandonment and caller-return ownership, signal-mask publisher pre/post-commit rollback, caller-owned carrier/quiescence/thread-start mask crossings, immutable thread-start outcome publication, direct and context caller double-interruption cleanup, primary-aware proxy cleanup aggregation, prearmed serve cancellation and independent gate release, pre-handoff second-signal release and persistence propagation before saved-local assignment, deferred refresh-lock error classification, post-exit public-release reopening, default-mode double-publication release reopening, exceptional cleanup-entry interruption, interruptible callback revocation, thread-identity reuse, internal resource and acquisition-to-owner heartbeat handoff, raw `mkdir`/`open` acquisition-result ownership, acquisition/release/abandon caller entry and result prearming, ambiguous `mkdir` failure retention, Linux release/abandon terminal-state truth, retain-only cleanup bypass, pre-yield lease leakage, interrupted normal-exit cleanup, double-interrupted diagnostic publication, generation-zero proof presence, callback-entry supersession, truncated-write carrier retention, signal-mask inheritance and post-apply rollback, Linux lease/start/cleanup mask-return ownership, missing-mask destructive cleanup, coordinator terminal retention after repeated control-flow interruption, pre-start coordinator cancellation, recoverable decision-wait interruption, descriptor-bound carrier-cleanup diagnostics, unknown native-thread startup quiescence, validation-control-flow persistence reporting, post-lock path/lease revalidation, carrier-cleanup ordering, cleanup control-flow-priority, double-control-flow recovery propagation, bounded acyclic secondary-evidence rendering, cleanup-artifact three-state validation, Python 3.10 rendered recovery-diagnostic, post-proof ancestor retargeting, retained-path non-publication for both path-owned and borrowed-descriptor anchors, destructive partial-cleanup gaps, resumable abandonment continuation, at-most-once descriptor settlement, lifecycle-versus-physical terminal reporting, deletion-latch publication ordering, non-terminal snapshot path isolation, lifecycle-safe recovery publication, operation-lock callee/caller result ownership, persistent unresolved ownership, normal-exit pending-handoff reconciliation, Linux terminal-fast-path snapshot enforcement, unknown-owner cross-thread isolation, active-context chronology recovery, guard-unwind chronology, masked pending-signal retention, and first-winner preservation across lifecycle reads, recovery attachments, demotion aggregation, final selection, and abandonment caller boundaries.
- Fixed-range egress preflight rejected ad-hoc credential placeholders in the changed Linux test snapshot. The affected inputs and expectations now select the helper-owned `access-a`, `access-expired`, and `refresh-a` authoring fixtures from pool `joey-private-v3` according to each credential's lifecycle state.
- A fixed-range Codex review found that a native watcher thread could be launched before `Thread.start()` published `_started` or `ident`, allowing cleanup to misclassify the watcher as unstarted. Delayed-bootstrap regressions now cover timeout and confirmation-error retention, source-anchor descriptor ownership, descriptor-bound host-lock abandonment, late worker closure, and bounded UNKNOWN-to-CONFIRMED quiescence.
- A subsequent fixed-range Codex review found the equivalent process-launch gap between `Popen` returning and Python storing its result. Python 3.13 opcode injection now covers the `CALL_KW` to `STORE_DEREF process` boundary, while provider regressions prove that an incomplete start publication reaches both platform runtimes as started-but-unquiesced and a Linux integration regression verifies private-carrier retention.
- Current-head fixed-range reviews found a deterministic Linux spin when both coordinator startup and cleanup signal masking failed, plus a macOS broker teardown window where `ForwardedSignal` could bypass fail-closed recovery. Exact regressions now prove terminal no-lease cancellation on Linux and abandon/detach/recovery plus quiescence-unproven propagation on macOS.
- Subsequent current-head reviews found persistent callback, watcher/source-transition, and live-heartbeat retry loops plus Python 3.14 borrowed-fast-local opcode drift. Exact regressions now prove bounded fail-closed carrier retention, coordinator-owned source-descriptor residue, terminal descriptor-bound lock residue with no destructive cleanup, and semantic opcode normalization at only the affected handoff probes.
- A current-head GitHub Codex review found two remaining tests that depended on legacy `BEFORE_WITH`/`SETUP_WITH` bytecode layouts. Both now inject at explicit lock/event/snapshot semantics instead of interpreter-specific offsets, while preserving the original race and prearmed-retention assertions.
- Current-head fixed-range and dirty-tree reviews found unbounded legacy cleanup transitions, staged-recovery ancestor traversal and descriptor-close uncertainty, a hidden Python 3.10 cleanup-secondary chain, shared waiter/control-flow state, late lease publication, dead-worker cleanup ownership, false terminal publication, unbounded persistent-settlement chronology, false source terminality, path-bearing structured cleanup errors, stale effective roots, and a post-timeout proof/scope race. New deterministic regressions prove bounded non-destructive retention, real-decision-only coordinator handoff, single synchronous takeover, root-to-child descriptor anchoring under symlink/retarget and real forwarded-signal races, descriptor-only unknown-close recovery, sticky source convergence, persistent source-close disposition, a non-mutating acyclic visible fallback diagnostic whose structured output never exceeds 32 nodes, and root-stable late recovery under both slot and direct concurrent merges.
- A final fixed-range Codex review found that off-main terminal settlement treated the helper's no-mask result as a valid handoff. The red/green regression now proves explicit inspection-inconclusive failure without a `pthread_sigmask` call, and the macOS integration regression proves the refresh lock is abandoned rather than released.
- A canonical clean-context review found that persistent source-anchor handoff failure could keep terminal proof inside its retry loop forever. Exact Python 3.13 regressions now prove bounded handoff and terminalizer failure, interruption-safe no-close residue publication, bounded retry of residue-diagnostic observation, stable repeated close refusal with live retained descriptors, typed error chronology, and `CLOSED` precedence over a late residue settlement.
- The subsequent dirty-diff review found that failed synchronous takeover could retain its cleanup claim and that a fallback observer could see a diagnostic later replaced by settlement. Public two-call dead-worker takeover and barrier-controlled publication races now prove retryable claim ownership and a single stable residue identity; the review rerun reported no findings.
- Python 3.13 refresh-lock, Linux runtime, and provider suites after the final dirty-tree review fixes: 984 tests passed with 4 skips.
- One full-suite run encountered the unchanged state cleanup-lock concurrency test's identity-race error. The state implementation and test have no branch diff; a focused 20-run stress check reproduced the pre-existing intermittent result (16 passes, 4 failures), so this branch does not claim to fix that unrelated race.
- Python 3.13 final full suite after merging the current `master`: 1,421 tests passed with 4 skips.
- Python 3.13 final full suite after the off-main signal-mask fix: 1,424 tests passed with 4 skips.
- Python 3.13 Linux runtime suite after the bounded source-terminal proof fix: 241 tests passed with 3 skips.
- Python 3.13 provider suite after the final source-terminal proof review fixes: 603 tests passed with 3 skips.
- Python 3.13 final full suite after the final source-terminal proof review fixes: 1,431 tests passed with 4 skips.
- GitHub Actions on commit `d6f037f` exposed Python 3.10-only `add_note` fallback expectations on both Ubuntu and macOS. The fix keeps the production fallback evidence user-visible and makes assertions accept the bounded typed diagnostic/cause representation; no local Python 3.10 run was performed.
- Python 3.13 fallback-focused regressions passed, followed by 150 refresh-lock tests, 241 Linux runtime tests with 3 skips, and 603 provider tests with 3 skips.
- An exact Python 3.13 full suite after the initial cross-version fallback fix passed 1,431 tests with 4 skips.
- The final dirty-diff review found that three legacy fallback boundaries could re-expose a suppressed context or clear the mask-restoration diagnostic when cleanup control flow won. Both diagnostic insertion helpers now preserve only a context that was already visible, and the cleanup-selected branch re-raises from its exact mask diagnostic; sensitive-path and full-traceback regressions pass, and the review rerun reported no findings.
- The final Python 3.13 full suite after those review fixes passed 1,432 tests with 4 skips.
- A clean-context fixed-range review then found that lease terminal snapshots and descriptor settlement were still executed without a retry bound, while the common process fallback could re-expose a context suppressed with `raise ... from None`. Bounded terminal proof now publishes coordinator completion separately from resource terminality, accepts successful descriptor settlement without another snapshot, returns inspection-inconclusive after exact retry budgets, preserves the first control-flow identity, and never reconciles a foreign unresolved handoff.
- The same legacy exception-chain audit closed equivalent suppressed-context exposure in Linux cleanup, provenance deadline cleanup, provider persistence, and provider detached-error rendering. Sensitive-path regressions format the complete traceback and prove the hidden context stays absent while an originally visible context remains visible.
- The final dirty-diff review found one post-terminal fallback snapshot in control-flow replay. Terminal publication now caches both the proof bit and recovery diagnostic atomically, and every later caller consumes only that cache; the review rerun reported no findings.
- Python 3.13 module suites after these fixes: common 64 tests, provenance 89 tests, Linux runtime 247 tests with 3 skips, and provider 605 tests with 3 skips. No local Python 3.10 run was performed; Python 3.10 remains covered by GitHub Actions and by Python 3.13 fixtures that explicitly disable `add_note`.
- The final Python 3.13 full suite after bounded lease proof, cached terminal recovery, and suppressed-context fixes passed 1,443 tests with 4 skips.
- Python compilation, the Linux launcher C `-Werror` syntax check, Ruff lint, skill validation, project-journal validation, and `git diff --check` passed.
- After merging `master` at `7e8a718`, semantic conflict resolution preserved both the descriptor-bound reviewer launch and the refresh-transaction lifecycle. Python 3.13 focused suites passed 68 common tests, 249 Linux runtime tests with 3 skips, and 637 provider tests with 3 skips. The first sandboxed full run reached all 1,754 tests but reported four loopback-bind permission errors; the exact full suite rerun with loopback access passed 1,754 tests with 5 skips in 365.238 seconds. No local Python 3.10 run was performed; Python 3.10 remains a GitHub Actions gate.
- The final clean-context review found that descriptor-launch preparation could fail after publishing an `UNKNOWN` process start even though `Popen` had not run, needlessly retaining the shared refresh lock as a stale fence. Lifecycle publication now occurs only after pipe creation, descriptor-command preparation, and inherited-descriptor merging, immediately before `Popen`; regressions prove all three preparation failures leave the owner `NOT_STARTED`, close handoff descriptors, and never publish quiescence. Python 3.13 then passed 71 common tests, 637 provider tests with 3 skips, and the complete 1,757-test suite with 5 skips in 351.568 seconds. No local Python 3.10 run was performed.
- Subsequent dirty-tree reviews found that repeated pending-signal checks still left a final check-to-`Popen` race, a returned handle could be lost at either worker startup or claim boundaries, universal logged paths did not share the same gate, internal descriptors could collide with closed standard descriptors or exceed `select()`'s `FD_SETSIZE`, and target loader variables could execute native code before `G`. The supervisor now uses one universal POSIX gate, a fault-safe descriptor owner, high-descriptor polling, a worker-owned claim/cancel state machine, one end-to-end deadline, and an empty bootstrap environment plus private bounded environment frame. Deterministic regressions cover result-store interruption, delayed worker startup and blocked `Popen`, exact child signal state, user `pass_fds`, status overflow and high descriptors, closed `0`/`1`, descriptor-close ambiguity, large control frames, Python startup injection, real Linux/macOS loader constructors, SIGPIPE restoration, and GO-before/after cancellation semantics.
- Python 3.13 focused validation after the atomic gate and environment-isolation fixes passed 94 common tests, 12 direct gate-launcher tests, 151 refresh-lock tests, 249 Linux runtime tests with 1 skip, 637 provider tests with 3 skips, 27 Claude capability tests, and 89 provenance tests. The strict C11 `-Werror` syntax check, Ruff, Python compilation, and `git diff --check` also passed. No local Python 3.10 run was performed; Python 3.10 remains a GitHub Actions gate.
- The final Python 3.13 full suite for the atomic gate, bounded spawn cancellation, and loader-isolated environment handoff passed all 1,792 tests with 5 host-gated skips in 400.626 seconds. Both skill validators, project-journal validation, full runtime/test Ruff, full Python compilation, the strict Linux-launcher C check, and `git diff --check` passed. No local Python 3.10 run was performed.
