---
id: 20260721-rpf001
title: Named Lane Review Guards
status: completed
created: 2026-07-21
updated: 2026-07-21
branch: codex/daily-skill-friction-20260721-codex-review-workflows-review-policy-final-review-fixes
pr: https://github.com/Joey-Tools/codex-review-workflows/pull/72
supersedes: []
superseded_by:
---

# Named Lane Review Guards

## Summary

- Close the final named-lane safety and evidence-correlation gaps without restoring retired prepared-diff or hidden review gates.

## Current State

- Each local lane now begins with a trusted `materialize-worktree` guard that initializes a private repository, imports only the hard-bounded frozen base/head reachable-object closure, audits that exact inventory before checkout, and materializes only the exact head with hooks, filters, fsmonitor, ambient configuration, and submodule recursion disabled. The existing `validate-worktree` guard is then the first worktree-status query immediately before reviewer launch; clone/fetch/upload-pack, `git worktree add`, or any earlier status query is a safety failure.
- The materializer binds the exact source marker/admin/common/object directories, requires Git 2.45.0 or newer, reads source configuration only as a bounded no-include byte stream, rejects shallow/alternate/promisor dependencies, and exposes the source object store only through a temporary destination-scoped alternate. It caps object count, logical bytes, checkout entry/blob/path bytes, and compressed pack bytes, imports the exact manifest through strict `index-pack`, verifies the destination inventory equals that closure, disables commit-graph and multi-pack-index consumption for every guard call and later bare Claude Git, rejects fsck policy overrides and bitmap state, and runs bounded full object-validity `fsck` before checkout. Its cleanup-to-receipt transaction defers forwarded signals until either a complete flushed success receipt or a complete flushed failure payload names every retained control/worktree path.
- The standalone post-materialization validator still treats smudge-only configuration as inert during its status/read-only phase, but the formal materializer rejects executable smudge configuration before checkout. That narrower distinction preserves validator compatibility without allowing a named lane to execute candidate-selected transforms during construction.
- Both local lanes run a parent-owned tracked-symlink containment check before reviewer launch. Stable in-worktree source links remain allowed, while escaping or unstable links and symlinked guidance are rejected.
- GitHub Codex completion and no-start evidence must bind to the exact request/run or satisfy the sole-unresolved/no-intervening fallback. A full SHA corroborates artifact scope but never identifies which request caused the response, including when an older-head request may execute after a push and review the new current head. Timestamp proximity alone cannot complete or downgrade triple review.
- The direct Claude lane uses a process-only bounded supervisor with a finite deadline, bounded streams, and process-group cleanup. Linux cleanup also reaps a direct child after its process group contains only zombies. Partial, over-limit, timed-out, or incompletely cleaned output is inconclusive.
- The same absolute monotonic deadline now begins before the CLI reads the control prompt through EOF and can only tighten the duration bound. The subsequent worktree Git resolution receives at most the smaller of its normal 30-second cap and the remaining shared budget before process supervision receives what remains. A writer that sends a short prompt but withholds EOF or a slow Git probe that exhausts the remainder becomes an inconclusive deadline result without launching the reviewer, while termination signals during prompt wait become structured `inconclusive` / `forwarded-signal` evidence.
- Worktree validation accepts safe system symlinks in path ancestors while still rejecting a symlink leaf, so canonical paths such as macOS `/tmp` do not become false safety failures.
- Claude process supervision is explicitly limited to the initial supervisor process group and inherited streams. It does not overclaim containment of descendants that deliberately escape with `setsid()` or `setpgid()` and close those streams.
- Claude output publication stays anchored to prevalidated parent-directory descriptors across process launch, rejects parent identity drift without treating content-derived directory metadata as identity, and rolls back through the retained descriptors.
- Tracked symlink targets are read in one aggregate 30-second Git batch with explicit entry, per-target, and total-output limits instead of one process deadline per symlink.
- Materialized gitlinks are rejected before the guard invokes `git status`, preventing a pre-existing gitfile from redirecting that query into external repository metadata.
- Repository-visible `include.path` and `includeIf.*.path` keys are blocked before status or reviewer execution, including inactive conditions and benign or malformed targets. Included values are not accepted as safety configuration; initial bounded repository-identity probes may still parse the target and fail closed, so the guard does not claim a no-read boundary. Other safety-relevant configuration is evaluated only from direct local/per-worktree values with includes disabled. Raw gitlinks without `.gitmodules` or name/path mappings still participate in repeated global `submodule.active` pathspec matching, alongside direct path mapping, per-name active-boolean precedence, and URL registration evidence. Status is forced to honor executable-bit drift even when `core.fileMode=false`. Every direct Git alias and direct executable clean/process filter plus `diff.external`, driver command, and textconv settings are rejected, while unrelated invalid booleans, explicit per-name false, smudge-only filters, required-only settings, and non-command diff metadata do not become false findings. Any parent- or reviewer-owned diff rendering must disable both external diff and textconv.
- Direct `core.fsmonitor` is validated before any status or reviewer Git command. Unset and Git-false values are allowed; built-in daemon, no-value, and path hook values are rejected without execution. Direct local/per-worktree precedence remains supported, so a worktree `false` can safely override a local path.
- Self-policy migration now keeps candidate-head Markdown available as review subject and scoped guidance while pinning the reviewer profile, prompt, guard, launcher, complete in-repo runtime closure, and external Python interpreter to independently trusted control-plane evidence. The runtime package exposes the legacy helper entrypoint lazily. The guard's default profile compiles only its three manifest-bound package/runtime sources; the Claude preflight profile uses an exact nine-source closure, and the stream validator profile uses an exact seven-source closure. Every profile records exact `.py` origins and keeps the package closed to ordinary import resolution, so same-name top-level modules, native extensions, and prebuilt bytecode cannot enter the closure. The release key, compatibility profile, audited schema baseline, capability source, and applicable `fd_exec.py` process companion are retained as initially validated immutable bytes and completely reread immediately before consumer entry. Formal descriptor-backed launches inject the initially bound `fd_exec.py` bytes into `common.py`, validate and compile those exact bytes, and execute them through an isolated `-I -B -S -c` bootstrap without reopening the companion path. Cross-read comparison protects exact content rather than inode or timestamp continuity: a same-content ordinary-file replacement remains valid, while any byte drift is rejected. The deterministic control manifest also binds the PR-readiness, GitHub-probe, base-only retarget, and egress-consent decision policy used to interpret review outcomes. The guard is non-executable source with no shebang and requires a revalidated absolute native Python `>=3.10` launched through `-I -B -S` under a fixed clean environment, so ambient `PATH`, `PYTHONPATH`, global/user site initialization, `.pth` files, `sitecustomize`, loader, virtualenv, and shell controls cannot select or preconfigure it. Every formal command resolves that guard from the manifest root through `skills/review-orchestration-playbook/scripts/named_lane_guard`; no flattened `<bundle>/scripts` path is accepted. The formal Claude preflight also ignores ambient `HOME`: it derives and validates the current POSIX account home, then binds that canonical path directly into the selector so ordinary user-level installs remain discoverable under the clean launcher. The bundle's absolute source and publisher release/frozen-commit version accompany a deterministic control-file manifest digest verified before each launch/spawn and after each lane. Candidate-head scripts cannot bootstrap their own formal review; an incompatible new guard activates only after the prior trusted policy reviews, merges, and releases it.
- The Codex reviewer receives one exact sanitized Git argv prefix and cannot use bare Git, alternate wrappers, extra environment state, changed safe flags, or another worktree. Every diff-producing invocation explicitly disables external diff and textconv.
- The canonical direct Claude lane is local-login-only in trusted real `HOME`. It has no API-key or OAuth-token interface, reports forbidden refresh or non-local-only credentials as `blocked-authentication`, rejects npm/NVM shebang shims under canonical provenance, and does not expand trusted `PATH` to accommodate them.
- Test-oriented `run-claude` CLI overrides can equal or tighten, but never raise, the 1,800-second timeout, 64 MiB per-stream, and 256 KiB prompt production caps. Direct Python API callers are bound by the same ceilings.
- Ambient `NODE_EXTRA_CA_CERTS` remains excluded by default. A value-free explicit opt-in validates the configured absolute non-symlink regular file without exposing its path in the guard argv; the direct lane deliberately does not claim the helper's copied/attested CA guarantees.
- Claude output publication uses a lane-unique current-user-owned mode-`0700` directory, temporary signal handlers, and an explicit commit point. A signal observed before commit rolls back the complete pair, while final and temporary cleanup preserve identity drift already observed before unlink. The contract explicitly excludes the non-portable stat-to-unlink race from non-cooperative same-UID writers instead of claiming an impossible conditional unlink.
- The guard does not prepare a diff, inspect ordinary file contents, broker authentication, replace the actual reviewer, or add another PR-readiness gate.

## Next Steps

- None after the canonical PR is squash-merged and the private overlay migration is regenerated from the merged source.

## Evidence

- Fresh-context review of `Joey-Tools/codex-private-workflows#128` found the three boundary gaps before merge.
- The final independent formal review identified four remaining control-boundary gaps: self-policy bootstrap trusted candidate code, repo-visible Git includes, unsanitized reviewer Git, and non-local direct-Claude credential interfaces. A supplemental frozen-diff review then found a fifth: repo-visible Git aliases could still execute commands through the allowed Git prefix. The next whole-range review found a sixth: eager package initialization loaded unlisted helper modules outside the trusted manifest. A further final-head pass found a seventh: the guard's `env python3` shebang still let ambient interpreter controls execute before the guard. The next whole-range pass found an eighth and ninth: `-I` alone still loaded global site initialization, and the ordinary Python importer could consume unmanifested sibling modules, native extensions, or valid prebuilt bytecode. Integrating target branch `eaec097` then exposed the exact-version preflight and stream validator outside that original three-source control boundary. The merged repair added explicit raw profiles and immutable companion-byte injection. The latest target-branch integration replaced that retired exact-version policy with the shared `>=2.1.211,<3.0.0` compatibility range, kept `2.1.212` only as the audited stream baseline, expanded the preflight and validator closures to nine and seven sources, and bound the compatibility profile, capability source, and `fd_exec.py` process companion. It also applies the filesystem-hardening lesson to those companions: no-follow descriptor checks protect each read, while complete bytes—not `dev`/`ino`, `mtime`, or `ctime`—decide whether the content changed. The subsequent whole-range review found a tenth gap: the clean formal launcher omitted `HOME`, but the selector still relied on ambient `HOME` and therefore skipped normal user-level installations. That repair binds a validated current-account home at the guard boundary while preserving the compatibility wrapper's low-level ambient-HOME behavior. The next whole-range review found an eleventh gap: `run-claude` started its shared deadline before prompt input but allowed worktree Git resolution to consume a separate fixed 30 seconds. That repair propagates the absolute deadline through worktree resolution and caps Git at the remaining budget. The following whole-range review found a twelfth gap: several formal examples treated the manifest root as if the skill's `scripts/` directory were directly beneath it, so the published release path did not exist. That repair uses the complete manifest-relative guard path everywhere and validates it against the actual `agents/` plus `skills/` bundle shape. The next whole-range review found a thirteenth gap: a terminal issue comment's full current SHA proved artifact scope but was incorrectly allowed to stand in for request causality. The repair now requires exact request/run binding or the sole-unresolved/no-intervening fallback even when the response names the current head, because an unresolved older request may execute after a push and review that same head. The fourteenth gap was earlier still: parent-created worktrees and ordinary clones could execute source-selected checkout hooks, filters, or fsmonitor before the final validator ever ran. The repair moved repository construction into the trusted guard and reserved the first status query for the final validator. The next formal whole-range review found that this clone still copied the source's entire unrelated object library without an aggregate disk bound, that recursive cleanup could let `RecursionError` replace terminal evidence, and that an initial output-temporary `fstat` failure leaked its leaf. The current repair replaces clone with exact bounded reachable-object import, classifies ordinary recursive-cleanup exceptions by retained path while preserving control-flow exceptions, and retries only to recover a temporary-file identity for conditional cleanup while retaining an unverified leaf when identity cannot be proved.
- The latest formal whole-range review found two additional control-boundary gaps: the verified `fd_exec.py` process companion could still be reopened through a mutable path at launch time, and four outcome-decision policy files were omitted from the trusted manifest. The repair binds and executes the initially verified launcher bytes, covers replacement after final revalidation with an executable malicious-symlink regression, and makes every omitted policy file change the canonical bundle digest.
- All 125 focused named-lane guard tests passed after the reachable-object import, target-branch integration, and bound-launcher repairs. They cover the exact nine-source preflight and seven-source validator closures, wrapper and ordinary-import exclusion, companion symlink/directory rejection, same-content ordinary-file replacement, same-inode/same-size byte drift, post-validation path replacement, bound-byte consumer injection, scrubbed-environment account-home derivation, worktree Git receiving only the remaining shared deadline, and transactional rollback for both optional namespaces. All 49 compatible-version preflight tests and all 48 stream-validator tests also passed.
- The 125-test named-lane suite covers exact reachable-object import, exclusion of a large unrelated side ref/object under a low pack cap, all seven trusted resource ceilings, SHA-1/SHA-256 and linked-worktree sources, source and destination shallow/alternate/promisor rejection, private endpoint refs, absence of clone/fetch/upload-pack and pre-validator status commands, recursive-cleanup failure plus signal races, transient versus persistent output-temporary inspection failures, subcommand-specific thread-start failure classification, and execution of the original bound launcher after final validation even if its path is replaced by a malicious symlink.
- The detached-boundary regression atomically publishes its child PID before a pipe handshake permits the child to detach; its `finally` path recovers that PID from the file after either normal return or an exception.
- Seventy-five canonical-profile contract tests passed after the merged manifest, optional raw-profile, bound-companion, exact control-closure, release-layout path, floating compatible-version policy, and full-SHA/request-causality assertions were added. They cover current-request GitHub evidence correlation—including unresolved older-head requests whose delayed result names the current SHA—the narrow guard boundary, effective `core.fsmonitor`, immutable production resource ceilings, direct-Claude process limits, current-head admission, descriptor-based logging, all three manifest-bound runtime closures, and resolution of every formal guard command from a real bundle root containing both `agents/` and `skills/`; private-profile coverage runs again after source sync.
- The latest loopback-enabled full review-policy suite passed all 2,070 tests with five skips in 548.105 seconds. The directly affected `common` plus `fd_exec` set passed 111 tests, the named-lane set passed 125 tests in 158.406 seconds, and the 75 contract tests passed. The preceding integrated run passed all 2,066 tests with five skips in 574.823 seconds. During an earlier repair iteration, one unchanged concurrent-cleanup test hit a transient identity race; it passed five consecutive exact reruns and did not recur in any completed full-suite run.
- Both modified skills passed the installed OpenAI quick validator. Python compilation, Ruff lint, project-journal validation, and `git diff --check` passed. Ruff 0.13.2's whole-tree format check reports the same ten pre-existing files on clean target commit `217a357`, so this PR deliberately does not introduce an unrelated formatter-only rewrite.
